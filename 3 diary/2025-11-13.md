# UHTCharacterStateManagerComponent
1 
```cpp

/*
1 FHTEnterStateRequest 请求进入某种状态的结构体，有要进入状态的枚举，进入参数，哪端先进入状态
2 EHTStateExecutionPolicy 进入状态的策略枚举，本地优先进入，服务器优先进入，客户端优先进入
3 ON_SCOPE_EXIT 方法中写，异常退出的宏
4 FHTStateOperationResult 进入状态的返回值，成功，失败，无效状态，已经处于此状态，没有找到状态，存在阻塞状态，存在新进入状态，存在不可打断状态
5 
*/
```