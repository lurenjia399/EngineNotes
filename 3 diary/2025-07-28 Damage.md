# 1 伤害流程
1 普攻激活 GA_Female051_Melee 这个GA
2 GA_Female051_Melee 这个GA激活 UAbilityTask_PlayMontageAndWaitForEvent 这个GT，这个GT字面意思就是播放Montage，在这个Montage期间等待Event。这个Event是配置在Montage中的 UNotifyState_SendGamepPlayEvent，Event中有Tag。
3 在播放Montage过程中收到了Event，就会调用到GT的 EventReceived 方法广播，进而执行蓝图中 的 OnReceiveEvent 方法
4 蓝图中的 OnReceiveEvent 方法中 会根据Tag找到GA中的 EffectContainerMap 配置，给自己添加Tag对应的GE。 蓝图中的 OnReceiveEvent 方法中 还会根据Tag找到GA中的 SpawnActorMaps 配置，生成Tag对应的UObject
5 在给自己添加GE的流程中，如果是单人模式，会创建一个FHTClientPredictionFightData对象，来保存客户端击中目标的各种信息。
6 会把记录的信息发到服务器来进行校验，是 SendPredictionDamageInfoToServer 这个方法。

```cpp
class FHTClientPredictionFightData
{
public:
	//受击者本地缓存伤害信息
	TArray<FSavedDamageInfoPtr> SavedDamageInfos;//缓存的伤害信息
	TArray<FSavedDamageInfoPtr> FreeDamageInfos;

	FSavedDamageInfoPtr PendingSavedDamageInfo;//这次伤害流程执行的伤害信息
	FSavedDamageInfoPtr LastClientAckDamageInfo;

	TWeakObjectPtr<class AHTPlayerController> m_ClientPlayerController;
	
	int32 MaxFreeCount;					// Limit on size of free list
	int32 MaxSavedCount;				// Limit on the size of the saved move buffer
	int64 ClientDamageInfoIndex = 0;
	
private:
		FSavedDamageInfoPtr AllocateNewDamageInfo();
};

class FSavedDamageInfo
{
protected:
		//消息ID
	int64 DamageIndex = INDEX_NONE;

	bool bSingleData = false;

	//攻击者
	TWeakObjectPtr<AHTAbilityCharacter> DamageCauser;// 攻击者Character
	TWeakObjectPtr<AActor> DamageCauserActor;// 攻击者Actor

	//当次伤害缓存所有对象的信息
	ClientTargetDataMap TargetDataMap;

	//等待发送的数据
	FNetFightInfoContainer    NetFightInfoContainer;
};

//上报行为同步信息容器
USTRUCT()
struct FNetFightInfoContainer
{
	int64 MsgIndex = 0;
	FHTClientActiveGE TriggerGE;// 触发的GE信息，GE唯一id，GE的层数，GE的等级
	FCharacterForNet NetSourceActor;//攻击的信息（攻击者CharacterID,召唤物索引）
	HTFightData::ERPCType RPCType = HTFightData::ServerFight;
	double TimeStamp = 0;//构建这个信息时的时间戳
	
	TArray<FNetFightInfoPtr> TargetData;//同步的数据
};
```
1 客户端UHTAbilitySystemComponent::ApplyEffectContainerSpec起始，如果是单机客户端就会执行CreatePredictionDamageInfo，往本地预测伤害对象中添加一个伤害信息（FSavedDamageInfo）。然后就对打中的目标应用这个GESpec，最终会执行到PostDamageAttributeExecute这个属性计算的方法（这个方法也会执行伤害冒子等客户端表现了），这个方法里也会执行到 MakeServerReplicatedReduceHpInfo这个方法，继续往本地预测伤害对象中添加信息，SendPredictionDamageInfoToServer方法
2 UGameplayAttributeSet::PostDamageAttributeExecute