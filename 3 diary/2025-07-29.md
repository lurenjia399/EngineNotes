AHTAISpawnManager 

1 AISpawnmanager 继承 AHTSpawnManagerBase ，AHTSpawnManagerBase 继承AActor和
ICacheHTTargetPointDataInterface (这个是AHTTargetPoint的接口类)
2 
```cpp
/*
USTRUCT(BlueprintType)  
struct FAISpawnEntry  
{
	//怪物蓝图
	UPROPERTY(EditAnywhere, BlueprintReadWrite)
	TSoftClassPtr<class AHTAICharacter> AITemplate;

	//怪物数量
	UPROPERTY(EditAnywhere, BlueprintReadWrite)
	int32 Count = 1;

#if WITH_EDITORONLY_DATA
	//刷怪点，该配置会覆盖随机点
	UPROPERTY(EditAnywhere, meta = (EditCondition = "!bUseDataTable"))
	TArray<class ATargetPoint*> SpawnPointsOverride;
#endif // WITH_EDITORONLY_DATA
}
struct FAISpawnWaveEntry  
{
	//刷新怪物信息
	UPROPERTY(EditAnywhere, BlueprintReadWrite)
	TArray<FAISpawnEntry> SpawnEntries;
}

//波次
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = Spawn)
TArray<FAISpawnWaveEntry> SpawnWaveEntries;
*/
```
3 
```cpp
/*
1 在刷怪器beginplay的时候将自己添加到SpawnCache里了，而这个cache是存储在WorldSetting里的
UHTAISpawnCache* SpawnCache = UHTAISpawnCache::Get(GetWorld());
if (SpawnCache)
{
	SpawnCache->AddAISpawnManager(this);
}
2 在beginplaye里还会初始化条件包 UHTUtil::InitSpawnReset(this, ResetConditionEntrie);
3 BeginSpawn 开始刷新的方法,一系列判断条件，能不能刷，刷新条件
4 然后就是刷新波次，SpawnQueueMgr->AddSpawnToQueue(this, SpawnParams);将信息添加到生成队列里
5 SpawnQueueMgr 这个也是存储在WorldSetting里的，和SpawnCache一样,初始化的时候设置了自己一系列的TickFunction
MyTickFunction.TickGroup = TG_DuringPhysics;
MyTickFunction.bStartWithTickEnabled = true;
MyTickFunction.bCanEverTick = true;
MyTickFunction.Target = this;
MyTickFunction.TickInterval = TICK_MEDIUM;
MyTickFunction.SetTickFunctionEnable(true);
if (GetWorld())
{
	MyTickFunction.RegisterTickFunction(GetWorld()->PersistentLevel);
}
6 static int SpawnNumPerTick = 8; 每帧生成的数量
// 根据平台调整SpawnNumPerTick值
if (UHTUtil::IsMobilePlatform())
{
	// 移动平台减少三分之二，即保留三分之一
	SpawnNumPerTick = SpawnNumPerTick / 3;
}
else if (UHTUtil::IsPSPlatform())
{
	// PS平台减少二分之一，即保留二分之一
	SpawnNumPerTick = SpawnNumPerTick / 2;
}
7 


*/
```
4 
```cpp
/*
1 UHTSpawnGlobalQueue 这个就是个Object，他在生成的时候创建
*/
```
# 问题：
1 客户端加载地图时创建了AISpawnManager，服务器加载地图也会创建吧，这两个AISpawnManager是相同的？

