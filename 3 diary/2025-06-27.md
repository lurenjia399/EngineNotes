音游结构 BP_DrumPlayActor
1 数据结构
```cpp
// UHTUI_DrumPlaylistModule::OnBtnClickedButton
/*
1 配置的数据在DA_Set的DA_Drum里面，通过DrumAsset = Cast<UHTDrumDataAsset>(UHTDataAssetSet::GetDataAsset(TEXT("DA_Drum")));来索引

UHTUI_DrumPlayMain 这个是主界面文件
UHTUI_DrumPlayButton 这个是主界面上四个鼓的按钮
UHTUI_DrumPlayPoint 这个是往下落可点击的那个界面
UHTUI_DrumChannelItemSlot 这个看上去没用都
AHTDrumPlayActor 这个是场景中的架子鼓
AHTDrumChannelBaseActor 这个是场景中架子鼓上的ChildActor
AHTDrumPointBaseActor 场景中的每个point

UHTDrumDataAsset 这个是DrumDataAsset数据表
FDrumSongData 这个是DT_DrumSongDataTable中的一行数据

// 看上去beat是音乐的节奏点,就是可以让玩家点击的时刻？ point是这个时刻的抽象化，可以是点击，长按这种不同的表现形式？

// 非运行时保存的音乐数据
FDrumSongPointInfo 这里记录的是一首song里面所有Beat的数据
FDrumBeatPointInfo ：Beat的数据,每个Beat有可能有四个slot可以点击，所以也是一个map存储了四份数据
FDrumChannelInfos Beat在某一个slot上保存的Point数据
FDrumChannelPointData Point数据
// 在运行时，加载的数据
FDrumPlayChannelData 保存在slot上数据
FDrumPlayPointData slot上PointData
*/
```
2 DA文件加载
```cpp
/*

class UHTDrumDataAsset : public UHTDataAsset
{
	QUICK_DECLARE_DEFINE_QUERY_DATA_TABLE(DrumSong);
}
// 我们在DA中声明宏，然后就可以通过QueryDrumSong(InSongId)方法来快速查找DA中DrumSongDataTable字段所配的数据中的一行。
*/
```
3 处理点击的方法
```cpp
/*
// 通过运行时point的数据来处理点击事件
bool FDrumPlayPointData::OnPointPressed(...)
// 场景中每个point响应点击完成回调
void AHTDrumPointBaseActor::OnPlayPointResultCallback(const FDrumPointResultData& InPlayResult)  

*/
```
4 资源文件
```cpp
/*
DrumDataAsset
StoreSongDataTable
*/
```
5 运行时修改datatable文件
```cpp
/*
UUUI_DrumEditor这个类可以参考
UUUI_DrumEditor::HandleImportAudioEventJson()
*/
```