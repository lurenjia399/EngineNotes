# 1 
输入文件配置：
1 IMC_Default_KBM 按键映射文件
2 BP_GameData 也有按键是在这个文件里
3 BP_PlayerControllerBase 这个里也有按键
4 controller里面可以配置AbilityInput，对应一个DT_AbilityInput

# 2 蓄力流程
1 AHTPlayerController::SetupInputComponent 这个方法里面绑定我们的输入系统
2 在 DT_AbilityInput 这个文件中配置的都会在 AHTPlayerController::AbilityInput_Pressed 这个方法中响应按下，通过表的索引来区分，看上去都是些技能相关的东西。
3 UHTCharacterAimmingState 这是瞄准状态
4 按G键在 NativeAbilityInput_Pressed 方法中进入瞄准状态（里面切换动画层）,并不会激活GA
5 UAbilityTask_HTAiming 这个是进入瞄准的GT
6 UAbilityTask_JumpSection 这个是技能连招的GT
# 白藏
```cpp
/*
1 普攻激活 GA_Cang_Melee 这个GA。GA里面会激活 HTWaitConditionJumpSection 这个GT
2 长按普攻，会在 HTWaitConditionJumpSection 这个GT里面根据 JumpSectionMap 配置跳转到 Branch1。
3 在第二下普攻后长按普攻，会跳转到 Branch2。
4 如果跳转到Branch1或者Branch2之后，就会设置 UCangLogicObj 这个里的 
SkillChoose = Cang_SC_TriggerA。
5 在普通状态下，按下E的时候， GA_Cang_Skill 这个GA会被激活。
6 在Cang_SC_TriggerA这个状态下，按下E的时候，GA_Cang_SkillA 这个GA的优先级高会被激活。
GA_Cang_SkillA 这个GA激活后会跳转到 SkillA 这个Sction上。
7 按下闪避，会激活 GA_Cang_Evade 这个GA，GA一激活就会设置 UCangLogicObj 这个里的 
SkillChoose = Cang_SC_TriggerB。
8 在Cang_SC_TriggerB这个状态下，按下E的时候，GA_Cang_SkillA 这个GA的优先级高会被激活。GA_Cang_SkillA 这个GA激活后会跳转到 SkillB 这个Sction上。
*/
/*
1 普攻3次，会给自己添加 Buff_Cang_SpawnWordMelee 这个GE，这段逻辑是在 UCangLogicObj的tick里。
2 Buff_Cang_SpawnWordMelee 这个GE会在生效时，创建 GA_Cang_SpawnWordMelee 这个GA。这个GA在激活的时候，会生成 BP_CangSkillWordC 这个Actor，这段逻是在UHTAbilitySpawnHTActor::TriggerSpawnAbilityActor 方法里面
3 TriggerSpawnAbilityActor 这个方法里面会找AttackTarget，也就是将 BP_CangSkillWordC 这个Actor上生成到攻击目标上。
4 释放大招会生成 GA_Cang_UltraSkill 这个GA，这个GA收到Tag后会创建 BP_CangUltraSkillField 这个Actor，是我们的那个大招领域。
5 领域中有Sphere碰撞盒，当与BP_CangSkillWordC触发overlap后，会执行BP_CangSkillWordC里面的 OnEnterField 方法，销毁掉BP_CangSkillWordC。
*/
```
# 翳
```cpp
/*
1 自己身上有 Buff_Skia_ShadowControl 这个GE，认为是进入特殊潜行状态，给自己加的
2 GE_Player_Skia_SkillShadowAtk_Damage 这个伤害GE，在特殊状态下攻击到有影子的目标会打出伤害，给目标加的
OnGameplayEffectAppliedDelegateToSelf
*/

```
1 按下E技能，会激活 GA_Skia_Skill 这个GA。在Default动作中会SendGameplayEvent，会在GA的GameplayEffects中收到。其中Skill.1和Skill.2都会给打中的目标添加GE（一个伤害GE，一个播击退效果的GE）。Skill.3会给目标添加伤害GE，击退效果GE，和3个影子叠加GE（给目标添加了持续伤害吧）。Skill.4会给自己添加Buff_Skia_ShadowControl和Buff_Skia_ShadowControl2这两个GE。
2 Buff_Skia_ShadowControl 这个GE的作用是：1他首先会激活 GA_Skia_ShadowEvade 这个GA，通过这个GA播动作。2他会激活GC，进入潜行，潜行是角色身上挂的UHTSneakEffectComponent这个实现的。3他的Buff效果有几个分别是普攻移除此Buff，且角色移除此buff，在Buff开始时候添加Buff_Skia_ShadowAtk（这个Buff会持续挂在身上并周期性检测击中目标，检测到了目标，还会检测目标身上是否有影子，如果有影子就给目标身上挂一个伤害Buff）。
3 教学副本里潜行状态下释放大招会被打断bug。按下大招激活 GA_Skia_UltraSkill ，在GAComponent里面移除 Buff_Skia_ShadowControl1 这个Buff。Buff_Skia_ShadowControl1 这个Buff会激活 GC_Skia_EvadeSneek 这个GC，在 Buff_Skia_ShadowControl1 移除的时候也会移除GC，移除GC的时候会播动作，这个动作打断了大招的动作，导致大招提前结束。

# 浔
```cpp
/*
1 普攻激活 GA_Jin_Melee 这个GA。GA里面会激活 PlayMontageAndWaitForEvent 和 HTWaitConditionJumpSection 这个两个GT
2 长按普攻，GA_Jin_Melee 会在 HTWaitConditionJumpSection 这个GT里面根据 JumpSectionMap 配置跳转到 Branch1。
3 BPUI_Jin_SubUltraSkillEffect 大招的ui
4 浔释放大招会播放Jin_UltraSkill这个montage，这个动作里会添加Tag（Event.Montage.Player.UltraSkill.Jin.EnterTimeStop），这个Tag的添加回调是在UJinRecordSkillLogicObj::OnTriggerUltraSkillPauseTimeEvent这个方法中，这个方法会将GA_Jin_UltraSkill_Melee这个GA赋予到浔身上
5 在大招时停期间，GA_Jin_UltraSkill_Melee 这个GA里的所有动作都会添加Tag(Event.Montage.Player.UltraSkill.Jin.CountSkill)，这个Tag的添加回调是在UJinRecordSkillLogicObj::OnTriggerUltraSubSkillEvent这个方法中，这个方法会广播delegate，告知ui，记录了几次攻击。
6 时停
*/

```
# 娜娜莉
```cpp
/*
1 释放大招，会激活大招A_Nanally_UltraSkill的GA，这个GA在播放大招动作时候会由动画通知来触发Event.Montage.Player.UltraSkill.5这个Event，这个Event会给自己添加GE_Nanally_Cat这个GE。
2 UNanallyLogicObj 逻辑Obj里面会监听GE的添加，如果是GE_Nanally_Cat这个GE被添加的话，就会生成对应的Actor，是 NanallyUltraSkillCat 这个
3 在娜娜莉的任意攻击montage中，都会配置UNotifyState_NanallyCatFunc这个动画通知，这个动画通知会在满足条件（角色身上是否有 GE_Nanally_Cat）时触发
4 动画通知触发后，就会播放对应猫的montage，在猫的蒙太奇里面会触发Event，UNanallyLogicObj这个里面会监听对应的Event，来触发伤害。
*/
```
# 早雾
```cpp
/*
1 吸力是怎么实现的？
2 UHTAbilityTask_SagiriSwallowTarget 在普攻GA激活的时候创建GT，查找可吸怪，然后广播
3 ASagiriWeapon::SwallowTarget，武器，吸怪的具体逻辑
4 ASagiriWeapon上SwallowSlots的这个是有吸怪cd的，看上去六名能吸3个，平常只能吸1个
3 buff_Trial_LockHP100_BP ，怪的无敌buff，mon_01_BP_Trial
4 浮空怎么做的?
5 破坏物怎么做的？ AHTBreakableActor_InstancedMesh?，
*/
```

# 小吱
```cpp
/*
1 吞噬功能：GA_Chiichan073_Melee GA上有SpawnActorMaps（生成actor的map），会在Branch的片段中生成AHTChiiChanActor这个Actor，吞噬是由这个Actor实现的。（蓝图是这个Chiichan_Branch_Actor）
2 长按普攻会创建吞噬Actor，这个Actor有一段内塞尔曲线，曲线运动结束后播吞噬动作，在动作中有动画通知，通知执行StartDevour吞噬行为。
3 StartDevour 吞噬行为会给目标添加伤害GE，并往服务器发送RPC，让服务器执行UChiiChanLogicObj::ServerRecordDevouredActor这个方法
4 UHTGAComponent_FollowUpComboAbility 小吱e技能应用了这个。e技能会保存当前释放片段的下一个片段，等下一次触发e技能就会触发保存的这个。
5 UHTGameplayAbility::SetSkillStartSectionName 每次释放技能都会走这个吧？会确定这次释放的片段是哪个。
*/
```
# 九原
```cpp
/*
1 Buff_Kuhara_PowerBullet 强化子弹buff，Melee3会添加一个强化子弹
2 Buff_Kuhara_AddPowerBullet_Skill e技能添加的种子buff，种子buff会添加4个强化子弹
3 GA_Kuhara_PerfectEvade 完美闪避GA的Atk也会添加Buff_Kuhara_AddPowerBullet_EvadeATK 种子buff，这个种子buff会添加2个强化子弹
4 e技能会对目标打出伤害，在伤害GE_Player_Kuhara_Skill3_Damage的GE中会对伤害目标执行Con_Kuhara_AddSeed，对目标添加Buff_Kuhara_Seed这个种子buff，种子buff持续时间5s

*/
```
# 哈索尔
```cpp
/*
1 长按鼠标触发Branch分支，在Branch分支动作最高点检测，上一次击中位置和当前角色位置是否小于10m
如果小于就触发BranchEnd分支
2 
*/
```
# 安魂曲
```cpp
/*
1 按G触发GA_Lacrimosa_SwitchSkill这个GA，这个ga会激活Buff_Lacrimosa_GiveMeleeModA这个buff，这个buff会激活GA_Lacrimosa_MeleeModA这个ga，这个ga就是远程普攻的ga
2 LacrimosaMeleeJumpSelection 这个蓝图控制普攻跳转，在执行蓝图的时候检查身上不同buff，然后跳转不同普攻
3 偷取技能是通过UStealTargetAbilityGEComponent这个实现的，会把偷的ga信息记在角色身上，安魂曲是GE_Player_Lacrimosa_BranchPraojectile_Damage在这个伤害ge里执行的。
4 安魂曲身上配置GE_Lacrimosa_GrantChangeAvatarSkill这个buff，在这个buff持续期间会一直存在GA_Lacrimosa_ChangeAvatar，释放偷取的技能是通过GA_Lacrimosa_ChangeAvatar这个ga实现的，

// 这里忽略目标打不出伤害
//忽略不可被选中目标
	if (bIgnoreCantBeSelected)
	{
		if (!TargetCharacter ||
			(TargetCharacter->GetHTCharacterAttributeSet() && TargetCharacter->GetHTCharacterAttributeSet()->
			                                                                   GetCantbeSelected()))
		{
			return false;
		}
	}
*/
```

# 完美闪避
1 进入通用教学，cheat RequestTeachInstance General
```cpp
if (HTPlayer)
{
	HTPlayer->ServerTriggerPassivityGamePlayEffect(true);
}
1 BP_TimeDilationVolumeActor 时间结界蓝图
/*
林克时间实现
1 主角的闪避montage里面会配置UNotifyState_PlayerTriggerLinkTime，这个通知会设置主角身上的UHTSkillEffectComponent 这个里面的bNotifyTriggerLinkTime标志位
2 怪物的攻击montage里面会配置UNotifyState_TriggerLinkTime，这个通知会设置怪物攻击目标的LinkTime，让被攻击目标触发林克时间。
3 总的实现思路就是，主角会在闪避动作的一段时间内等待触发林克时间，怪物攻击时会告诉攻击目标可以触发林克时间，如果此时被攻击目标正处于等待出发林克时间，则触发林克时间，激活林克时间的GA，播相应的动作。

UHTEnhancedPlayerInput::InputKey
*/
```
# 异能环合
```cpp
/*
BPUI_HeterochromeSlot 异能环合的slot
UHTUI_MonsterHeadUpInfo 怪物头顶血条
*/
```
# 怪物释放特殊技能
```cpp
/*
1 怪物身上会配置DefaultWeaponList，会在beginplay的时候创建出武器列表，并装备第一把武器
2 怪物身上会配置BossStageEntries，这个是怪物的阶段配置，每次在切换阶段的时候会判断WeaponSlot这个武器id是否是当前装备的，不是就切换武器
3 AHTAIController::GetCurrentBestSpecHandle 在AIcontroller中会获取当前可以使用的技能，这个方法会遍历当前武器上配的技能，也就可以释放特殊技能了
*/
```

# 其他
1 UKismetSystemLibrary::GetGameTimeInSeconds(this) 获取当前世界时间吧
2 
```cpp
/*
{
	do
	{
	}while(false)
	
	和直接执行有啥区别？
}
*/
```
3 
void AHTPlayerCharacter::AddStartupGameplayAbilities() 这个方法会将身上的ga初始化，并调用GiceAbility方法 ->
UAbilitySystemComponent::GiveAbility 这个赋予会把GA添加到ActivatableAbilities这个数组里
4 bp_gameData里面 StateObjectClassTemplateArray 是个状态管理配置
5
```cpp
/*
在跳到教学关卡的时候，TeachCloneLogicActor开启了空间加载，并且是一个WorldPartition关卡
1 服务器在开启玩家碰撞的时候，Actor还未加载响应不了overlap事件
pHTCharacter->OpenPlayerCollisionWithWorldDynamic();
2 Actor在开启碰撞的时候，因为先开启碰撞，后注册overlap回调事件，也导致overlap事件不会响应
3 overlap不会响应导致了不能切换试用角色，副本流程走不通。
*/
```
6 AHTAbilityCharacter 这个里面的 CustomAbilityLogicObjs 这个属性可以配置LogicObjList，并且他用UROPERTY标记了是一个Instance，他这个是每个角色自定义的逻辑Obj。
7 UHTGameplayAbility 这个里面的 GAComponents 这个属性可以配置自己定义的行为，也是一个Instance。
8 UHTUI_SkillController 这个应该是角色界面的ui吧。
9 
Buff_CantUseAim_TA_Infinity
Buff_CantUseSkill_TA_Infinity
Buff_CantUseUltraSkill_TA_Infinity
这几个就是玩家禁用的GE吧
10 DT_SkillLimitPropData Tag对应的限制条件
11 AHTPlayerState::PossessedPlayerAfterChangeLocation 这个方法是切换角色会执行的方法
12 cheat SetTrialID TeachChar_Mint 命令行切换试用角色
13  UHTEnhancedPlayerInput::InputKey 最早响应输入的地方

# 待做
1 InitializeRuntimeData 这里方法里面的返回值有啥用？
~~2 slot单挪出来，给每个阶段的slot使用~~
~~3 教学结束的最后一个阶段，需要显示slot和关掉所有的buff~~
~~4 Tag类为啥在server监听的，服务器也能监听到？~~
~~5 生成退出actor的位置，改成配transform~~
6 白藏的战斗状态下前闪避和后闪避不对
~~7 重复充满能量~~
~~8 阶段添加能量，默认给-1吧，填0就设置能量为0。~~
~~9 法迪娅的被动，需要保证是2号位角色被攻击，这个之后优化吧，现在是法迪娅血量减少就算被攻击~~
~~1 木桩怪触发不了异能环合~~
~~2 木桩怪失衡值改变不了~~
~~3 打木桩怪回不了异色值，大招能量~~
~~4 切角色ui上正在播动画，引导的界面显示不出来~~
~~5 异能环合的时候，触发的引导必须要在第一个引导出来之后~~
6 玩家死亡，复活逻辑没写吧，看看好不好使
~~7 HTPlayerState_SeamlessSingleClone 720行改坏了，没拷贝~~
~~8 破防触发不了，破防阶段还会触发极限闪避~~
~~9 在异能环合阶段结束，还会触发引导~~
==6 sdklogin 一键解锁载具 server UnlockAllRacingInviteMode==
~~7 在最后自由练习阶段，直接关掉exe，会崩溃，崩在dataTable上？~~
8  这阶段法迪娅大招需要没有cd并且无线充能，现在依然有cd
并且这阶段开始时需要吧法迪娅大招充满，现在只充了70%
9 这阶段法迪娅大招状态下平a五下依然有可能会出现之前那种有一次平a没算进去的bug|||||||||||||
10 UHTUI_Loading在显示后，在第一次tick里面会直接获取现在需要异步加载的package，然后就显示进度，预加载角色和加载角色里面需要等的就是外观，loading的第一次tick的时候还没加载外观，导致进度条很快100%了。
11 副本里死亡，需要处理啥东西么

12 Buff_WishWell_Anomaly_032 测试这个buff
13 Buff_CantUseSkill_SLY_Infinity 禁止使用技能
