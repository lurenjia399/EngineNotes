1
AHTPlayerState::SetTrialCloneID(const FName& InTrialCloneID) 这个看上去抽卡试用角色

TrialCharacterType_Clone 试用角色的枚举，切换场景那种 

AHTTrialCharacterLogicActor 这个是挂在副本里的，这个里添加咱们的教学逻辑
SetTrialCloneID 这个是切换试用副本的逻辑吧
boss_05_BP_Weekly 一个怪物蓝图，可以配在刷怪器上面

测试方法：
cheat TestRequestWeeklyCloneScene  WeeklyClone_Boss05 1
cheat RequestTeachInstance TeachCloneTest
cheat RequestTeachInstance 1003
cheat FinishTeachInstanceState 0
cheat DebugTeachInstanceStateInfo 1

测试配置：
CharacterDataAsset 这个是角色总表，在bp_GameData里配置的
CharacterDataAsset
{
	DT_TrialCharacterDataTable:配置TrialID对应的试用配置
	DT_TrialCloneDataTable:配置cloneID，及其对应的TrialID和怪物id
}

cloneId从CloneSystemData里找

怪物id从 DT_TrialCharacterCloneMonsterConfig 里配置怪物id
DT_MatchConfig 这个是地图匹配的配置，新地图需要在这里配上
DT_LevelConfig 这个是副本类型的配置，新地图这里也要配
DT_Character 这个表示角色id和角色配置表
CloneSystemDataAsset 这个da文件里需要配置副本显示的ui索引
这个ui索引是 DT_UIExtension 这个表里的key，这个表是bp_htHUD里面配置的
AHTTeachCloneLogicActor 这个里有个tag，需要和副本类型一致，不然刷不出怪
地图的WorldSettings中，HTSceneSolelyDataAsset没改成大世界的，用的副本的，副本里面没这个木桩怪。

2 文件
```cpp
/*
1 CharacterDataAsset 角色的文件，里面有配试用的副本和试用的角色id
2 DT_TrialCloneDataTable 试用副本文件，有了副本id就从这个表查试用id
3 DT_TrialCharacterDataTable 试用角色的文件，有了试用id就从这个表查试用配置
4 UHTUI_CloneSystemExit 退出副本ui
5 AHTCloneBaseLogicActor::OpenCloneSceneMainForm 打开副本界面方法，只有打开界面了才能刷怪
6 AHTPlayerCharacter::ActivateAbilityWithButton 这个方法里触发普攻激活ga
7 char_f_skill_melee1_Montage 这个是主角普攻第一段蒙太奇
8 UNotifyState_JumpSection 这个动画通知中，触发普攻连击
9 AHTPlayerCharacter::AddStartupGameplayAbilities 这个方法会赋予角色初始的ga数组
10 GA_Female051_Melee 这个是主角普攻的ga
11 AHTPlayerController::ServerKillTarget 这个方法是服务器杀死目标的方法
12 UHTAISpawnCache::CheckAndUpdatePrivateMonsterDeadAtServer 这个方法是服务器把杀死目标信息记录的方法，里边也会广播刷怪器所有怪死亡事件
13 
*/
```

3 问题

1 CloneSystemDataAsset 这个文件中的ClonePopupUIDataArray这个段控制副本打开的ui，这里要配置
2 

10 
1 客户端在混合模式下会创建出怪，怪的replicate标记成true，啥东西会同步呢，这个可以之后看看
2 EditConditionParserTests.cpp文件，这个里面可以看Uproperty上的editcondition

EditCondition

3 meta=(ToolTip = "高展鹏测试类 11")用这个改类鼠标悬停名称
