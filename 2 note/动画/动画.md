同步组：
https://zhuanlan.zhihu.com/p/614854028
重定向：
https://www.cnblogs.com/yaoling1997/p/16260297.html
https://www.youtube.com/watch?v=Ujb6JcHO6x8
动画更新原理：
https://zhuanlan.zhihu.com/p/545596818
https://zhuanlan.zhihu.com/p/405437842
BlendMask，BlendProfile
https://www.bubblestudy.info/wiki/index.php/UE5-%E6%B7%B7%E5%90%88%E9%81%AE%E7%BD%A9%E5%92%8C%E6%B7%B7%E5%90%88%E6%8F%8F%E8%BF%B0
动画状态机
https://zhuanlan.zhihu.com/p/663365780

# 节点
## Blend Poses by bool
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411041844567.png)


FAnimNode_BlendListByBool：
```cpp
/*
FAnimNode_BlendListByBool 结构体表示这个节点，主要逻辑都在父类中
*/
// 只重写了GetActiveChildIndex方法，通过bool选择不同的pose
int32 FAnimNode_BlendListByBool::GetActiveChildIndex()
{
	return GetActiveValue() ? 0 : 1;
}

bool FAnimNode_BlendListByBool::GetActiveValue() const
{
	return GET_ANIM_NODE_DATA(bool, bActiveValue);
}
```

父类 FAnimNode_BlendListBase::Update_AnyThread：
```cpp
void FAnimNode_BlendListBase::Update_AnyThread(const FAnimationUpdateContext& Context)
{
	
	GetEvaluateGraphExposedInputs().Execute(Context);
	
	// BlendPose 数组里面存的是链接的两个pose
	const int32 NumPoses = BlendPose.Num();
	// CurrentBlendTimes 数组里面存的是两个BlendTime
	const TArray<float>& CurrentBlendTimes = GetBlendTimes();
	bool bRequestedInertializationOnActiveChildIndexChange = false;
	
	if (NumPoses > 0)
	{
		// 混合的配置文件
		UBlendProfile* CurrentBlendProfile = GetBlendProfile();
		// 获取到当前应该选择哪个pose的索引，truePose就是0，falsePose就是1
		const int32 ChildIndex = GetActiveChildIndex();
		// 当前索引 ！= 之前索引。说明是在切换中
		if (ChildIndex != LastActiveChildIndex)
		{
			// 是否是第一次
			bool LastChildIndexIsInvalid = (LastActiveChildIndex == INDEX_NONE);
			// 计算WeightDifference，表示当前帧选择的pose，距离目标权重1还差多少。因为权重都是0到1，所以也可以看成还差多少比例
			const float CurrentWeight = PerBlendData[ChildIndex].Weight;
			const float DesiredWeight = 1.0f;
			const float WeightDifference = FMath::Clamp<float>(FMath::Abs<float>(DesiredWeight - CurrentWeight), 0.0f, 1.0f);
			// 计算剩余混合时间，还剩多少时间完全混合到当前帧选择的pose
			float RemainingBlendTime;
			if (LastChildIndexIsInvalid)
			{
				RemainingBlendTime = 0.0f;
			}
			else if (GetTransitionType() == EBlendListTransitionType::Inertialization)
			{
				// 这边是惯性混合部分，先省略
			}
			else
			{
				RemainingBlendTime = CurrentBlendTimes[ChildIndex] * WeightDifference;
			}
			for (int32 i = 0; i < PerBlendData.Num(); ++i)
			{
				PerBlendData[i].RemainingTime = RemainingBlendTime;
			}
			// 如果我们剩余混合时间为0，并且有上一个pose。就将上一个pose直接按权重为0混合，也就是去除掉上一个pose的影响
			if(RemainingBlendTime == 0.0f && !LastChildIndexIsInvalid)
			{
				BlendPose[LastActiveChildIndex].Update(
				Context.FractionalWeight(0.0f));
			}
			// 更新当前帧的混合数据
			for(int32 i = 0; i < PerBlendData.Num(); ++i)
			{
				FAlphaBlend& Blend = PerBlendData[i].Blend;
				// 设置剩余的混合时间
				Blend.SetBlendTime(RemainingBlendTime);
				// 设置权重的起始值和目标值
				if(i == ChildIndex)
				{
					Blend.SetValueRange(PerBlendData[i].Weight, 1.0f);

					if (CurrentBlendProfile)
					{
						Blend.ResetAlpha();
						PerBlendData[i].StartAlpha = Blend.GetAlpha();
					}
				}
				else
				{
					Blend.SetValueRange(PerBlendData[i].Weight, 0.0f);
				}
				if (CurrentBlendProfile)
				{
					PerBlendData[i].StartAlpha = Blend.GetAlpha();
				}
			}

			// 如果勾选了ResetChildOnActivation，并且当前权重为0。也就是第一次切换pose，会重新走遍初始化的流程
			if (GetResetChildOnActivation() && CurrentWeight <= ZERO_ANIMWEIGHT_THRESH)
			{
				FAnimationInitializeContext ReinitializeContext(Context.AnimInstanceProxy, Context.SharedContext);
				// reinitialize
				BlendPose[ChildIndex].Initialize(ReinitializeContext);
			}
			// 记录前一个pose索引
			LastActiveChildIndex = ChildIndex;
		}
		// 计算两个pose的权重和，然后归一化权重，放置权重和大于1吧
		float SumWeight = 0.0f;
		for (int32 i = 0; i < PerBlendData.Num(); ++i)
		{
			float& BlendWeight = PerBlendData[i].Weight;
			FAlphaBlend& Blend = PerBlendData[i].Blend;
			Blend.Update(Context.GetDeltaTime());
			BlendWeight = Blend.GetBlendedValue();
			SumWeight += BlendWeight;
		}
		if ((SumWeight > ZERO_ANIMWEIGHT_THRESH) && (FMath::Abs<float>(SumWeight - 1.0f) > ZERO_ANIMWEIGHT_THRESH))
		{
			float ReciprocalSum = 1.0f / SumWeight;
			for (int32 i = 0; i < PerBlendData.Num(); ++i)
			{
				PerBlendData[i].Weight *= ReciprocalSum;
			}
		}
		// 开始UpdatePose了
		for (int32 i = 0; i < BlendPose.Num(); ++i)
		{
			// 权重>0的才会走进来
			const float BlendWeight = PerBlendData[i].Weight;
			if (BlendWeight > ZERO_ANIMWEIGHT_THRESH)
			{
				// 叠加权重，递归网上找的时候会依次乘进去
				FAnimationUpdateContext ChildContext = Context.FractionalWeight(
					BlendWeight);
				// 更新方法，递归的网上执行了
				BlendPose[i].Update((i == ChildIndex) ? ChildContext : ChildContext.AsInactive());
			}
		}
	}
}
```
每个PerBlendData结构里都有FAlphaBlend结构，下面看下这个FAlphaBlend结构：
```cpp
/*
// 路程表示pose从开始切换到切换完成
// 完成所有路程所需的时间
UPROPERTY(EditAnywhere, Category = "Blend")  
float	BlendTime;

// 完成剩余路程所需的时间
float	BlendTimeRemaining;

// 已经完成的路程比例。由BlendTimeRemaining和帧长计算得出
float	AlphaLerp;

// 实际使用的已经完成的路程比例，将AlphaLerp曲线采样得出
float	AlphaBlend;

// 已经完成的路程，由AlphaBlend比例插值计算出
float BlendedValue;

// 路程起点
float BeginValue;

// 路程终点
float DesiredValue;
*/

// 下面列举一些关键方法：

// 重置已经完成的路程比例，自然用已经完成的路程BlendedValue来计算出已经完成路程比例
void FAlphaBlend::ResetAlpha()
{
	float SmallerValue = FMath::Min(BeginValue, DesiredValue);
	float BiggerValue = FMath::Max(BeginValue, DesiredValue);
	float NewBlendedValue = FMath::Clamp(BlendedValue, SmallerValue, BiggerValue);
	if (BeginValue == DesiredValue)
	{
		SetAlpha(1.f);
	}
	else
	{
		AlphaLerp = (BlendedValue - BeginValue)/(DesiredValue - BeginValue);
		SetAlpha(AlphaLerp);
	}
	bNeedsToResetAlpha = false;
}
// 重置完成剩余路程所需的时间，用完成所有路程所需的时间 * 剩余的路程比例
void FAlphaBlend::ResetBlendTime()
{
	if(BlendTime <= 0.f)
	{
		BlendTimeRemaining = 0.f;
		SetAlpha(1.f);
	}
	else
	{
		BlendTimeRemaining = BlendTime * FMath::Abs(1.f - AlphaLerp);
	}
	bNeedsToResetBlendTime = false;
}

float FAlphaBlend::Update(float InDeltaTime)
{
	// 重置已经完成的路程比例，每次设置路程起点终点的时候就需要重置比例了
	if (bNeedsToResetAlpha)
	{
		ResetAlpha();
	}
	// 重置完成剩余路程所需的时间，每次设置 完成所有路程所需的时间就需要重置了
	if (bNeedsToResetBlendTime)
	{
		ResetBlendTime();
	}
	// 是否完成 = （已经完成的路程 == 路程终点）
	if( !IsComplete() )
	{
		// 完成剩余路程所需的时间 > 帧长
		if( BlendTimeRemaining > InDeltaTime )
		{
			// 当前帧完成的路径比例 = （未完成路程比例 / 完成剩余路程所需的时间）* 帧长
			const float BlendDelta = 1.f - AlphaLerp;
			//  已经完成的路程比例 = 已经完成的路程比例 + 当前帧完成的路径比例
			AlphaLerp += (BlendDelta / BlendTimeRemaining) * InDeltaTime;
			// 完成剩余路程所需的时间 = 完成剩余路程所需的时间 - 帧长
			BlendTimeRemaining -= InDeltaTime;
			SetAlpha(AlphaLerp);
		}
		// 完成剩余路程所需的时间 <= 帧长
		else
		{
			// 跑完所有路程还多余的时间
			float Overshoot = InDeltaTime - BlendTimeRemaining;
			// 完成剩余路程所需的时间 = 0.f
			BlendTimeRemaining = 0.f; 
			// 已经完成的路程比例 = 1.f
			SetAlpha(1.f);
			return Overshoot;
		}
	}
	return 0.f;
}
```

## 节点上的函数执行时机

```cpp
// 主线程
void USkeletalMeshComponent::InitAnim(...)
 ->bool InitializeAnimScriptInstance(...)
  ->void UAnimInstance::InitializeAnimation(...)
   {
	   // 初始话Proxy
	   GetProxyOnGameThread<FAnimInstanceProxy>().Initialize(this);
	   // cpp执行的Initialize
	   NativeInitializeAnimation();
	   // 蓝图执行的BlueprintInitializeAnimation节点
	   BlueprintInitializeAnimation();
   }
   ->NativeBeginPlay()// cpp执行的BeginPlay
   ->BlueprintBeginPlay()// 蓝图执行的BeginPlay
  ->void USkeletalMeshComponent::TickAnimation(...) // 这里边很多montage逻辑
  ->void RefreshBoneTransforms_WithTeleport(...)
   {
	   // 创建了FParallelAnimationEvaluationTask来更新节点，FParallelAnimationEvaluationTask这个在上边Task结束后执行吧
	   DispatchParallelEvaluationTasks(TickFunction)
   }

// FParallelAnimationEvaluationTask 更新线程
void FParallelAnimationEvaluationTask::DoTask()
 ->void USkeletalMeshComponent::ParallelAnimationEvaluation()
  ->void USkeletalMeshComponent::PerformAnimationProcessing()
   ->void FAnimInstanceProxy::UpdateAnimation()
    ->FAnimInstanceProxy::UpdateAnimation_WithRoot()// 从这个出发执行根节点然后递归执行
	 {
		// 这个就是节点上三个方法的执行时机，是在节点Update_AnyTread之前执行
		UE::Anim::FNodeFunctionCaller::InitialUpdate(InContext, *InRootNode);
		UE::Anim::FNodeFunctionCaller::BecomeRelevant(InContext, *InRootNode);
		UE::Anim::FNodeFunctionCaller::Update(InContext, *InRootNode);
		InRootNode->Update_AnyThread(InContext);
	 }
```

展示图
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191703505.png)
### Function部分
#### 1 设置
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191705237.png)
全是UAnimGraphNpde_Base上的三个成员变量
```cpp
void UAnimGraphNode_Base::PostEditChangeProperty(...)
{
	// 在这个方法中会将这三个方法（InitialUpdateFunction，BecomeRelevantFunction，UpdateFunction）放到FAnimNode_Base节点的对应变量中（）
}
```

![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191811280.png)
#### 2 执行
1 InitialUpdateFunction
```cpp
void FNodeFunctionCaller::InitialUpdate(const FAnimationUpdateContext& InContext, FAnimNode_Base& InNode)
{
	// 最关键的就是这个部分，会执行UpdateNodeInitializationStatus方法，返回Status
	// 进而判断Status是否等于InitialUpdate，如果等于才会执行Function
	const EAnimNodeInitializationStatus Status = RelevancySubsystem.UpdateNodeInitializationStatus(InContext, InNode);
	if(Status == EAnimNodeInitializationStatus::InitialUpdate)
	{
		CallFunctionHelper<FAnimUpdateContext>(Function, InContext, InNode);
	}
}
// 这个方法呢，就是该动画蓝图第一次执行这个节点的时候会为InitialUpdate，其余都为Updated
EAnimNodeInitializationStatus FAnimSubsystemInstance_NodeRelevancy::UpdateNodeInitializationStatus()
{
	EAnimNodeInitializationStatus& Status = NodeInitTrackers.FindOrAdd(&InNode);

	switch(Status)
	{
	case EAnimNodeInitializationStatus::NotUpdated:
		Status = EAnimNodeInitializationStatus::InitialUpdate;
		break;
	default:
	case EAnimNodeInitializationStatus::InitialUpdate:
		Status = EAnimNodeInitializationStatus::Updated;
		break;
	}

	return Status;
}
```
2 BecomeRelevantFunction
```cpp
void FNodeFunctionCaller::BecomeRelevant(const FAnimationUpdateContext& InContext, FAnimNode_Base& InNode)
{
	// 最关键的就是这个部分，会执行UpdateNodeRelevancy方法，返回Status
	// 进而判断Status是否需要执行BecomeRelevant
	FAnimNodeRelevancyStatus Status = RelevancySubsystem.UpdateNodeRelevancy(InContext, InNode);
	if(Status.HasJustBecomeRelevant())
	{
		CallFunctionHelper<FAnimUpdateContext>(Function, InContext, InNode);
	}
}
// 这个方法呢就是更新Status的PreviousWeight和CurrentWeight
// 如果前权重(PreviousWeight)为0，当前权重(CurrentWeight)大于0,BecomeRelevantFunction这个方法才会执行
FAnimNodeRelevancyStatus FAnimSubsystemInstance_NodeRelevancy::UpdateNodeRelevancy(...)
{
	FTracker& Tracker = NodeTrackers.FindOrAdd(&InNode);
	
	if(!Tracker.Counter.HasEverBeenUpdated() || !Tracker.Counter.WasSynchronizedCounter(InContext.AnimInstanceProxy->GetUpdateCounter()))
	{
		Tracker.Status.CurrentWeight = 0.0f;
	}

	if(!Tracker.Counter.IsSynchronized_All(InContext.AnimInstanceProxy->GetUpdateCounter()))
	{
		Tracker.Counter.SynchronizeWith(InContext.AnimInstanceProxy->GetUpdateCounter());
		Tracker.Status.PreviousWeight = Tracker.Status.CurrentWeight;
		Tracker.Status.CurrentWeight = InContext.GetFinalBlendWeight();
	}
	
	return Tracker.Status;
}
```
3 UpdateFunction
```cpp
void FNodeFunctionCaller::Update(const FAnimationUpdateContext& InContext, FAnimNode_Base& InNode)
{
	// 任何判断条件都没有，每次节点Update_AnyThread之前都会执行
	if(InNode.NodeData != nullptr && InNode.NodeData->HasNodeAnyFlags(EAnimNodeDataFlags::HasUpdateFunction))
	{
		CallFunctionHelper<FAnimUpdateContext>(InNode.GetUpdateFunction(), InContext, InNode);
	}
}

```

### State部分
```cpp
// State这部分是在状态机中调用，TransitionToState是状态切换执行的方法
void FAnimNode_StateMachine::TransitionToState()
->void SetState()
 {
// 执行当前节点的StateExitFunction方法
UE::Anim::FNodeFunctionCaller::CallFunction(static_cast<FAnimNode_StateResult*>(StatePoseLinks[CurrentState].GetLinkNode())->GetStateExitFunction(), Context, *this);
// 执行切换节点的StateEntryFunction方法
UE::Anim::FNodeFunctionCaller::CallFunction(static_cast<FAnimNode_StateResult*>(StatePoseLinks[CurrentState].GetLinkNode())->GetStateEntryFunction(), Context, *this);
 }
```
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191830469.png)

这个部分应该是OutpuitAnimationPose特有的，对应的类型是UAnimGraphNode_StateResult
#### 1 设置
和Function部分一样，都是在PostEditChangeProperty方法中设置的
#### 2 执行

1 StateEntryFunction 和 StateExitFunction
这两个配套在FAnimNode_StateMachine::SetState方法中调用，在状态切换时旧状态调用StateExitFunction方法，新状态调用StateEntryFunction方法
2 StateFullyBlendedInFunction 和 StateFullyBlendedOutFunction
字面意思，当状态机中的节点完全混出或者是完全混入的时候会调用，跳过节点的时候不会调用
# 开枪锁定

## 动画部分

整体思路，AzureCharacter上面有ANBP_BaseMesh动画蓝图，在其下边挂上Retarget动画蓝图，ANBP_BaseMesh动画蓝图里面执行基础移动逻辑，Retarget是重定向的部分
ANBP_BaseMesh动画蓝图分成四个部分：
1 首先是基础移动部分
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411081628545.png)
动画层接口，然后把Locomotion保存起来
2 蒙太奇的叠加动画
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411081633397.png)
3 基础移动和蒙太奇混合上半身，左手，右手
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411081634123.png)
4 选择是2的部分还是3的部分
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411081635017.png)

### 开枪锁定

释放技能 -> 切换动画层 -> 播放蒙太奇 
1 释放技能
2 游戏中切换动画层会切换成ANBP_LockLocomotion这个，也就是用这个动画蓝图来跑基础移动
3 播放开枪动画的蒙太奇，蒙太奇里有混合上半身的曲线。也就是上半身使用蒙太奇动画，下半身使用基础移动动作
下面看下ANBP_LockLocomotion这个动画层
首先通过6个方向（前，后，左前，左后，右前，右后）来选择了基础资源pose，然后通过方向扭曲来实现移动，其中LocalVelocityDirectionAngle是在cpp中计算，就是Velocity2D和当前角色旋转的夹角
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202411081645911.png)

## 摄像机部分

1 会调用到ViewModel的set_CoLookAtGO这个方法，并将一些列参数传递进去，set_CoLookAtGO这个方法是赋值m_pCoLookAtInfo变量。
2 set_CoLookAtGO 的使用是在UAzurePlayerCameraViewModeComponentBase::TickFunc方法中的，会在每帧Tick。TickFunc会调用UpdateCameraRotation方法，


# 走跑
## Als 走跑混合

![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412181113572.png)

基本的走跑混合是通过二维混合控件来实现，横坐标是步幅，纵坐标是步态，还有播放速率来控制频率。在相同的频率下步幅越大，速度越快。

### WalkRunBlend 步态计算

1 获取到玩家的按键，从而计算出Gait。
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412181202690.png)
2 通过Gait来决定当前的步态。
### StrideBlend 步幅计算

![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412181125374.png)

1 首先获取我们的速度。
2 我们需要有两根不同步态下的步幅曲线，也就是StrideBlend_N_Walk和StrideBlend_N_Run，横坐标都是速度，纵坐标是步幅（范围是从0到1的）。
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412181149573.png)

3 至此我们有了走跑的步幅值。如果我们是非走即跑的状态，那么直接根据步态切换对应的步幅就可以。但是我们目前是通过混合空间来让走跑混合，所以还需要有从走到跑中间态的步幅值，所以就给走动画序列添加Weight_Gait曲线，值为1。给跑动画序列添加Weight_Gait曲线，值为2。我们会读取Weight_Gait曲线值，然后将走跑的步幅插值，得到结果。

### StandingPlayRate 步频计算

![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412181600552.png)
1 当前速度 / 设定的走跑速度，然后在通过Weight_Gait插值。

## Odin 走跑混合

ANBP_BasicLocomotion 这个动画层里
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191426426.png)
1 状态机这块就是，Idle -> Start -> Cycle -> Stop这种的循环。
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191429728.png)

- Idle里就是播一下Idle的动画序列，CanToMovine到ToMoving的切换条件就是有没有2D的加速度
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202412191440334.png)
- Start里


## Lyra 走跑混合

