1 继承UMassObserverProcessor
2 在Register的时候会将,Processor添加到UMassObserverRegistry中，是个映射
key是观察的Fragment，value是processor
3 在ObserveManager的Initialize中会遍历ObserverRegistry中注册的Processor，去执行Processor的初始化。并顺便将ObserverRegistry中记录的注册数组缓存到ObserveManager中的FragmentObservers观察数组中。
4 SpawnEntities方法的返回值是FCreationContext的智能指针，这个创建上下文有ObserveLock的成员变量。在SpawnEntities流程里会将创建的entity添加到ObserveLock的BufferedNotifications通知数组中
5 在ObserveLock析构的时候会触发ResumeExecution方法，里面会遍历通知数组，找到通知中带有的fragment和观察数组比较，如果观察数组中有那就执行观察数组中的processor。

6 观察的是FMassZoneGraphAnnotationFragment这个的添加，如果添加了，会在Execute中读取entity上的LaneLoaction，确定entity当前站在那个lane上，然后在通过ZoneGraphAnnotationSubsystem来确定这个lane上的AnnotationTag是哪个，并将tag记录到ZoneGraphAnnotationFragment中。

# UMassZoneGraphAnnotationTagUpdateProcessor
1 他是一个SignalProcessor，监听的是道路改变UE::Mass::Signals::CurrentLaneChanged
2 在这个processor执行的时候，
