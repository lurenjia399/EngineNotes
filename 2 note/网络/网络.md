总体上ue的网络各种概念
https://blog.ch-wind.com/ue4-network-overview/
切换关卡，客户端服务器链接
https://zhuanlan.zhihu.com/p/60996708
网络同步详解以及问题
https://gwb.tencent.com/community/detail/121544
https://gwb.tencent.com/community/detail/121545
启动uds
https://dev.epicgames.com/documentation/en-us/unreal-engine/setting-up-dedicated-servers-in-unreal-engine?application_version=5.1
命令行添加-server启动uds
https://blog.csdn.net/ZFSR05255134/article/details/127287290
https://blog.csdn.net/qq_39108291/article/details/119619482
断线重连
https://jkchen.blog.csdn.net/article/details/134178763?spm=1001.2101.3001.6650.3&utm_medium=distribute.pc_relevant.none-task-blog-2%7Edefault%7EBlogCommendFromBaidu%7ERate-3-134178763-blog-84815922.235%5Ev43%5Epc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2%7Edefault%7EBlogCommendFromBaidu%7ERate-3-134178763-blog-84815922.235%5Ev43%5Epc_blog_bottom_relevance_base3&utm_relevant_index=6

https://www.jianshu.com/p/b4f1a5412cc9

# 1 客户端 服务器建立链接
## 1 服务器链接
### 1 Listen
服务器的监听起始点是在LoadMap方法中
```cpp
// Listen for clients.
if (Pending == NULL && (!GIsClient || URL.HasOption(TEXT("Listen"))) && !URL.HasOption(TEXT("DeferredListen")))
{
	if (!WorldContext.World()->Listen(URL))
	{
		UE_LOG(LogNet, Error, TEXT("LoadMap: failed to Listen(%s)"), *URL.ToString());
	}
}
```
如果不是客户端，或者是URL中有Listen，就认为是服务器走到了LoadMap方法，初始化监听。
```cpp
bool UWorld::Listen( FURL& InURL )
{
#if WITH_SERVER_CODE

	// Create net driver.
	// 创建netDriver
	if (GEngine->CreateNamedNetDriver(this, NAME_GameNetDriver, NAME_GameNetDriver))
	{
		NetDriver = GEngine->FindNamedNetDriver(this, NAME_GameNetDriver);
		NetDriver->SetWorld(this);
		FLevelCollection* const SourceCollection = FindCollectionByType(ELevelCollectionType::DynamicSourceLevels);
		if (SourceCollection)
		{
			SourceCollection->SetNetDriver(NetDriver);
		}
		FLevelCollection* const StaticCollection = FindCollectionByType(ELevelCollectionType::StaticLevels);
		if (StaticCollection)
		{
			StaticCollection->SetNetDriver(NetDriver);
		}
	}
	// NetDriver调用InitListen方法
	if( !NetDriver->InitListen( this, InURL, false, Error ) )
	{
		
	}
	return true;
#else
	return false;
#endif // WITH_SERVER_CODE
}
```
整个方法都由WITH_SERVER_CODE宏包装起来，是在UBT中设置的宏。第一部分就是创建NetDriver，创建这里会通过BaseEngine.ini里面的NetDriverDefinitions配置创建出IpNetDriver类，创建完后还有一些和当前world绑定的操作。第二部分就是调用InitListen方法初始化监听
### 2 UIpNetDriver::InitListen
```cpp
// 这个方法就两部分，一个是InitBase方法初始化，一个是InitConnectionlessHandler
bool UIpNetDriver::InitListen( FNetworkNotify* InNotify, FURL& LocalURL, bool bReuseAddressAndPort, FString& Error )
{
	if( !InitBase( false, InNotify, LocalURL, bReuseAddressAndPort, Error ) )
	{
		return false;
	}
	InitConnectionlessHandler();
	LocalURL.Port = LocalAddr->GetPort();
	return true;
}
```
在UNetDriver::InitBase里面(也就是基类里面)，创建了NetConnectionClass（这是个UCLass，不是UObject），是通过BaseEngine.ini里面的NetConnectionClassName配置创建出IpConnection类(这是个UPlayer的子类)的。然后就是设置了FNetworkNotify类型的成员变量Notify，在服务器这部分这个Notify是当前的World，也就是调用Listen的World。最后如果NetConnectionClass创建成功就返回true。
在UIpNetDriver::InitBase里面，看上去就是创建Socket，然后做一些Socket的初始化，
### 3 UNetDriver::InitConnectionlessHandler
```cpp
void UNetDriver::InitConnectionlessHandler()
{
	check(!ConnectionlessHandler.IsValid());
	{
		ConnectionlessHandler = MakeUnique<PacketHandler>(&DDoS);

		if (ConnectionlessHandler.IsValid())
		{
			ConnectionlessHandler->NotifyAnalyticsProvider(AnalyticsProvider, AnalyticsAggregator);
			ConnectionlessHandler->Initialize(Handler::Mode::Server, MAX_PACKET_SIZE, true, nullptr, nullptr, NetDriverName);

			// Add handling for the stateless connect handshake, for connectionless packets, as the outermost layer
			TSharedPtr<HandlerComponent> NewComponent =
				ConnectionlessHandler->AddHandler(TEXT("Engine.EngineHandlerComponentFactory(StatelessConnectHandlerComponent)"), true);

			StatelessConnectComponent = StaticCastSharedPtr<StatelessConnectHandlerComponent>(NewComponent);

			if (StatelessConnectComponent.IsValid())
			{
				StatelessConnectComponent.Pin()->SetDriver(this);
			}

			ConnectionlessHandler->InitializeComponents();
		}
	}
}
```
这个方法里就是创建ConnectionlessHandler和StatelessConnectComponent
## 2 客户端链接

如果是客户端走的Browse方法，里面会创建PendingNetGame，然后会调用InitNetDriver方法来进行与服务器链接。
### 1 UPendingNetGame::InitNetDriver

```cpp
void UPendingNetGame::InitNetDriver()
{
	LLM_SCOPE(ELLMTag::Networking);

	if (!GDisallowNetworkTravel)
	{
		// 开始这和服务器创建一样，都是创建出NetDriver
		if (GEngine->CreateNamedNetDriver(this, NAME_PendingNetDriver, NAME_GameNetDriver))
		{
			NetDriver = GEngine->FindNamedNetDriver(this, NAME_PendingNetDriver);
		}
		// 然后就是初始化链接
		if( NetDriver->InitConnect( this, URL, ConnectionError ) )
		{
			UNetConnection* ServerConn = NetDriver->ServerConnection;

			FNetDelegates::OnPendingNetGameConnectionCreated.Broadcast(this);

			// Kick off the connection handshake
			// 触发握手流程
			if (ServerConn->Handler.IsValid())
			{
				ServerConn->Handler->BeginHandshaking(
					FPacketHandlerHandshakeComplete::CreateUObject(this, &UPendingNetGame::SendInitialJoin));
			}
			else
			{
				SendInitialJoin();
			}
		}
		else
		{
			UE_LOG(LogNet, Warning, TEXT("error initializing the network stack"));
			GEngine->DestroyNamedNetDriver(this, NetDriver->NetDriverName);
			NetDriver = NULL;
		}
	}
}
```
不是很复杂，首先就是通过CreateNamedNetDriver创建出NetDriver，这部分和服务器的UWorld::Listen方法一样，服务器之后会调用NetDriver的InitListen方法，而这里客户端会调用NetDriver的InitConnect方法。
### 2 InitConnect
```cpp
bool UIpNetDriver::InitConnect( FNetworkNotify* InNotify, const FURL& ConnectURL, FString& Error )
{
	// 这部分也是初始化，和服务器一样
	// 然后就是设置了FNetworkNotify类型的成员变量Notify，在客户端这部分这个Notify是当前的UPendingNetGame，而服务器是World
	if( !InitBase( true, InNotify, ConnectURL, false, Error ) )
	{
		return false;
	}

	// Create new connection.
	// 注意这个ServerConnection是客户端创建赋值的，表示链接的服务器只有一个
	// 这里创建了ServerConnection这是一个UPlayer的子类
	// 其中NetConnectionClass是在InitBase里面赋值的
	ServerConnection = NewObject<UNetConnection>(GetTransientPackage(), NetConnectionClass);
	UIpConnection* IPConnection = CastChecked<UIpConnection>(ServerConnection);

	// 这里里面初始化一堆数据，PacketHandler，StatelessConnectComponent，UPackageMapClient
	// 最终会走到UNetConnection::InitBase方法里初始化，
	ServerConnection->InitLocalConnection(this, Socket, ConnectURL, USOCK_Pending);

	// 创建Channel
	CreateInitialClientChannels();

	return true;
}
```
这个部分就是通过InitBase初始化一些数据，然后创建出ServerConnection的UPlyaer对象，然后初始化，最后就是创建Channels，Channels是通过BaseEngine.ini配置中ChannelDefinitions这个字段创建的，默认是三个（ControlChannel，VoiceChannel，ActorChannel）
### 3 BeginHandshaking 回调

#### 0 UControlChannel::ReceivedBunch
先看下ControlChannel处理消息的方法
```cpp
// 这个方法就是ConTrolChannel处理消息的地方
void UControlChannel::ReceivedBunch( FInBunch& Bunch )
{
	// 其余的全省略掉了，和这次没段可能关系不大
	// Process control message on client/server connection
	Connection->Driver->Notify->NotifyControlMessage(Connection, MessageType, Bunch);
}
```
在服务器上Notify是UWorld，也就是服务器处理ControlChannel消息是会走到UWorld的NotifyControlMessage方法。
在客户端上Notify是UPendingNetGame，也就是客户端处理ControlChannel消息是会走到UPendingNetGame的NotifyControlMessage方法。

![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202406182036105.png)

在握手建立了链接之后，会走到回调方法
#### 1 Client 发送NMT_Hello包
```cpp
void UPendingNetGame::SendInitialJoin()
{
	if (NetDriver != nullptr)
	{
		UNetConnection* ServerConn = NetDriver->ServerConnection;

		if (ServerConn != nullptr)
		{
			uint8 IsLittleEndian = uint8(PLATFORM_LITTLE_ENDIAN);
			
			uint32 LocalNetworkVersion = FNetworkVersion::GetLocalNetworkVersion();


			FString EncryptionToken;
			if (CVarNetAllowEncryption.GetValueOnGameThread() != 0)
			{
				EncryptionToken = URL.GetOption(TEXT("EncryptionToken="), TEXT(""));
			}

			FNetControlMessage<NMT_Hello>::Send(ServerConn, IsLittleEndian, LocalNetworkVersion, EncryptionToken);

			ServerConn->FlushNet();
		}
	}
}
```
NetDriver是否存在，ServerConnection是否存在，这个值是客户端创建的，也就是说这部分是客户端在走，然后通过ControlChannel向服务器发送NMT_Hello包。
客户端通过SendInitialJoin方法，使用ControlChannel通道向服务器发送NMT_Hello包。
#### 2 Server 处理NMT_Hello包 -> 发送NMT_Challenge包
```cpp
void UWorld::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		case NMT_Hello:
		{
			uint8 IsLittleEndian = 0;
			uint32 RemoteNetworkVersion = 0;
			uint32 LocalNetworkVersion = FNetworkVersion::GetLocalNetworkVersion();
			FString EncryptionToken;

			if (FNetControlMessage<NMT_Hello>::Receive(Bunch, IsLittleEndian, RemoteNetworkVersion, EncryptionToken))
			{
				if (!FNetworkVersion::IsNetworkCompatible(LocalNetworkVersion, RemoteNetworkVersion))
				{
					// 信息不匹配，就会发送NMT_Upgrade包
					FNetControlMessage<NMT_Upgrade>::Send(Connection, LocalNetworkVersion);
					Connection->FlushNet(true);
					Connection->Close();
				}
				else
				{
					if (EncryptionToken.IsEmpty())
					{
						// 发送Challenge包
						Connection->SendChallengeControlMessage();
					}
				}
			}
			break;
		}
	}
}

void UNetConnection::SendChallengeControlMessage()
{
	if (State != USOCK_Invalid && State != USOCK_Closed && Driver)
	{
		Challenge = FString::Printf(TEXT("%08X"), FPlatformTime::Cycles());
		SetExpectedClientLoginMsgType(NMT_Login);
		FNetControlMessage<NMT_Challenge>::Send(this, Challenge);
		FlushNet();
	}
}
```
服务器收到后会进行一系列判断，都通过之后会调用SendChallengeControlMessage方法，这个方法会向客户但发送NMT_Challenge包。
#### 3 Client 处理NMT_Challenge包 -> 发送NMT_Login包
```cpp
void UPendingNetGame::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		case NMT_Challenge:
		{
			// Challenged by server.
			if (FNetControlMessage<NMT_Challenge>::Receive(Bunch, Connection->Challenge))
			{
				// 过去GameInstance中的0号LoaclPlayer
				// 还记得在哪创建的LocalPlayer么，是在Engine::init时候在通过ViewportClient在客户端创建的
				ULocalPlayer* LocalPlayer = GEngine->GetFirstGamePlayer(this);
				if (LocalPlayer)
				{
				}

				Connection->ClientResponse = TEXT("0");
				FString URLString(PartialURL.ToString());
				FString OnlinePlatformNameString = OnlinePlatformName.ToString();

				// 之前的操作都是为了组装这四个参数
				// 向服务器发送NMT_Login包
				FNetControlMessage<NMT_Login>::Send(Connection, Connection->ClientResponse, URLString, Connection->PlayerId, OnlinePlatformNameString);
				NetDriver->ServerConnection->FlushNet();
			}
			else
			{
				Connection->Challenge.Empty();
			}
			break;
		}
	}
}
```
通过GameInstance拿到LocalPlayer（是在Engine::init里面创建的），组装了新的URL，LocalPlayer的UniqueNetId，然后往服务器发送NMT_Login包，参数有四个（Connection->ClientResponse，URLString，Connection->PlayerId， OnlinePlatformNameString）
#### 3 Server 处理NMT_Login包 -> 发送NMT_Welcome包
```cpp
void UWorld::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		bool bReceived = FNetControlMessage<NMT_Login>::Receive(Bunch, Connection->ClientResponse, RequestURL, UniqueIdRepl,OnlinePlatformName);

		if (bReceived)
		{
			// 接收参数
			const TCHAR* NewRequestURL = *RequestURL;
			FURL InURL( NULL, NewRequestURL, TRAVEL_Absolute );

			// keep track of net id for player associated with remote connection
			// 接收参数
			Connection->PlayerId = UniqueIdRepl;

			// keep track of the online platform the player associated with this connection is using.
			// 接收参数
			Connection->SetPlayerOnlinePlatformName(FName(*OnlinePlatformName));

			// ask the game code if this player can join
			FString ErrorMsg;
			AGameModeBase* GameMode = GetAuthGameMode();

			if (GameMode)
			{
				// 一些设置？
				GameMode->PreLogin(Tmp, Connection->LowLevelGetRemoteAddress(), Connection->PlayerId, ErrorMsg);
			}
			if (!ErrorMsg.IsEmpty())
			{
			}
			else
			{
				WelcomePlayer(Connection);
			}
		}
		else
		{
			Connection->ClientResponse.Empty();
			RequestURL.Empty();
		}
	}
}
```
接收包中的各种参数，然后进行一些设置，最后调用WelcomePlayer方法，这个方法里就是向客户端发送NMT_Welcome包，里面带有三个参数（关卡Name，GameModeClassName，URL）
#### 4 Client 处理NMT_Welcome包 -> 发送NMT_Netspeed包
```cpp
void UPendingNetGame::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		case NMT_Welcome:
		{
			// Server accepted connection.
			FString GameName;
			FString RedirectURL;

			if (FNetControlMessage<NMT_Welcome>::Receive(Bunch, URL.Map, GameName, RedirectURL))
			{
				// 通过三个参数组装UPendingNetGame中的URL
				{
					FURL DefaultURL;
					FURL TempURL(&DefaultURL, *URL.Map, TRAVEL_Partial);
					URL.Map = TempURL.Map;
					URL.RedirectURL = RedirectURL;
					URL.Op.Append(TempURL.Op);
				}
				if (GameName.Len() > 0)
				{
					URL.AddOption(*FString::Printf(TEXT("game=%s"), *GameName));
				}

				// Send out netspeed now that we're connected
				FNetControlMessage<NMT_Netspeed>::Send(Connection, Connection->CurrentNetSpeed);

				// We have successfully connected
				// TickWorldTravel will load the map and call LoadMapCompleted which eventually calls SendJoin
				// 并设置标志位,客户端可以加载地图了
				bSuccessfullyConnected = true;
			}
			else
			{
				URL.Map.Empty();
			}
			break;
		}
	}
}
```
这个里面就是组装出URL，和设置bSuccessfullyConnected标志位。后续就会在TickWorldTravel里面进行加在URL中的map。
#### 5 Server 处理NMT_Netspeed包
```cpp
void UWorld::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		case NMT_Netspeed:
		{
			int32 Rate;
	
			if (FNetControlMessage<NMT_Netspeed>::Receive(Bunch, Rate))
			{
				Connection->CurrentNetSpeed = FMath::Clamp(Rate, 1800, NetDriver->MaxClientRate);
			}
	
			break;
		}
	}
}
```
#### 6 Client 地图加载完后 -> 发送NMT_Join包
```cpp
// 在tickWorldTravel里面，客户端会使用LoadMap加在地图，当地图加载完后
void UEngine::TickWorldTravel(...)
{
	const bool bLoadedMapSuccessfully = LoadMap(Context, Context.PendingNetGame->URL, Context.PendingNetGame, Error);

	if (Context.PendingNetGame != nullptr)
	{
		Context.PendingNetGame->LoadMapCompleted(this, Context, bLoadedMapSuccessfully, Error);

		// Kill the pending level.
		Context.PendingNetGame = nullptr;
	}
}

void UPendingNetGame::LoadMapCompleted(...)
{
	{
		// Show connecting message, cause precaching to occur.
		Engine->TransitionType = ETransitionType::Connecting;
		Engine->RedrawViewports(false);
		// Send join.
		Context.PendingNetGame->SendJoin();
		Context.PendingNetGame->NetDriver = NULL;
	}
}

void UPendingNetGame::SendJoin()  
{  
    bSentJoinRequest = true;  
  
    FNetControlMessage<NMT_Join>::Send(NetDriver->ServerConnection);  
    NetDriver->ServerConnection->FlushNet(true);  
}

```
客户端地图加载完成后，清空PendingNetGame里面的NetDriver(这个NetDriver已经在LoadMap中移交给了World)，清空PendingNetGame，向服务器发送NMT_Join包。
#### 7 Server 处理NMT_Join包

```cpp
void UWorld::NotifyControlMessage(...)
{
	switch (MessageType)
	{
		case NMT_Join:
		{
			if (Connection->PlayerController == NULL)
			{
				FURL InURL( NULL, *Connection->RequestURL, TRAVEL_Absolute );
				// 创建出了NetConnection的PlayerController
				Connection->PlayerController = SpawnPlayActor( Connection, ROLE_AutonomousProxy, InURL, Connection->PlayerId, ErrorMsg );
				if (Connection->PlayerController == NULL)
				{
				}
				else
				{
					Connection->SetClientLoginState(EClientLoginState::ReceivedJoin);
					// if we're in the middle of a transition or the client is in the wrong world, tell it to travel
					FString LevelName;
					// 下边是客户端和服务器地图不一致，就通知客户端切换地图
					if (LevelName != TEXT(""))
					{
						Connection->PlayerController->ClientTravel(LevelName, TRAVEL_Relative, true);
					}
					Connection->QueuedBits = 0;
				}
			}
		}
	}
}

APlayerController* UWorld::SpawnPlayActor(...)
{
	if (AGameModeBase* const GameMode = GetAuthGameMode())
	{
		// Give the GameMode a chance to accept the login
		APlayerController* const NewPlayerController = GameMode->Login(NewPlayer, RemoteRole, *InURL.Portal, Options, UniqueId, Error);
		if (NewPlayerController == NULL)
		{
			UE_LOG(LogSpawn, Warning, TEXT("Login failed: %s"), *Error);
			return NULL;
		}
		NewPlayerController->NetPlayerIndex = InNetPlayerIndex;
		NewPlayerController->SetRole(ROLE_Authority);
		NewPlayerController->SetReplicates(RemoteRole != ROLE_None);
		if (RemoteRole == ROLE_AutonomousProxy)
		{
			NewPlayerController->SetAutonomousProxy(true);
		}
		NewPlayerController->SetPlayer(NewPlayer);
		GameMode->PostLogin(NewPlayerController);
		return NewPlayerController;
	}
	UE_LOG(LogSpawn, Warning, TEXT("Login failed: No game mode set."));
	return nullptr;
}

```
服务器这个处理就是创建NetConnect的PlayerController，并设置PlayerController的各项属性。设置ROLE_Authority，设置可复制的，与NetConnect相关联(NetConnection里OwningActor属性就是PlayerController)，最后通过PostLogin在出生点生成一个默认的Pawn。

# 2 Actor同步
NetDriver是驱动网络相关的操作，同步也是由NetDriver::tick执行的
```cpp
void UWorld::Tick(...)
{
	{
		// 这里客户端和服务器都会调用到TickDispatch
		BroadcastTickDispatch(DeltaSeconds);
		BroadcastPostTickDispatch();
		if( NetDriver && NetDriver->ServerConnection )
		{
			TickNetClient( DeltaSeconds );
		}
	}
	
	// 客户端TickFlush
	if (!IsRunningDedicatedServer())
		{
			// Update net and flush networking.
			// Tick all net drivers
			{
				BroadcastTickFlush(RealDeltaSeconds); // note: undilated time is being used here
			}
	
			// PostTick all net drivers
			{
				LLM_SCOPE(ELLMTag::Networking);
				BroadcastPostTickFlush(RealDeltaSeconds); // note: undilated time is being used here
			}
		}
	}
	// 这里会执行Actor的Tick，也就是RunTickGroup
	// 服务器TickFlush
	if (IsRunningDedicatedServer())
	{
		// Update net and flush networking.
		// Tick all net drivers
		{
			SCOPE_CYCLE_COUNTER(STAT_NetBroadcastTickTime);
			BroadcastTickFlush(RealDeltaSeconds); // note: undilated time is being used here
		}
	}
	
```
客户端和服务器都会优先执行TickDispatch，然后客户端执行TickFlush，进而执行RunTickGroup，最后服务器执行TickFlush
## 1 Server Actor同步 UNetDriver::ServerReplicateActors
```cpp
void UNetDriver::TickFlush(float DeltaSeconds)
{
	if (IsServer() && ClientConnections.Num() > 0 && !bSkipServerReplicateActors)
	{
		// Update all clients.
		int32 Updated = ServerReplicateActors( DeltaSeconds );
		// 客户端走
		if( ServerConnection )
		{
			// Queue client voice packets in the server's voice channel
			ProcessLocalClientPackets();
			// 客户端这会执行NetConnection的Tick
			// 在NetConnection的Tick里面会执行ChannelsTick数组的Tick，也就是会执行到ProcessQueuedBunches方法
			ServerConnection->Tick(DeltaSeconds);
		}
		// 服务器走
		else
		{
			// Queue up any voice packets the server has locally
			ProcessLocalServerPackets();
		}
	}
}
```
### Server UNetDriver::ServerReplicateActors
```cpp
int32 UNetDriver::ServerReplicateActors(float DeltaSeconds)
{
	// 这个方法的返回值代表了有几个ClientConnection可以Tick，也就是说有几个客户端需要考虑同步
	// 并切赋值了Connection中的ViewTarget
	{
		const int32 NumClientsToTick = ServerReplicateActors_PrepConnections( DeltaSeconds );  
		if ( NumClientsToTick == 0 )  
		{  
		    return 0;  
		}
	}
	// 构建需要考虑复制的Actor
	{
		TArray<FNetworkObjectInfo*> ConsiderList;
		ConsiderList.Reserve( GetNetworkObjectList().GetActiveObjects().Num() );
		ServerReplicateActors_BuildConsiderList( ConsiderList, ServerTickTime );
	}
	// 根据ViewTarget的位置，确定PriorityList，PriorityActors两个数组，带有优先级的Actor，这里面的Actor应该是ConsiderList的子集
	// 然后是对排序后的Actor进行处理，没有ActorChannel会创建，然后同步数据到客户端
	{
		FActorPriority* PriorityList    = NULL;  
		FActorPriority** PriorityActors = NULL;
		for ( int32 i=0; i < ClientConnections.Num(); i++ )  
		{
			if (Connection->ViewTarget)  
			{
				// 这里筛选排序
				const int32 FinalSortedCount = ServerReplicateActors_PrioritizeActors( Connection, ConnectionViewers, ConsiderList, bCPUSaturated, PriorityList, PriorityActors );
				const int32 LastProcessedActor = ServerReplicateActors_ProcessPrioritizedActors( Connection, ConnectionViewers, PriorityActors, FinalSortedCount, Updated );
				
			}
		}
	}
	// 在处理Actor的时候，因为连接饱和了导致没处理完，剩下还未处理的在下一帧处理
	{
		for ( int32 k=LastProcessedActor; k<FinalSortedCount; k++ )
		{
			// 根据不同情况设置bPendingNetUpdate标志位，下一帧同步这个Actor
			PriorityActors[k]->ActorInfo->bPendingNetUpdate = true;
		}
	}
	// 记录Connection这次同步帧号
	Connection->LastProcessedFrame = ReplicationFrame;
	// 将这次处理的Connection放到ClientConnection数组的后边，以便下一帧能优先处理
	{
		if (NumClientsToTick < ClientConnections.Num())
		{
			int32 NumConnectionsToMove = NumClientsToTick;
			while (NumConnectionsToMove > 0)
			{
				UNetConnection *Connection = ClientConnections[0];
				ClientConnections.RemoveAt(0,1);
				ClientConnections.Add(Connection);
				NumConnectionsToMove--;
			}
		}
	}
	// 关闭掉之前记录的需要关闭的Connection
	{
		for (UNetConnection* ConnectionToClose : ConnectionsToClose)
		{
			ConnectionToClose->Close();
		}
	}
}

```
#### 1 UNetDriver::ServerReplicateActors_PrepConnections
```cpp
int32 UNetDriver::ServerReplicateActors_PrepConnections( const float DeltaSeconds )
{
	int32 NumClientsToTick = ClientConnections.Num();

	for ( int32 ConnIdx = 0; ConnIdx < ClientConnections.Num(); ConnIdx++ )
	{
		UNetConnection* Connection = ClientConnections[ConnIdx];
		AActor* OwningActor = Connection->OwningActor;
		if ( OwningActor != NULL && Connection->State == USOCK_Open)
		{

			bFoundReadyConnection = true;
			AActor* DesiredViewTarget = OwningActor;
			if (Connection->PlayerController)
			{
				if (AActor* ViewTarget = Connection->PlayerController->GetViewTarget())
				{
					if (ViewTarget->GetWorld())
					{
						// 这里拿到了Controller里面的ViewTarget
						DesiredViewTarget = ViewTarget;
					}
				}
			}
			// 这里是赋值Connection->ViewTarget
			Connection->ViewTarget = DesiredViewTarget;
			// 以及Child的ViewTarget
			for ( int32 ChildIdx = 0; ChildIdx < Connection->Children.Num(); ChildIdx++ )
			{
				UNetConnection *Child = Connection->Children[ChildIdx];
				APlayerController* ChildPlayerController = Child->PlayerController;
				if ( ChildPlayerController != NULL )
				{
					Child->ViewTarget = ChildPlayerController->GetViewTarget();
				}
				else
				{
					Child->ViewTarget = NULL;
				}
			}
		}
	}

	return bFoundReadyConnection ? NumClientsToTick : 0;
}
```
这部分是个提前处理，目的是给ClientConnection里面的ViewTarget赋值，赋的值是ClientConnection里面的PlayerController里的ViewTarget，也就是客户端的位置。
#### 2 UNetDriver::ServerReplicateActors_BuildConsiderList
```cpp
void UNetDriver::ServerReplicateActors_BuildConsiderList(...)
{
	// 遍历ActiveNetworkObjects数组，这个数组会在AActor::SetReplicates这个方法中通过AddNetworkActor方法被添加
	// 也就是所有被设置成可复制的Actor
	for ( const TSharedPtr<FNetworkObjectInfo>& ObjectInfo : GetNetworkObjectList().GetActiveObjects() )
	{
		FNetworkObjectInfo* ActorInfo = ObjectInfo.Get();
		// 如果Actor不在同步过程中 并且 同步时间没到 不同步跳过
		if ( !ActorInfo->bPendingNetUpdate && World->TimeSeconds <= ActorInfo->NextUpdateTime )
		{
			continue;
		}
		AActor* Actor = ActorInfo->Actor;
		// Actor应该被销毁，不同步跳过
		if ( Actor->IsPendingKillPending() )
		{
			ActorsToRemove.Add( Actor );
			continue;
		}
		// Actor没有Role，不同步跳过
		if ( Actor->GetRemoteRole() == ROLE_None )  
		{  
			ActorsToRemove.Add( Actor );  
			continue;  
		}
		// Actor不属于当前这个NetDriver，不同步跳过
		if (Actor->GetNetDriverName() != NetDriverName)  
		{
		    continue;  
		}
		// Actor还没有初始化，不同步跳过
		if ( !Actor->IsActorInitialized() )  
		{  
		    continue;  
		}
		// 这个自己加的，Actor延迟BeginPlay，不同步跳过
		if ( Actor->bDeferredBeginPlay )  
		{  
		    continue;  
		}
		// Actor所处的Level正在切换过程中，不同步跳过
		ULevel* Level = Actor->GetLevel();  
		if ( Level->HasVisibilityChangeRequestPending() || Level->bIsAssociatingLevel )  
		{  
		    continue;  
		}
		// 是一个休眠的网络Actor，不同步跳过，不太清楚干嘛的
		if ( IsDormInitialStartupActor(Actor) )
		{
			ActorsToRemove.Add( Actor );
			continue;
		}
		// Actor下一次需要同步的时间 = 当前世界的时间 + 随机值 + 配置时间
		ActorInfo->NextUpdateTime = World->TimeSeconds + UpdateDelayRandomStream.FRand() * ServerTickTime + NextUpdateDelta;
		// 最终赋值结果数组
		OutConsiderList.Add( ActorInfo );
		// 调用一个赋值之前的方法，可以放些操作？
		Actor->CallPreReplication( this );
	}
}
```
这个方法就是构建需要同步给客户端的Actor数组，每个Actor在SetReplicates这个方法中会将自己添加到NetDriver的数组里面，我们首先遍历NetDriver里面的数组，然后判断各种状态，确实Actor是否可以同步。这个方法里也计算了Actor的下一次同步的时间NextUpdateTime。
#### 3 UNetDriver::ServerReplicateActors_PrioritizeActors
```cpp
for ( FNetworkObjectInfo* ActorInfo : ConsiderList )
{
	AActor* Actor = ActorInfo->Actor;
	UActorChannel* Channel = Connection->FindActorChannelRef( ActorInfo->WeakActor     );
	// 这个Channel不存在，是说第一次同步？
	if (!Channel)
	{
		// 如果这个Actor属于的Level，在客户端上还没有加载，跳过
		if (!IsLevelInitializedForActor(Actor, Connection))
		{
			continue;
		}
		// 这个是判断Actor和所有ClientConnection的相关性，使用了Actor中这个NetCullDistanceSquared属性，超过这个距离就不同步，跳过
		if (!IsActorRelevantToConnection(Actor, ConnectionViewers))
		{
			continue;
		}
	}
	// 计算Actor的同步优先级
	if ( Actor->NetTag != NetTag )
	{
		Actor->NetTag = NetTag;
		OutPriorityList[FinalSortedCount] = FActorPriority( PriorityConnection, Channel, ActorInfo, ConnectionViewers, bLowNetBandwidth );
		OutPriorityActors[FinalSortedCount] = OutPriorityList + FinalSortedCount;
		FinalSortedCount++;
	}
}
// 最后进行排序
Sort( OutPriorityActors, FinalSortedCount, FCompareFActorPriority() );
```
这个方法需要计算需要同步Actor的优先级，优先级是通过AActor::GetNetPriority这个方法计算的，正常的返回值是NetPriority * Time，也就是Actor蓝图中可以配置乘上时间，这个时间代表了这个Actor距离上一次同步的时间差，然后这个时间差也乘上了个系数，这个系数的计算是根据CLOSEPROXIMITYSQUARED配置和Actor的距离来确定的。
#### 4 UNetDriver::ServerReplicateActors_ProcessPrioritizedActors
```cpp
int32 UNetDriver::ServerReplicateActors_ProcessPrioritizedActors(...)
{
	// 遍历我们排好序的Actor，拿到ActorInfo
	for ( int32 j = 0; j < FinalSortedCount; j++ )
	{
		FNetworkObjectInfo*	ActorInfo = PriorityActors[j]->ActorInfo;
		UActorChannel* Channel = PriorityActors[j]->Channel;
		// 当前处理的Actor的ActorChannel还没创建，或者是已经创建了
		if ( !Channel || Channel->Actor )
		{
			AActor* Actor = ActorInfo->Actor;
			// 表示了此次Actor同步，是否与Connection相关联
			bool bIsRelevant = false;
			// 当前Actor所在的Level，在对应Connection客户端是否加载了
			const bool bLevelInitializedForActor = IsLevelInitializedForActor( Actor, Connection );
			// 如果客户端加载了Actor所处的Level
			if ( bLevelInitializedForActor )
			{
				// GetTearOff 代表当前Actor不在参与网络复制，在客户端已经复制了的就改变其Role为ROLE_Authority
				// 不还没创建对应的ActorChannel或者是netDriver创建的持续时间 - Actor上一次与Connection相关联的时间 > 1。也就是没超过1s都检查下此Actor是否还与Connection相关联。
				if ( !Actor->GetTearOff() && ( !Channel || ElapsedTime - Channel->RelevantTime > 1.0 ) )
				{
					if ( IsActorRelevantToConnection( Actor, ConnectionViewers ) )
					{
						bIsRelevant = true;
					}
				}
			}
			// 这个标志位表示了，此Actor最近是否与Connection相关联
			// 如果此次同步Actor是相关联的 或者 当前距离上一次相关联时间小于5s 都是为true
			const bool bIsRecentlyRelevant = bIsRelevant || ( Channel && ElapsedTime - Channel->RelevantTime < RelevantTimeout ) || (ActorInfo->ForceRelevantFrame >= Connection->LastProcessedFrame);

			if ( bIsRecentlyRelevant )
			{
				FinalRelevantCount++;
				// 进行一系列判断(这里都省略掉了)
				// 判断是否能够给当前Actor创建ActorChannel
				if ( Channel == NULL)
				{
					// Actor所处的Level已经加载好了
					if ( bLevelInitializedForActor )
					{
						// 给当前Actor创建ActorChannel，并赋值ChannelActor
						Channel = (UActorChannel*)Connection->CreateChannelByName( NAME_Actor, EChannelCreateFlags::OpenedLocally );
						if ( Channel )
						{
							Channel->SetChannelActor(Actor, ESetChannelActorFlags::None);
						}
					}
					// Actor所处的Level已经没加载好，所以不能同步，也就不创建ActorChannel
					// 但如果Actor的同步更新频率很快，则我们就设置下Actor下次更新时间
					else if ( Actor->NetUpdateFrequency < 1.0f )
					{
						ActorInfo->NextUpdateTime = World->TimeSeconds + 0.2f * FMath::FRand();
					}
				}

				if ( Channel )
				{
					// 如果此次Actor是相关的，就记录一下相关时间
					if ( bIsRelevant )
					{
						Channel->RelevantTime = ElapsedTime + 0.5 * UpdateDelayRandomStream.FRand();
					}
					// Actor准备好了，可以发数据？
					if ( Channel->IsNetReady( 0 ) )
					{
						// 这个就是ActorChannel同步Actor数据的方法，后面详细看下
						if ( Channel->ReplicateActor() )
						{
							ActorUpdatesThisConnectionSent++;
							// 这边记录了下Actor同步的时间，是world的绝对时间
							ActorInfo->LastNetReplicateTime = World->TimeSeconds;
						}
						ActorUpdatesThisConnection++;
						OutUpdated++;
					}
				}
			}
			// 如果最近不相关，并且ActorChannel还存在
			// 如果Actor所处的Level还不存在 或者 Actor不是一个可同步Actor（直接放在地图里的可同步Actor）那就close ActorChannel
			if ( ( !bIsRecentlyRelevant || Actor->GetTearOff() ) && Channel!=NULL)
			{
				if ( !bLevelInitializedForActor || !Actor->IsNetStartupActor() )
				{
					// 关闭ActorChannel
					Channel->Close(Actor->GetTearOff() ? EChannelCloseReason::TearOff : EChannelCloseReason::Relevancy);
				}
			}
		}
	}
	// 返回的是已经处理的Actor也就是同步的或者是提前continue的
	return FinalSortedCount;
}
```
这个方法就是对根据优先级排好序需要同步Actor的处理。
1 首先确定是否相关。如果距离上一次记录的相关时间超过1s，就调用IsActorRelevantToConnection方法（这个方法里会用到NetCullDistanceSquared变量，Actor距离NetConnection的距离需要小于这个变量）检查，并将赋值bIsRelevant，表示这次同步是否相关。
2 赋值bIsRecentlyRelevant变量。这个变量表示Actor这次或者是RelevantTimeout（引擎中配置5s）秒前是相关的。
3 创建ActorChannel。如果我们还没有创建ActorChannel就创建一个，并调用SetChannelActor初始化Channel。
4 记录相关时间。是NetDriver持续时间 + 0.5 乘上随机数。
5 同步数据。ActorChannel调用ReplicateActor方法。
6 如果Actor最近不相关（此次同步是不相关的，RelevantTimeout秒前也是不相关的），并且Channel还存在，就关闭Channel。
7 返回值。返回了处理了的Actor数量，如果正常返回值会等于排好序Actor的数量，如果同步中途Connection饱和了，这一次就停止同步，提前返回这一次处理了多少Actor。
#### 5 UNetConnection::CreateChannelByName
```cpp
// ChName是空
// CreateFlags 是 OpenedLocally
// ChIndex是空，表示当前这个Channel是在NetConnection的Channels数组里的索引
UChannel* UNetConnection::CreateChannelByName(const FName& ChName, EChannelCreateFlags CreateFlags, int32 ChIndex)
{
	if (ChIndex == INDEX_NONE)
	{
		// 首先根据ChName获取ChIndex，看上去就是获取了Channels数组的长度，也就是会当前Channel的索引是最后一个
		ChIndex = GetFreeChannelIndex(ChName);
	}
	// 根据名称找Channel，找不见就创建一个
	UChannel* Channel = Driver->GetOrCreateChannelByName(ChName);
	/*
	初始化Channel中的一些数据
	Connection 传进来的第一个参数
	ChIndex 传进来的第二个参数
	OpenedLocally 传进来的第三个参数，这里为true
	OpenPacketId 初始化这个，调用普通构造函数
	bPausedUntilReliableACK = 0
	SentClosingBunch = 0
	*/
	Channel->Init( this, ChIndex, CreateFlags );
	// 把当前Channel保存到数组中
	Channels[ChIndex] = Channel;
	// 把当前Channel保存到打开数组中
	OpenChannels.Add(Channel);
	// 创建的时候是否开启Tick，这里好像没用到
	if (Driver->ChannelDefinitionMap[ChName].bTickOnCreate)  
	{  
	    StartTickingChannel(Channel);  
	}
	// 最终返回Channel
	return Channel;
}
```
#### 6 UActorChannel::ReplicateActor
```cpp
int64 UActorChannel::ReplicateActor()
{
	// 创建我们要发送的Bunch
	// 后面的部分就是为这个Bunch赋值
	FOutBunch Bunch( this, 0 );
	// 如果是第一次就把Actor序列化到Bunch里面
	if (RepFlags.bNetInitial && OpenedLocally)
	{
		Connection->PackageMap->SerializeNewActor(Bunch, this, Actor);
		WroteSomethingImportant = true;
		Actor->OnSerializeNewActor(Bunch);
	}
	// 这个是往Bunch里写入改变的属性
	ActorReplicator->ReplicateProperties(Bunch, RepFlags, bUpdateNothingChangedState, UniqueId);
	// 后面紧接着往Bunch里写入子对象，也就是身上挂着的UObject
	Actor->ReplicateSubobjects(this, &Bunch, &RepFlags);
	// 最后服务器这发送这个Bunch
	FPacketIdRange PacketRange = SendBunch( &Bunch, 1 );
}
```
这个方法里面不光有同步actor的部分还有同步属性的部分，后面看下
##### 1 UPackageMapClient::SerializeNewActor
```cpp
/*
*/
bool UPackageMapClient::SerializeNewActor(FArchive& Ar, class UActorChannel *Channel, class AActor*& Actor)
{
	FNetworkGUID NetGUID;
	UObject *NewObj = Actor;
	// 这个方法我们在下边分析了，如果不是静态Actor的话，这里只会往Ar中写入NetGUID
	// 除了地图中的Actor，也都是动态创建的Actor的吧，所以这里会写入NetGUID
	SerializeObject(Ar, AActor::StaticClass(), NewObj, &NetGUID);
	// 赋值Channel里的ActorNetGUID
	Channel->ActorNetGUID = NetGUID;
	if (UE::Net::FilterGuidRemapping != 0)
	{
	
		if ( GuidCache.IsValid() )
		{
			if (ensureMsgf(NetGUID.IsValid()))
			{
				GuidCache->ImportedNetGuids.Add(NetGUID);
			}
		}
	}
	Actor = Cast<AActor>(NewObj);
	if ( NetGUID.IsDynamic() )
	{
		if (Ar.IsSaving())
		{
			// 获取Actor的原型，通常是CDO
			if (UChildActorComponent* CAC = Actor->GetParentComponent())
			{
				Archetype = CAC->GetSpawnableChildActorTemplate();
			}
			if (Archetype == nullptr)
			{
				Archetype = Actor->GetArchetype();
			}
			// Actor所处的Level
			if (UE::Net::Private::SerializeNewActorOverrideLevel)
			{
				ActorLevel = Actor->GetLevel();
			}
			// 还会拿到基础信息Location,rotation,scale啥的
			const USceneComponent* RootComponent = Actor->GetRootComponent();
			if (RootComponent)
			{
				Location = FRepMovement::RebaseOntoZeroOrigin(Actor->GetActorLocation(), Actor);
				Rotation = Actor->GetActorRotation();
				Scale = Actor->GetActorScale();
			}
		}
		// 往Ar中序列化Archetype，也就是序列化CDO
		// 需要注意的是CDO是网络稳定的，也就是说这里和序列化Actor时候不同，具体差异后边分析SerializeObject方法时有说
		FNetworkGUID ArchetypeNetGUID;
		SerializeObject(Ar, UObject::StaticClass(), Archetype, &ArchetypeNetGUID);
		// 满足条件的话，也会把ActorLevel序列化进去
		// 这里条件就省略了，几乎100%满足条件
		if ()
		{
			SerializeObject(Ar, ULevel::StaticClass(), ActorLevel);
		}
		// 下面就是会将上面拿到的基础信息Location,rotation,scale序列化到Ar中
	}
}
```
###### 1 UPackageMapClient::SerializeObject
```cpp
bool UPackageMapClient::SerializeObject( FArchive& Ar, UClass* Class, UObject*& Object, FNetworkGUID *OutNetGUID)
{
	// IsSaving是服务器写入的过程
	if (Ar.IsSaving())
	{
		// 在服务器上给这个Object分配NetGUID
		FNetworkGUID NetGUID = GuidCache->GetOrAssignNetGUID( Object );
		if (OutNetGUID)
		{
			*OutNetGUID = NetGUID;
		}
		// 这个方法在这里的作用就是将NetGUID写入到Ar中，其余的部分应该都不走
		InternalWriteObject( Ar, NetGUID, Object, TEXT( "" ), NULL );
		if (!NetGUID.IsDefault() && ShouldSendFullPath(Object, NetGUID))
		{
			// 这个部分就是静态的Object么，里面会再次调用InternalWriteObject方法，
			// 这里面还会写入Object的Outer，是个递归，一直写入知道Outer已经被写如果了就退出
			// 多次调用InternalWriteObject方法，每次都会写入NetGUID，但写入的不是Ar，是CurrentExportBunch这个Bunch，这个Bunch最终会在SendBunch使用，会放在发送给客户端最终Bunch的头部
			if ( !ExportNetGUID( NetGUID, Object, TEXT(""), NULL ) )
			{
			}
		}
		return true;
	}
	// IsLoading是客户端读取的过程
	else if (Ar.IsLoading())
	{
		FNetworkGUID NetGUID;
		// 这个方法下边详细看下
		NetGUID = InternalLoadObject(Ar, Object, 0);
		// Object所在的Level没加载完
		if (!ObjectLevelHasFinishedLoading(Object))
		{
			Object = NULL;
		}
		// Object是否是正确的
		if (Object && !Object->IsA(Class))
		{
			Object = NULL;
		}
	}
}
```
这里用下知乎的图片，懒得化了。上边Archive就是正常的Bunch主体，下边的ExportNetworkGUID Info就是ExportBunch部分。
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202408151118700.png)

###### 2 UPackageMapClient::ExportNetGUID
```cpp
/*
1 这个方法是在上边SerializeObject方法调用，即Object满足if (!NetGUID.IsDefault() && ShouldSendFullPath(Object, NetGUID))这个条件调用过来的。
2 这个方法的作用就是将Object及其Outer的NetGUID都序列化到CurrentExportBunch这个Bunch里面。
3 如果CurrentExportBunch这个Bunch满了，就通过ExportNetGUIDHeader方法将CurrentExportBunch放到ExportBunches这个数组里面。
4 我们在SendBunch的时候，会执行AppendExportBunches方法，这个方法作用就是遍历ExportBunches数组，将其内容放到SendBunch里的要发送的Bunch中（在3中说的CurrentExportBunch满了ExportBunches才会有值，如果CurrentExportBunch没满，这一次SenBunch就不会发送么？也会发送，逻辑也是在AppendExportBunches方法中，这个方法会先判断CurrentExportBunch这个里面是否有值，就会把值放到ExportBunches数组里面）。
5 最终CurrentExportBunch这个Bunch会放在发送给客户端Bunch的头部
*/
```
###### 3 UPackageMapClient::InternalWriteObject
```cpp
void UPackageMapClient::InternalWriteObject( FArchive & Ar, FNetworkGUID NetGUID, UObject* Object, FString ObjectPathName, UObject* ObjectOuter )
{
	// 
	/*
		GuidCache->ShouldAsyncLoad() 客户端是否支持异步加载Object
		IsNetGUIDAuthority() 是否在服务器上
		!GuidCache->IsExportingNetGUIDBunch 不能是ExportingNetGUIDBunch的流程
		!bNoLoad 客户端可以加载此Object
	*/
	// 满足条件后，会将Object的NetGUID添加到MustBeMappedGuidsInLastBunch这个数组里
	if ( GuidCache->ShouldAsyncLoad() && IsNetGUIDAuthority() && !GuidCache->IsExportingNetGUIDBunch && !bNoLoad )
	{
		MustBeMappedGuidsInLastBunch.AddUnique( NetGUID );
	}
	// 把NetGUID写入
	Ar << NetGUID;
	if ( NetGUID.IsDefault() )
	{
		ExportFlags.bHasPath = 1;
		Ar << ExportFlags.Value;
	}
	// 上边的ExportNetGUID这个方法就会走这里，如果我们Object通过ShouldSendFullPath这个方法，他就会写入bHasPath，意思是静态的Object,或者说是网络名称稳定的那种
	else if ( GuidCache->IsExportingNetGUIDBunch )
	{
		if ( Object != NULL )
		{
			ExportFlags.bHasPath = ShouldSendFullPath( Object, NetGUID ) ? 1 : 0;
		}
		else
		{
			ExportFlags.bHasPath = ObjectPathName.IsEmpty() ? 0 : 1;
		}
		ExportFlags.bNoLoad	= bNoLoad ? 1 : 0;
		Ar << ExportFlags.Value;
	}
	if ( ExportFlags.bHasPath )
	{
		if ( Object != NULL )
		{
			ObjectPathName = Object->GetName();
			ObjectOuter = Object->GetOuter();
		}
		// 是不是Package里面的Object，也就是场景中放置的Object
		// 如果Object不是网络名称稳定的，这个NetGUID.IsStatic()就返回true
		const bool bIsPackage = ( NetGUID.IsStatic() && Object != NULL && Object->GetOuter() == NULL );
		// 拿到Outer的GUID，然后递归调用
		FNetworkGUID OuterNetGUID = GuidCache->GetOrAssignNetGUID( ObjectOuter );
		InternalWriteObject( Ar, OuterNetGUID, ObjectOuter, TEXT( "" ), NULL );
		// 这部分就是写入ObjectPathName，这个就是Object那个路径吧
		if (Connection->Driver)
		{
			FName SearchPath = FName(*ObjectPathName);
			FName RenamedPath = Connection->Driver->RenamedStartupActors.FindRef(SearchPath);
			if (RenamedPath != NAME_None)
			{
				ObjectPathName = RenamedPath.ToString();
			}
		}
		// 这个GEnigne是走GameEngine么，打断点是走到的GameEngine
		GEngine->NetworkRemapPath(Connection, ObjectPathName, false);
		Ar << ObjectPathName;
		// 写入NetworkChecksum，还不知道这个干嘛的
		uint32 NetworkChecksum = 0;
		if ( ExportFlags.bHasNetworkChecksum )
		{
			NetworkChecksum = GuidCache->GetNetworkChecksum( Object );
			Ar << NetworkChecksum;
		}
		// 给ObjectLookup数组里存的，重新赋值
		FNetGuidCacheObject* CacheObject = GuidCache->ObjectLookup.Find( NetGUID );
		if ( CacheObject != NULL )
		{
			CacheObject->PathName			= FName( *ObjectPathName );
			CacheObject->OuterGUID			= OuterNetGUID;
			CacheObject->bNoLoad			= ExportFlags.bNoLoad;
			CacheObject->bIgnoreWhenMissing = ExportFlags.bNoLoad;
			CacheObject->NetworkChecksum	= NetworkChecksum;
		}
	}
}
```
#### 7 UChannel::SendBunch
```cpp
FPacketIdRange UChannel::SendBunch( FOutBunch* Bunch, bool Merge )
{
	TArray<FOutBunch*>& OutgoingBunches = Connection->GetOutgoingBunches();
	OutgoingBunches.Reset();
	if (!Connection->IsInternalAck())
	{
		// 这个方法会将ExportBunch里的Bunch都放到OutgoingBunches
		AppendExportBunches( OutgoingBunches );
	}
	if ( Connection->Driver->IsServer() )
	{
		// 这个方法会将MustBeMappedGuidsInLastBunch中的数据放到Bunch的前边
		AppendMustBeMappedGuids( Bunch );

		if ( Bunch->bHasMustBeMappedGUIDs )
		{
			Merge = false;
		}
	}
	// 如果大Bunch很大就拆分成小Bunch，然后将小Bunch添加到OutgoingBunches数组中
	if( Bunch->GetNumBits() > MAX_SINGLE_BUNCH_SIZE_BITS )
	{
		// 这个里面省略了分割的代码
	}
	else
	{
		OutgoingBunches.Add(Bunch);
	}
	for( int32 PartialNum = 0; PartialNum < OutgoingBunches.Num(); ++PartialNum)
	{
		FOutBunch *ThisOutBunch = PrepBunch(NextBunch, OutBunch, Merge);
		//将每个小Bunch添加头部信息，并记录当前Bunch在Pcket中的位置
		int32 PacketId = SendRawBunch(ThisOutBunch, Merge, GetTraceCollector(*NextBunch));
	}
}
```
这里用下知乎的图片，懒得化了。最终SendBunch里面的Bunch结构就像下图中的所示。
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202408151125503.png)

## 2 Client Actor接收 UIpNetDriver::TickDispatch
```cpp
void UWord::Tick(...)
{
	{
		// 这里调用到TickDispatch
		BroadcastTickDispatch(DeltaSeconds);
		BroadcastPostTickDispatch();
		if( NetDriver && NetDriver->ServerConnection )
		{
			TickNetClient( DeltaSeconds );
		}
	}
}


void UIpNetDriver::TickDispatch(float DeltaTime)
{
	// 真正处理之前，在找是哪个Connection，找不到就重新握手建立连接
	
	// 真正处理收到的Packet
	Connection->ReceivedRawPacket((uint8*)ReceivedPacket.DataView.GetData(), ReceivedPacket.DataView.NumBytes());
}

```
### 1 UNetConnection::ReceivedRawPacket 
```cpp
void UNetConnection::ReceivedRawPacket( void* InData, int32 Count )
{
	ReceivedPacket(Reader);
}

void UNetConnection::ReceivedPacket( FBitReader& Reader, bool bIsReinjectedPacket)
{
	// 这里是在客户端创建了ActorChannel
	Channel = CreateChannelByName( Bunch.ChName, EChannelCreateFlags::None, Bunch.ChIndex );
	// 这个方法就是组装Bunch吧，然后调用ReceivedRawBunch这个
	Channel->ReceivedRawBunch( Bunch, bLocalSkipAck );
}

void UChannel::ReceivedRawBunch( FInBunch & Bunch, bool & bOutSkipAck )
{
	if ( Bunch.bHasPackageMapExports && !Connection->IsInternalAck() )
	{
		// 如果是ExportBunch的部分就先解析
		Cast<UPackageMapClient>( Connection->PackageMap )->ReceiveNetGUIDBunch( Bunch );
	}
	bDeleted = ReceivedNextBunch( *Release, bLocalSkipAck );
}
// 注意这个方法，客户端优先会加载ExportBunch中的内容，也就是网络名称稳定的那些UObject，并注册NetGUID
void UPackageMapClient::ReceiveNetGUIDBunch( FInBunch &InBunch )
{
	const bool bHasRepLayoutExport = InBunch.ReadBit() == 1 ? true : false;
	TGuardValue<bool> IsExportingGuard(GuidCache->IsExportingNetGUIDBunch, true);
	int32 NumGUIDsRead = 0;
	while( NumGUIDsRead < NumGUIDsInBunch )
	{
		UObject* Obj = NULL;
		const FNetworkGUID LoadedGUID = InternalLoadObject( InBunch, Obj, 0 );
		NumGUIDsRead++;
	}
}

bool UChannel::ReceivedNextBunch( FInBunch & Bunch, bool & bOutSkipAck )
{
	return ReceivedSequencedBunch( *HandleBunch );
}

bool UChannel::ReceivedSequencedBunch( FInBunch& Bunch )
{
	// 最终会执行ReceivedBunch方法
	if ( !Closing )
	{
		ReceivedBunch( Bunch );
	}
}
```
看不太懂这些方法里面的操作，就是对服务器发送过来的包进行处理，组装出Bunch，最终会调用到对应Channel的ReceivedBunch方法。
### 2 UActorChannel::ReceivedBunch
```cpp
void UActorChannel::ReceivedBunch( FInBunch & Bunch )
{
	TArray<TPair<FNetworkGUID, UObject*>> QueuedObjectsToTrack;
	if (Connection->Driver->IsServer())
	{
		// 在服务器上如果还有MustBeMappedGUIDs,就说明有问题
		// bHasMustBeMappedGUIDs这个字段标识了，客户端收到同步Object，是否用异步的方式创建
		if (Bunch.bHasMustBeMappedGUIDs)
		{
			Bunch.SetError();
			return;
		}
	}
	else
	{
		if (Bunch.bHasMustBeMappedGUIDs)
		{
			// 从Bunch中拿出有多少个GUID的Object，可以被异步加载出来
			uint16 NumMustBeMappedGUIDs = 0;
			Bunch << NumMustBeMappedGUIDs;
			// 设置数组的大小
			QueuedObjectsToTrack.SetNum(NumMustBeMappedGUIDs);
			// 拿到GUIDCache
			FNetGUIDCache* GuidCache = Connection->Driver->GuidCache.Get();
			// 遍历
			for (int32 i = 0; i < NumMustBeMappedGUIDs; i++)
			{
				// 拿出GUID
				FNetworkGUID NetGUID;
				Bunch << NetGUID;
				// 如果不支持异步加载，就直接返回掉
				if (!GuidCache->ShouldAsyncLoad())
				{
					continue;
				}
				// 在Cache中找到了NetGUID对应的CacheObject
				if (FNetGuidCacheObject const * const GuidCacheObject = GuidCache->GetCacheObject(NetGUID))
				{
					// 如果Cache中的Object已经加载出来了，放到QueuedObjectsToTrack数组里
					if (UObject* const Object = GuidCacheObject->Object.Get())
					{
						QueuedObjectsToTrack.Emplace(NetGUID, Object);
					}
					else
					// Cache中的Object没有加载出来
					{
						// 放到PendingGuidResolves数组里
						PendingGuidResolves.Add(NetGUID);
						// 将此Channel添加到ChannelsToTick数组中，等待被Tick
						Connection->StartTickingChannel(this);
						// 客户端会把未解析出静态Object的NetGUID放到ActorChannel的QueuedBunchObjectReferences数组中，也会放到NetGUIDCache的QueuedBunchObjectReferences数组中
						QueuedBunchObjectReferences.Add(GuidCache->TrackQueuedBunchObjectReference(NetGUID, nullptr));
					}
				}
				// 如果在Cache中找不见NetGUID对应的CacheObject，就说明有问题
				// 这也说明了在ReceivedBunch之前，就对静态的Object完成了注册
				// 静态的Object都是在ExportBunch中是在ReceivedRawBunch方法里注册的
				else
				{
					Bunch.SetError();
					return;
				}
			}
		}
		if (Actor == NULL && Bunch.bOpen)
		{
			// 序列化出ActorNetGUID
			Bunch << ActorNetGUID;
		}
		if (PendingGuidResolves.Num() > 0 || QueuedBunches.Num() > 0 || Connection->KeepProcessingActorChannelBunchesMap.Contains(ActorNetGUID) ||
			 (Connection->Driver->ShouldQueueBunchesForActorGUID(ActorNetGUID)))
		{
			// 把当前这个Bunch加入QueueBunches里
			QueuedBunches.Add(new FInBunch(Bunch));
			Connection->StartTickingChannel(this);
			return;
		}
	}
	// 这个EnableProcessNewActorOnly是我们自己加的，ue5没有
	if (EnableProcessNewActorOnly)
	{
		// Azure SpawnedNewActor
		// 如果ActorChannel里面没有Actor，就根据Bunch里的数据创建一个新的
		if (Actor == nullptr)
		{
			SpawnedNewActor(Bunch);
		}

		// We can process this bunch now
		ProcessBunchWithoutNewActor(Bunch);

		// Azure PostSpawnedNewActor
		if (bSpawningNewActor)
		{
			PostSpawnedNewActor();
		}
	}
	else
	{
		// We can process this bunch now
		ProcessBunch(Bunch);
	}
}
```
### 3 UActorChannel::SpawnedNewActor
```cpp
bool UActorChannel::SpawnedNewActor(FInBunch & Bunch)
{
	AActor* NewChannelActor = NULL;
	// 通过Bunch的内容反序列化，创建出Actor
	bool bNewActor = Connection->PackageMap->SerializeNewActor(Bunch, this, NewChannelActor);

	ESetChannelActorFlags Flags = ESetChannelActorFlags::None;
	if (GSkipReplicatorForDestructionInfos != 0 && Bunch.bClose && Bunch.AtEnd())
	{
		Flags |= ESetChannelActorFlags::SkipReplicatorCreation;
	}
	// 将新创建的Actor和客户端ActorChannel关联起来
	SetChannelActor(NewChannelActor, Flags);
	// 通知客户端Actor已经创建好了
	NotifyActorChannelOpen(Actor, Bunch);
	// 记录一些变量
	Actor->CustomTimeDilation = CustomTimeDilation;
	bSpawningNewActor = true;
	Connection->Driver->SpawnedNewActorChannels.Add(this);
	if (!Connection->Driver->IsServer() && EnableDeferredRepNotify)
	{
		bDeferredRepNotify = true;
	}
	
	return true;
}
```
这个是方法是客户端的ActorChannel里面没有对应的Actor，也就是Actor服务器创建了，需要同步到客户端，那么客户端就通过Bunch里面的数据反序列化创建新的Actor，并于ActorChannel相关联。
#### 1 UPackageMapClient::SerializeNewActor
```cpp
bool UPackageMapClient::SerializeNewActor(FArchive& Ar, class UActorChannel *Channel, class AActor*& Actor)
{
	// 客户端这也是一样，首先从Ar中序列化出Actor
	FNetworkGUID NetGUID;
	UObject *NewObj = Actor;
	SerializeObject(Ar, AActor::StaticClass(), NewObj, &NetGUID);
	// 设置Channel中的NetGUID
	Channel->ActorNetGUID = NetGUID;
	Actor = Cast<AActor>(NewObj);
	// 标记这一帧解析出了新的NetGUID
	if (bFilterGuidRemapping)
	{
		if ( GuidCache.IsValid() )
		{
			if (NetGUID.IsValid())
			{
				GuidCache->ImportedNetGuids.Add(NetGUID);
			}
		}
	}
	if ( NetGUID.IsDynamic() )  
	{
		// 同理从Ar中序列化出Actor的Archetype和Actor所在的Level，这两个都是网络稳定的，所以是从内存中找就行
		FNetworkGUID ArchetypeNetGUID;
		SerializeObject(Ar, UObject::StaticClass(), Archetype, &ArchetypeNetGUID);
		if (Ar.IsSaving())
		{
			SerializeObject(Ar, ULevel::StaticClass(), ActorLevel);
		}
		// 然后是从Ar中序列化出Location,Rotation,Scale,Velocity信息
		// 下面应该就是根据序列化出的Archetype,Location,Rotation,Scale,Velocity信息来SpawnActor了
		if ( Ar.IsLoading() )
		{
			if ( Actor == NULL )
			{
				if ( Archetype )
				{
					ULevel* SpawnLevel = Cast<ULevel>(ActorLevel);
					if (SpawnLevel == nullptr || SpawnLevel->GetWorld() != nullptr)
					{
						UWorld* World = Connection->Driver->GetWorld();
						FVector SpawnLocation = FRepMovement::RebaseOntoLocalOrigin(Location, World->OriginLocation);
						if (!Actor)
						{
							FActorSpawnParameters SpawnInfo;
							SpawnInfo.Template = Cast<AActor>(Archetype);
							SpawnInfo.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
							SpawnInfo.bRemoteOwned = true;
							SpawnInfo.bNoFail = true;
							// 这里就是SpawnActorAbsolute
							Actor = World->SpawnActorAbsolute(Archetype->GetClass(), FTransform(Rotation, SpawnLocation), SpawnInfo);
						}
						if (Actor)
						{
							// 这里创建完Actor后，设置Actor的属性
							if (bSerializeVelocity)
							{
								Actor->PostNetReceiveVelocity(Velocity);
							}
							if (bSerializeScale)
							{
								Actor->SetActorScale3D(Scale);
							}
							// 客户端注册Actor的NetGUID
							GuidCache->RegisterNetGUID_Client(NetGUID, Actor);
							bActorWasSpawned = true;
						}
					}
				}
			}
		}
	}
	// 返回Actor是否生成的标志位
	return bActorWasSpawned;
}
```

#### 2 UPackageMapClient::SerializeObject
```cpp
bool UPackageMapClient::SerializeObject( FArchive& Ar, UClass* Class, UObject*& Object, FNetworkGUID *OutNetGUID)
{
	// IsSaving是服务器写入的过程
	if (Ar.IsSaving())
	{
		// 省略掉
	}
	// IsLoading是客户端读取的过程
	else if (Ar.IsLoading())
	{
		FNetworkGUID NetGUID;
		// 这个方法下边详细看下
		NetGUID = InternalLoadObject(Ar, Object, 0);
		// Object所在的Level没加载完
		if (!ObjectLevelHasFinishedLoading(Object))
		{
			Object = NULL;
		}
		// Object是否是正确的
		if (Object && !Object->IsA(Class))
		{
			Object = NULL;
		}
	}
}
```
#### 3 UPackageMapClient::InternalLoadObject
```cpp
// Ar 客户端收到的这个Bunch
// Object 这个Bunch里的Subobject，客户端需要序列化出来，然后传出去
// InternalLoadObjectRecursionCount 递归的次数，通过SerializeObject这里传进来是0
FNetworkGUID UPackageMapClient::InternalLoadObject( FArchive & Ar, UObject *& Object, const int InternalLoadObjectRecursionCount )  
{
	// 从Ar中读取出NetGUID
	FNetworkGUID NetGUID;  
	Ar << NetGUID;
	// 尝试通过NetGUID拿到他对应的Object，这个方法在客户端 子物体接收的地方看下
	if ( NetGUID.IsValid() && !NetGUID.IsDefault() )
	{
		Object = GetObjectFromNetGUID(NetGUID,GuidCache->IsExportingNetGUIDBunch );
	}
	// 和InternalWriteObject方法写入的时候保持一致，这里读取出FExportFlags
	FExportFlags ExportFlags;
	if ( NetGUID.IsDefault() || GuidCache->IsExportingNetGUIDBunch )
	{
		Ar << ExportFlags.Value;
	}
	// 记录我们的ImportNetGUID
	if ( GuidCache->IsExportingNetGUIDBunch )
	{
		if (NetGUID.IsValid())
		{
			GuidCache->ImportedNetGuids.Add( NetGUID );
		}
	}
	if ( ExportFlags.bHasPath )
	{
		// 递归，获取outer的NetGUID
		FNetworkGUID OuterGUID = InternalLoadObject( Ar, ObjOuter, InternalLoadObjectRecursionCount + 1 );
		// 读取我们的路径名称
		Ar << PathName;
		// 读取NetworkChecksum
		if ( ExportFlags.bHasNetworkChecksum )
		{
			Ar << NetworkChecksum;
		}
		// 网络名称稳定的NetGUID.IsStatic()为true
		const bool bIsPackage = NetGUID.IsStatic() && !OuterGUID.IsValid();
		
		GEngine->NetworkRemapPath( Connection, PathName, true );
		// 客户端这边注册NetGUID,也就是写到ObjectLookup数组中
		GuidCache->RegisterNetGUIDFromPath_Client( NetGUID, PathName, OuterGUID, NetworkChecksum, ExportFlags.bNoLoad, bIgnoreWhenMissing );
		// 再一次尝试通过NetGUID来拿到他对应的UObject
		Object = GuidCache->GetObjectFromNetGUID( NetGUID, GuidCache->IsExportingNetGUIDBunch );
	}
}
```
# 3 Actor属性同步
## 1 Server 属性同步准备 UActorChannel::SetChannelActor
还记得我们在 UNetDriver::ServerReplicateActors_ProcessPrioritizedActors 这个方法里面，如果actor的actorChnnel不存在，我们就会创建一个新的actorChannel，在创建完成后我们会调用SetChannelActor方法，将ActorChannel和Actor关联起来。
```cpp
void UActorChannel::SetChannelActor(AActor* InActor, ESetChannelActorFlags Flags)
{
	// 设置成员变量
	Actor = InActor;
	bActorIsPendingKill = false;
	// 如果actor存在
	if (Actor)
	{
		// 将Actor和ActorChannel的关联保存到Connection的map里面
		Connection->AddActorChannel(Actor, this);
		// 判断这个参数是否是跳过属性复制，这里Flags为ESetChannelActorFlags::None
		if (!EnumHasAnyFlags(Flags, ESetChannelActorFlags::SkipReplicatorCreation))
		{
			// 找or创建我们Actor的属性复制器
			ActorReplicator = FindOrCreateReplicator(Actor);
		}

		if (!EnumHasAnyFlags(Flags, ESetChannelActorFlags::SkipMarkActive))
		{
			// 从休眠数组中移除，加入到激活数组ActiveNetworkObjects中
			Connection->Driver->GetNetworkObjectList().MarkActive(Actor, Connection, Connection->Driver);
			Connection->Driver->GetNetworkObjectList().ClearRecentlyDormantConnection(Actor, Connection, Connection->Driver);
		}
	}
}
```
在服务器上，SetChannelActor是紧接着ActorChannel的创建之后调用的，在这个关键的就是赋值ActorReplicator这个成员变量，通过找已经创建的或者是新创建一个Actor的属性复制器。
### Server FObjectReplicator 创建
```cpp
TSharedRef<FObjectReplicator>& UActorChannel::CreateReplicator(...)
{
	TSharedPtr<FObjectReplicator> NewReplicator;
	// 第一部分，创建新的属性复制器，下面详细看下
	NewReplicator = Connection->CreateReplicatorForNewActorChannel(Obj);
	// 将我们的acotr和对应的复制器添加到ReplicationMap里面
	TSharedRef<FObjectReplicator>& NewRef = ReplicationMap.Add(Obj, NewReplicator.ToSharedRef());
	// 从休眠数组中移除
	Connection->DormantReplicatorMap.Remove(Obj);
	// 第二部分，主要是服务器用来记录StaticBuffer
	NewRef->StartReplicating(this);
	return NewRef;
}
```
这个方法就是创建复制器的大方法，里面主要就是三部分。
第一部分，通过CreateReplicatorForNewActorChannel创建出NewReplicator，并在这个方法里做一些初始化，后面详细看下这个方法。然后将obj和NewReplicator添加到ReplicationMap里面缓存。
第二部分，StartReplicating方法，再做一些初始化的东西，后面详细看下这个方法。
#### 1 第一部分
```cpp
// 创建FObjectReplicator没什么说的，主要就是看数据初始化InitWithObject的方法
TSharedPtr<FObjectReplicator> UNetConnection::CreateReplicatorForNewActorChannel(UObject* Object)
{
	TSharedPtr<FObjectReplicator> NewReplicator = MakeShareable(new FObjectReplicator());
	NewReplicator->InitWithObject( Object, this, true );
	return NewReplicator;
}

void FObjectReplicator::InitWithObject(...)
{
	// 这里这个方法保存了InObject的指针，也就是保存了需要同步对象的所有数据，当同步对象属性变化的时候，自然这个指针指向的数据也发生了变化
	SetObject(InObject);
	// 一系列的成员变量初始化
	ObjectClass = InObject->GetClass(); // 同步对象的UClass
	Connection = InConnection;// 复制器属于哪个Connection
	RemoteFunctions = nullptr;
	bHasReplicatedProperties = false;
	bOpenAckCalled = false;
	RepState = nullptr;
	OwningChannel = nullptr;// Initially nullptr until StartReplicating is called
	TrackedGuidMemoryBytes = 0;
	// 这个成员变量关键，是描述了同步对象的同步属性布局
	// 通过UClass里的ClassReps数组创建的FRepLayout，很复杂后面看下吧
	RepLayout = Connection->Driver->GetObjectClassRepLayout(ObjectClass);
	// bUseDefaultState这个参数我们传进来的是true
	uint8* Source = bUseDefaultState ? (uint8*)GetObject()->GetArchetype() : (uint8*)InObject;
	// 初始化同步数据，下面看下这个
	InitRecentProperties(Source);
	// 将自己复制器添加到AllOwnedReplicators数组中
	Connection->Driver->AllOwnedReplicators.Add(this);

```
这个主要就是InitWithObject这个方法，初始化Replicator一些成员变量，其中就有创建RepLayout，然后就是初始化同步数据的这个InitRecentProperties方法，这个方法里会创建repState以及RepChangedPropertyTracker，最后就是把创建出来的ObjectReplicator保存起来。
##### 1 FObjectReplicator中RepLayout 创建

```cpp
TSharedPtr<FRepLayout> UNetDriver::GetObjectClassRepLayout( UClass * Class )
{
	TSharedPtr<FRepLayout>* RepLayoutPtr = RepLayoutMap.Find(Class);

	if (!RepLayoutPtr) 
	{
		// 走的应该是ECreateRepLayoutFlags::None，MaySendProperties() 这个函数返回false
		ECreateRepLayoutFlags Flags = MaySendProperties() ? ECreateRepLayoutFlags::MaySendProperties : ECreateRepLayoutFlags::None;
		RepLayoutPtr = &RepLayoutMap.Add(Class, FRepLayout::CreateFromClass(Class, ServerConnection, Flags));
	}

	return *RepLayoutPtr;
}
```
这个方法就是通过UNetDriver创建出RepLayout，然后存储到RepLayoutMap数组里面
```cpp
void FRepLayout::InitFromClass(
	UClass* InObjectClass, // 对象的UClass
	const UNetConnection* ServerConnection, // 在服务器上是null的
	const ECreateRepLayoutFlags CreateFlags) // ECreateRepLayoutFlags::None
{
	// 这个方法就是组装UClass的ClassReps，也就是需要同步的属性数组
	// 这个会把父类的和自己的需要同步的属性都记录上
	InObjectClass->SetUpRuntimeReplicationData();
	// 赋值Parents的大小
	Parents.Empty(InObjectClass->ClassReps.Num());
	for (int32 i = 0; i < InObjectClass->ClassReps.Num(); i++)  
	{
		FProperty * Property = InObjectClass->ClassReps[i].Property;
		const int32 ArrayIdx = InObjectClass->ClassReps[i].Index;
		// 下面就是遍历Reps，将其添加到Parents数组里面，ParentHandle是数组索引
		const int32 ParentHandle = AddParentProperty(Parents, Property, ArrayIdx);
		// 当前属性在Parents里面的偏移
		const int32 ParentOffset = Property->ElementSize * ArrayIdx;
		FInitFromPropertySharedParams SharedParams
		{
			/*Cmds=*/Cmds,//这里为空吧
			/*ServerConnection=*/ServerConnection,//服务器上这个也为空
			/*ParentIndex=*/ParentHandle,//当前属性在Parents里面的索引吧
			/*Parent=*/Parents[ParentHandle],//当前属性
			/*bHasObjectProperties=*/false,//当前属性是否是UObject类型？
			/*bHasNetSerializeProperties=*/false,//当前属性已经序列化过了？
			/*NetSerializeLayouts=*/GbTrackNetSerializeObjectReferences ? &TempNetSerializeLayouts : nullptr,// nullptr
		};
		FInitFromPropertyStackParams StackParams
		{
			/*Property=*/Property,// 当前属性
			/*Offset=*/ParentOffset,// 当前属性在Parents里面的偏移
			/*RelativeHandle=*/RelativeHandle,// 
			/*ParentChecksum=*/0,//
			/*StaticArrayIndex=*/ArrayIdx//当前属性在ClassRep里面的偏移
		};
		// CmdStart是0？
		Parents[ParentHandle].CmdStart = Cmds.Num();
		// 下面这个方法里会往cmd里面添加属性，就是说我们有一个属性是数组A，我们会把数组A放到Parents里面，然后会把数组A里的元素放到Cmds里面。数组，结构体数组，结构体都是这样保存的
		RelativeHandle = InitFromProperty_r<ERepBuildType::Class>(SharedParams, StackParams);
		// 记录当前属性CmdEnd
		Parents[ParentHandle].CmdEnd = Cmds.Num();
		// 记录当前属性Flags
		Parents[ParentHandle].Flags |= ERepParentFlags::IsConditional;
		// 记录当前属性在Parents偏移
		Parents[ParentHandle].Offset = GetOffsetForProperty<ERepBuildType::Class>(*Property) + ParentOffset;
		// 如果当前属性的cmd中存了东西，就保存下cmd的offset。Parents里的offset是当前属性在类中的偏移，cmds的offset是cmd里面元素的offset
		if (Parents[i].CmdEnd > Parents[i].CmdStart)
		{
			LastOffset = Cmds[Parents[i].CmdStart].Offset;
		}
		// 继续给Parents[ParentHandle].Flags或上标志位,先省略掉了
	}
	// 这下面这部分就是同步属性的条件，通过拿到对象的cdo，然后调用GetLifetimeReplicatedProps方法，拿到对象中属性的同步条件，存储到LifetimeProps数组里面
	TArray<FLifetimeProperty> LifetimeProps;
	LifetimeProps.Reserve(Parents.Num());
	UObject* Object = InObjectClass->GetDefaultObject();
	Object->GetLifetimeReplicatedProps(LifetimeProps);
	for (int32 i = 0; i < LifetimeProps.Num(); i++)
	{
		// 拿到可复制属性的在Parents数组里的索引
		const int32 ParentIndex = LifetimeProps[i].RepIndex;
		// 接下来就是判断属性赋值条件，然后存储起来
		if (COND_Never == LifetimeProps[i].Condition)
		{
			continue;
		}
		// 记录当前属性的复制条件
		Parents[ParentIndex].Condition = LifetimeProps[i].Condition;  
		// 记录当前属性OnRep的条件
		Parents[ParentIndex].RepNotifyCondition = LifetimeProps[i].RepNotifyCondition;
		// 这边还记录了OnRep的参数数量，也就是说OnRep函数是可以传参数的
		if (UFunction* RepNotifyFunc = InObjectClass->FindFunctionByName(Parents[ParentIndex].Property->RepNotifyFunc))
		{
			Parents[ParentIndex].RepNotifyNumParams = RepNotifyFunc->NumParms;
		}
	}
	// 这个方法就是赋值ShadowDataBufferSize这个大小，通过InObjectClass得到可复制属性在内存中的空间，然后赋值到ShadowDataBufferSize里面
	BuildShadowOffsets<ERepBuildType::Class>(InObjectClass, Parents, Cmds, ShadowDataBufferSize);
	// 最终设置变量
	Owner = InObjectClass;
}
```
FRepLayout就是找到UClass中记录的需要赋值的属性，然后初始化RepLayout的成员变量。
注意这里面会走到Object的GetLifetimeReplicatedProps方法，来获取同步属性的同步条件，看上去这个更侧重生命周期，也就是什么时机同步，往哪个端同步。
###### InitFromProperty_r
```cpp
template<ERepBuildType BuildType>
static int32 InitFromProperty_r(FInitFromPropertySharedParams& SharedParams,
	FInitFromPropertyStackParams StackParams)
{
	// 对数组的处理
	if (FArrayProperty* ArrayProp = CastField<FArrayProperty>(StackParams.Property))  
	{
		
	}
	// 对结构体的处理
	else if(FStructProperty* StructProp = CastField<FStructProperty>(StackParams.Property))
	{
	}
	// 对一般数据的处理
	else
	{
		++StackParams.RelativeHandle;
		StackParams.Offset += GetOffsetForProperty<BuildType>(*StackParams.Property);
		// 将我们的属性添加到SharedParams的Cmds里面
		AddPropertyCmd(SharedParams, StackParams);
	}
	return StackParams.RelativeHandle;
}
```
##### 2 FObjectReplicator中InitRecentProperties方法 FRepChangedPropertyTracker创建部分
```cpp
// ue5版本的
void FObjectReplicator::InitRecentProperties(uint8* Source)
{
	// 通过Connection创建出RepChangedPropertyTracker
	ConnectionDriver->FindOrCreateRepChangedPropertyTracker(MyObject)
}

// 最终会走到这个创建的方法里面,参数是需要同步的UObject
TSharedPtr<FRepChangedPropertyTracker> FNetPropertyConditionManager::FindOrCreatePropertyTracker(const FObjectKey ObjectKey)
{
	TSharedPtr<FRepChangedPropertyTracker> Tracker = FindPropertyTracker(ObjectKey);
	if (!Tracker.IsValid())
	{
		if (UObject* Obj = ObjectKey.ResolveObjectPtr())
		{
			// 开始的步骤一样，拿到Object的UClass，然后设置需要同步的属性数组ClassReps
			UClass* ObjectClass = Obj->GetClass();
			// 设置UClass中的ClassReps?在添加上运行时的可复制属性？
			ObjectClass->SetUpRuntimeReplicationData();
			const int32 NumProperties = ObjectClass->ClassReps.Num();
			// 下边是调用GetReplicatedCustomConditionState方法，判断需要同步的属性
			// 是否满足自定义的激活条件，如果满足才会同步
			FCustomPropertyConditionState ActiveState(NumProperties);
			Obj->GetReplicatedCustomConditionState(ActiveState);
			// 最终创建Tracker，然后添加到map中记录
			Tracker = MakeShared<FRepChangedPropertyTracker>(MoveTemp(ActiveState));
			PropertyTrackerMap.Add(ObjectKey, Tracker);
		}
	}
	// 最终返回
	return Tracker;
}
```
就是创建RepChangedPropertyTracker，通过UClass找到需要同步的属性，然后会调用UObject的GetReplicatedCustomConditionState这个方法，这个方法的作用是判断UClass中需要同步的属性是否满足我们自定义的同步条件，如果满足才能同步？应该是这个意思。
##### 3 FObjectReplicator中InitRecentProperties方法 FRepState创建部分
```cpp
void FObjectReplicator::InitRecentProperties(uint8* Source)
{
	// 先弄个标志位，如果是服务器就是SkipCreateReceivingState，客户端是None
	ECreateRepStateFlags Flags = (Connection->IsInternalAck() && bIsServer) ? ECreateRepStateFlags::SkipCreateReceivingState : ECreateRepStateFlags::None;
	{
		RepState = LocalRepLayout.CreateRepState(Source, RepChangedPropertyTracker, Flags);
	};
}

TUniquePtr<FRepState> FRepLayout::CreateRepState(
	const FConstRepObjectDataBuffer Source,
	TSharedPtr<FRepChangedPropertyTracker>& InRepChangedPropertyTracker,
	ECreateRepStateFlags CreateFlags) const
{
	TUniquePtr<FRepState> RepState(new FRepState());

	// 设置标志位，客户端上是没有Tracker的，而服务器有
	const bool bIsServer = InRepChangedPropertyTracker.IsValid();
	// 在服务器上，这个判断通过
	if (InRepChangedPropertyTracker.IsValid())
	{
		// 设置初始值
		RepState->SendingRepState.Reset(new FSendingRepState());
		RepState->SendingRepState->RepChangedPropertyTracker = InRepChangedPropertyTracker;

		// 重新构建ConditionalMap，也就是属性的同步条件
		// 看上去这里啥都没走，初始化的时候不走吧
		RebuildConditionalProperties(RepState->SendingRepState.Get(), FReplicationFlags());
		// 初始化这个不活跃数组都为false，数量和Parents保持一致
		RepState->SendingRepState->InactiveParents.Init(false, Parents.Num());
	}
	// 只有在服务器上并且是IsInternalAck的情况才会为true
	if (!EnumHasAnyFlags(CreateFlags, ECreateRepStateFlags::SkipCreateReceivingState))
	{
		// 初始化这个StaticBuffer数组
		FRepStateStaticBuffer StaticBuffer(AsShared());
		// 依然是客户端会初始化这个StaticBuffer
		if (!bIsServer)
		{
			// 初始化StaticBuffer
			InitRepStateStaticBuffer(StaticBuffer, Source);
		}
		// 赋值ReceivingRepState，看上去服务器和客户端都有这个ReceivingRepState呀
		RepState->ReceivingRepState.Reset(new FReceivingRepState(MoveTemp(StaticBuffer)));
	}
	return RepState;
}
```
创建这个RepState分成了服务器客户端两部分，在服务器上会创建FSendingRepState和ReceivingRepState，但是在这里只有客户端的StaticBuffer会进行初始化
###### 1 客户端上的StaticBuffer
```cpp
void FRepLayout::InitRepStateStaticBuffer(FRepStateStaticBuffer& ShadowData, const FConstRepObjectDataBuffer Source) const
{
	// ShadowDataBufferSize 这个变量实在RepLayout创建的时候，在InitFromClass方法的最后赋值的，这里就是设置客户端ShadowData.Buffer的大小
	ShadowData.Buffer.SetNumZeroed(ShadowDataBufferSize);
	// 下面看下
	ConstructProperties(ShadowData);
	// 下面看下
	CopyProperties(ShadowData, Source);
}

void FRepLayout::ConstructProperties(FRepStateStaticBuffer& InShadowData) const
{
	FRepShadowDataBuffer ShadowData = InShadowData.GetData();
	for (const FRepParentCmd& Parent : Parents)
	{
		// 
		if (Parent.ArrayIndex == 0)
		{
			// 初始化ShadowData中的数据为0
			Parent.Property->InitializeValue(ShadowData + Parent);
		}
	}
}

void FRepLayout::CopyProperties(FRepStateStaticBuffer& InShadowData, const FConstRepObjectDataBuffer Source) const
{
	FRepShadowDataBuffer ShadowData = InShadowData.GetData();
	for (const FRepParentCmd& Parent : Parents)
	{
		if (Parent.ArrayIndex == 0)
		{
			// 将Source中的数据拷贝到ShadowData中
			Parent.Property->CopyCompleteValue(ShadowData + Parent, Source + Parent);
		}
	}
}
```
至此，客户端的RepState中的ReceivingRepState属性里面的StaticBuffer已经创建完了
##### 4 FObjectReplicator中InitRecentProperties方法
```cpp
// 这个是处理LifetimeCustomPropertyState，自定义属性的部分，不太知道干嘛的
```
#### 第二部分
第一部分里面我们创建除了属性同步器，第二部分我们就是初始化一些数据
```cpp
void FObjectReplicator::StartReplicating(class UActorChannel * InActorChannel)
{
	// 记录变量
	OwningChannel = InActorChannel;
	// 记录guid，用来看下Object是否被删除，删除就不同步了
	ObjectNetGUID = ConnectionNetDriver->GuidCache->GetOrAssignNetGUID( Object );
	const bool bIsValidToReplicate = !ObjectNetGUID.IsDefault() && ObjectNetGUID.IsValid();
	if (!bIsValidToReplicate)
	{
		return;
	}
	//我们正在复制的 Actor Channel 是否已被接收方打开
	bOpenAckCalled = false;
	// 创建ReplicationChangeListMgr
	// 这个里面也会创建一个FRepStateStaticBuffer，通过FRepLayout::CreateShadowBuffer方法来创建，这个是服务器用来比较属性是否改变，和上面客户端创建的流程是一样的
	ChangelistMgr = WorldNetDriver->GetReplicationChangeListMgr(Object);
}
```
至此服务器的StaticBuffer也已经创建完了，他是保存在FReplicationChangelistMgr这个结构里面。然后就是走完了同步属性需要的结构的创建部分，下面就该是真正的服务器同步的逻辑了
## 2 Server 属性同步执行 FObjectReplicator::ReplicateProperties
```cpp
int64 UActorChannel::ReplicateActor()
{
	if (!bIsNewlyReplicationPaused)
	{
		// AZURE
		bool bDoReplicateProperties = true;
		bool bUpdateNothingChangedState = true;
		// 这一大段都是我们自己加的
		{
			if (RepFlags.bNetInitial || Connection->ResendAllDataState != EResendAllDataState::None || bForceCompareProperties)
			{
				ActorReplicator->ChangedState.ResetReplicatePropertiesNothingChanged();
				bUpdateNothingChangedState = false;
			}
			else
			{
				const FSendingRepState* SendingRepState = ActorReplicator->RepState->GetSendingRepState();

				if (SendingRepState->NumNaks != 0 || (SendingRepState->PreOpenAckHistory.Num() != 0 && SendingRepState->bOpenAckedCalled))
				{
					ActorReplicator->ChangedState.ResetReplicatePropertiesNothingChanged();
					bUpdateNothingChangedState = false;
				}
				else if (Actor->PreCheckReplicatePropertiesNothingChanged((void*)&ActorReplicator->ChangedState, RepFlags.Value))
				{
					bDoReplicateProperties = false;
				}
			}
		}
		// 主要是这个方法，同步属性
		if (bDoReplicateProperties)
			WroteSomethingImportant |= ActorReplicator->ReplicateProperties(Bunch, RepFlags, bUpdateNothingChangedState, UniqueId);
	}
	// 记录了同步属性的时间
	LastUpdateTime = Connection->Driver->GetElapsedTime();
}
```
这个ReplicateActor方法就是在actorchannel里面复制actor的大方法，里面会调用ReplicateProperties来复制actor的属性。
### 1 FObjectReplicator::ReplicateProperties
```cpp
bool FObjectReplicator::ReplicateProperties(...)
{
	// 这个方法是关键方法，里面有比较属性改变的逻辑，然后记录属性改变的属性序号
	// 参数1 *RepLayout： 我们之前setActorChannel里面创建的RepLayout
	// 参数2 SendingRepState： 我们之前setActorChannel里面创建的RepState
	// 参数3 *ChangelistMgr： 在服务器上，StartReplicating方法里创建的，里面存储了StaticBuffer
	// 参数4 Object： 需要同步属性的对象UObject
	// 参数5 ReplicationFrame： 记录的帧数，上一次属性改变，需要同步的帧数
	// 参数6 RepFlags： FReplicationFlags结构体，是个union
	// 参数7 ： 是否强制比较属性，这里是false。bForceCompareProperties这个值可以在AActor::ForcePropertyCompare方法里设置
	// 这个方法执行完之后，ChangelistMgr这个里面就存储了变化的属性的索引
	FNetSerializeCB::UpdateChangelistMgr(*RepLayout, SendingRepState, *ChangelistMgr, Object, Connection->Driver->ReplicationFrame, RepFlags, OwningChannel->bForceCompareProperties || bUseCheckpointRepState);

	// 参数1 SendingRepState: 我们之前setActorChannel里面创建的RepState
	// 参数2 RepChangelistState：ReplicationChangelistMgr里面的ListState,上面的UpdateChangelistMgr这个方法会对其赋值，记录改变属性的序号
	// 参数3 Object: 需要同步属性的对象UObject
	// 参数4 ObjectClass：Object的UClass
	// 参数5 OwningChannel: 这个属性复制器所处的ActorChannel
	// 参数6 *Writer：局部变量，将属性写入这个Writer
	// 参数7 RepFlags：FReplicationFlags结构体，是个union
	// 参数8 &bNothingChangeFlag：标志位，是否没有属性改变
	// 这个方法就会把ChangelistMgr这个里面变化的属性序列化到Writer里面
	RepLayout->ReplicateProperties(SendingRepState, ChangelistMgr->GetRepChangelistState(), (uint8*)Object, ObjectClass, OwningChannel, *Writer, RepFlags, &bNothingChangeFlag);

	// 这还有同步自定义属性的，还没看过
	bool bSkippedPropertyCondition = false;
	ReplicateCustomDeltaProperties(Writer, RepFlags, bSkippedPropertyCondition);

	// 这些应该是RPC会走到，服务器再调用RPC的时候有两种情况，立即向客户端发送SendBunch或者随着Actor的下一次同步顺带的写入到Bunch里面，如果是后者那么就会将信息写到RemoteFunctions这个里面，在这里就将RemoteFunctions里面的东西写到Writer里了
	if ( RemoteFunctions != nullptr && RemoteFunctions->GetNumBits() > 0 )
	{
		Writer.SerializeBits( RemoteFunctions->GetData(), RemoteFunctions->GetNumBits() );
		RemoteFunctions->Reset();
		RemoteFuncInfo.Empty();
	}
	const bool WroteImportantData = Writer.GetNumBits() != 0;
	// 再写入属性的情况下，如果Object不是Actor，我们还需要把Object序列化到Bunch里面，然后添加Writer部分，也就是Bunch是由Object信息 和 Object属性信息两部分组成。
	if ( WroteImportantData )
	{
		OwningChannel->WriteContentBlockPayload( Object, Bunch, bHasRepLayout, Writer );
	}
	// 最终返回
	return WroteImportantData;
}

```
#### 1 FRepLayout::UpdateChangelistMgr
```cpp
ERepLayoutResult FRepLayout::UpdateChangelistMgr(...) const
{
	// 比较属性的方法
	// 参数1 ：上边ReplicateProperties方法传进来的参数2，RepState
	// 参数2 ：上边ReplicateProperties方法传进来的参数3，RepChangelistState
	// 参数3 ：上边ReplicateProperties方法传进来的参数4，需要处理的Object
	// 参数4 ：上边ReplicateProperties方法传进来的参数6，标志位
	Result = CompareProperties(RepState, &InChangelistMgr.RepChangelistState, (const uint8*)InObject, RepFlags);

	if (LIKELY(ERepLayoutResult::FatalError != Result))
	{
		// 记录一下属性改变时候的帧数
		InChangelistMgr.LastReplicationFrame = ReplicationFrame;
	}
	return result
}
```
##### FRepLayout::CompareProperties
```cpp
ERepLayoutResult FRepLayout::CompareProperties(...) const
{
	// 增加比较次数
	RepChangelistState->CompareIndex++;
	// 通过HistoryEnd（他是一个0到63的循环，如果满了就变成0了）来拿到我们这次要处理的RepChangedHistory，就是ChangeHistory数组里面的一个元素，这个元素里有Changed数组，这个数组存储的应该是Object中变化属性的的索引
	const int32 HistoryIndex = RepChangelistState->HistoryEnd % FRepChangelistState::MAX_CHANGE_HISTORY;
	FRepChangedHistory& NewHistoryItem = RepChangelistState->ChangeHistory[HistoryIndex];
	TArray<uint16>& Changed = NewHistoryItem.Changed;
	Changed.Empty(1);
	// 初始化这个返回值
	ERepLayoutResult Result = ERepLayoutResult::Success;
	// SharedParams参数
	FComparePropertiesSharedParams SharedParams{
		/*bIsInitial=*/ !!RepFlags.bNetInitial,//是不是第一次
		/*bForceFail=*/ false,
		Flags,
		Parents,
		Cmds,
		RepState,
		RepChangelistState,
		(RepState ? RepState->RepChangedPropertyTracker.Get() : nullptr),
		NetSerializeLayouts,
		.PushModelState = UE_RepLayout_Private::GetPerNetDriverState(RepChangelistState),//pushmovel状态
		.PushModelProperties = LocalPushModelProperties,
		.bValidateProperties = GbPushModelValidateProperties,
		.bIsNetworkProfilerActive = UE_RepLayout_Private::IsNetworkProfilerComparisonTrackingEnabled(),
		.bChangedNetOwner = RepState && RepState->RepFlags.bNetOwner != RepFlags.bNetOwner,
		.bForceCustomPropsActive = !!RepFlags.bClientReplay,
		.bForceCompareProperties = bForceCompare
	};
	// StackParams参数
	FComparePropertiesStackParams StackParams{
		Data,//现在需要同步的UObject
		RepChangelistState->StaticBuffer.GetData(),//UObject中原始的数据
		Changed,// 记录变化属性索引的数组,是ChangeHistory数组中的一个元素
		Result//返回值
	};
	// 通过这两个参数比较数据，也就是会赋值StackParams里面的Changed数组
	CompareParentProperties(SharedParams, StackParams);
	// 如果没有改变的，就返回Empty
	if (Changed.Num() == 0)  
	{  
	    return ERepLayoutResult::Empty;  
	}
	// 最后再添加0，一个占位？表示结束？
	Changed.Add(0);
	// HistoryEnd索引再往后挪一位
	RepChangelistState->HistoryEnd++;
	// 目前还不知道干嘛的
	RepChangelistState->SharedSerialization.Reset();
	// 如果保存的改变次数超过了ChangeHistory数组的大小，就需要合并其中的元素，第一次合并是合并0号元素和1号元素，将合并的内容放到1号元素中，这样0号元素就空了，下一次tick比较的时候我们赋值的元素就是0号元素，这样类似一个循环数组，这样循环下去。
	if ((RepChangelistState->HistoryEnd - RepChangelistState->HistoryStart) == FRepChangelistState::MAX_CHANGE_HISTORY)
	{
		const int32 FirstHistoryIndex = RepChangelistState->HistoryStart % FRepChangelistState::MAX_CHANGE_HISTORY;
		RepChangelistState->HistoryStart++;
		const int32 SecondHistoryIndex = RepChangelistState->HistoryStart % FRepChangelistState::MAX_CHANGE_HISTORY;
		TArray<uint16>& FirstChangelistRef = RepChangelistState->ChangeHistory[FirstHistoryIndex].Changed;
		TArray<uint16> SecondChangelistCopy = MoveTemp(RepChangelistState->ChangeHistory[SecondHistoryIndex].Changed);
		MergeChangeList(Data, FirstChangelistRef, SecondChangelistCopy, RepChangelistState->ChangeHistory[SecondHistoryIndex].Changed);
	}
}
```
1 这个方法还是会执行CompareParentProperties方法，继续往里执行比较属性。如果开启了pushmovel，属性如果是脏的才会进行属性比较（IsPropertyDirty这个方法）。
2 这个方法其他的部分就是对FRepChangelistState这个结构体的操作，这个机构里有ChangeHistory成员变量，其的大小是64，里面每个元素是个数组存储的是发生变化的属性序号。最后还会有ChangeHistory合并数组元素的操作。
3 比较完成后，Changed就会附上值，记录了变化属性的序号。最终会记录在RepChangelistState参数中
#### 2 FRepLayout::ReplicateProperties
```cpp
bool FRepLayout::ReplicateProperties(...) const
{
	// 赋值了LastCompareIndex
	RepState->LastCompareIndex = RepChangelistState->CompareIndex;
	// 拿到HistoryEnd指向的那个Item，HistoryEnd指向的都是有效Item的下一个
	const int32 PossibleNewHistoryIndex = RepState->HistoryEnd % FSendingRepState::MAX_CHANGE_HISTORY;
	FRepChangedHistory& PossibleNewHistoryItem = RepState->ChangeHistory[PossibleNewHistoryIndex];
	TArray<uint16>& Changed = PossibleNewHistoryItem.Changed;
	// 从上一次同步开始遍历，知道此次的HistoryEnd结束，将Changed合并起来
	for (int32 i = RepState->LastChangelistIndex; i < RepChangelistState->HistoryEnd; ++i)
	{
		const int32 HistoryIndex = i % FRepChangelistState::MAX_CHANGE_HISTORY;
		FRepChangedHistory& HistoryItem = RepChangelistState->ChangeHistory[HistoryIndex];
		TArray<uint16> Temp = MoveTemp(Changed);
		MergeChangeList(Data, HistoryItem.Changed, Temp, Changed);
	}
	// 记录上HistoryEnd指针
	RepState->LastChangelistIndex = RepChangelistState->HistoryEnd;
	// 此次有属性改变了，Changed里面有东西
	if (Changed.Num() > 0 || RepState->NumNaks > 0 || bFlushPreOpenAckHistory)
	{
		// 增加RepState的HistoryEnd
		RepState->HistoryEnd++;
		// 这个方法是把RepState里面HistoryChanged数组里的数据也放到Changed数组里面了
		UpdateChangelistHistory(RepState, ObjectClass, Data, OwningChannel->Connection, &Changed, true);
	}
	// 记录此时Writer的大小
	const int32 NumBits = Writer.GetNumBits();
	// 通过这个方法将改变的属性写入到Writer里面，然后传到外边去
	SendProperties(RepState, ChangeTracker, Data, ObjectClass, Writer, Changed, RepChangelistState->SharedSerialization);
	// 经过SendProperties方法后，判断Writer大小是否改变，如果改变了就说明了写入东西了
	onst bool bSomethingSent = NumBits != Writer.GetNumBits();
	return bSomethingSent;
}
```
##### FRepLayout::SendProperties
```cpp
void FRepLayout::SendProperties(...) const
{
	// 记录下Writer的大小
	const int32 NumBits = Writer.GetNumBits();
	// 创建两个迭代器，应该是为了遍历数组方便吧，自己写了个结构体
	FChangelistIterator ChangelistIterator(Changed, 0);
	FRepHandleIterator HandleIterator(Owner, ChangelistIterator, Cmds, BaseHandleToCmdIndex, 0, 1, 0, Cmds.Num() - 1);
	// 真正的往Writer里面写入数据的方法
	SendProperties_r(RepState, Writer, bDoChecksum, HandleIterator, Data, 0, &SharedInfo);
}
```
##### FRepLayout::SendProperties_r
```cpp
void FRepLayout::SendProperties_r(...)
{
	while (HandleIterator.NextHandle())
	{
		const FRepLayoutCmd& Cmd = Cmds[HandleIterator.CmdIndex];
		const FRepParentCmd& ParentCmd = Parents[Cmd.ParentIndex];

		if(SharedPropInfo)
		{
		}
		else
		{
			WritePropertyHandle(Writer, HandleIterator.Handle, bDoChecksum);
			const int32 NumStartBits = Writer.GetNumBits();
			// 具体的序列化属性到Writer里面，这是个虚函数需要看具体的实现，如果是那种UObject类型的属性，就会走UPackageMapClient::SerializeObject这个方法序列化，进而分配NetGUID等等操作
			Cmd.Property->NetSerializeItem(Writer, Writer.PackageMap, const_cast<uint8*>(Data.Data));
			const int32 NumEndBits = Writer.GetNumBits();
		}
		
	}
}

```
## 3 Client 属性同步接收 UIpNetDriver::TickDispatch

下面这个是我跑ue5的堆栈
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/20240703220132.png)
### 1 Client UActorChannel::ProcessBunch
我们直接从ProcessBunch开始，前边就是和同步actor流程一样，这里我们主要看同步属性
```cpp
/*
	第一部分：这边就是判断这个actorChannel里面的actor在客户端是否创建，如果没有创建就通过PackageMap序列化一个actor出来，然后调用SetChannelActor关联起来（这里面肯定有RepLyout相关东西的创建），然后就是NotifyActorChannelOpen发送通知actorChannel已经创建了。
	第二部分：一个循环遍历，遍历Bunch里面的内容，看上去就是遍历actor的属性和身上的subobject对象（也就是owner为 当前actorChannel里actor 的subobject对象），RepObj变量就是当前Actor或者是属性Object或者是subobject，然后也会创建其Replicator，进而保存到actorChannel里的ReplicationMap这个map里面。创建Replicator完成后会通过ReceivedBunch方法进行属性比较，如果改变了就会将属性保存到RepNotifies这个数组里面，然后如果无法序列化出Object的话，会将其Replicator添加到UnmappedReplicators数组里面。
	第三部分：遍历actorChannel里的ReplicationMap这个map，一次调用PostReceivedBunch方法，里面有属性同步的部分，后面我们详细看下这个方法。
	第四部分：如果actor是第一次创建，就调用其PostNetInit方法。
*/
void UActorChannel::ProcessBunch( FInBunch & Bunch )
{
	//第一部分
	if( Actor == NULL )
	{
		AActor* NewChannelActor = NULL;
		bSpawnedNewActor = Connection->PackageMap->SerializeNewActor(Bunch, this, NewChannelActor);
		SetChannelActor(NewChannelActor, Flags);
		NotifyActorChannelOpen(Actor, Bunch);
		RepFlags.bNetInitial = true;
	}
	//第二部分
	while ( !Bunch.AtEnd() && Connection != NULL && Connection->GetConnectionState() != USOCK_Closed )
	{
		// 这个会读取UObject，如果是Actor就直接返回这个ActorChannel里的Actor，如果是Object就会根据Bunch反序列化出来一个Object
		UObject* RepObj = ReadContentBlockPayload( Bunch, Reader, bHasRepLayout );
		// 创建Object的属性复制器，也就是再客户端创建属性同步需要的那些对象
		TSharedRef< FObjectReplicator > & Replicator = FindOrCreateReplicator( RepObj );
		// 在这个ObjectReplicator::RecivedBunch方法里面,如果客户端通过SerializeObject无法序列化出Object，这个标志位就为false
		bool bHasUnmapped = false;
		// 这个里面是客户端比较属性是否改变，改变了的会存到RepNotifies数组里面
		if ( !Replicator->ReceivedBunch( Reader, RepFlags, bHasRepLayout, bHasUnmapped ) )
		{
			// 有问题的情况才会走进来
			Connection->Close();
			return;
		}
		// 如果客户端没序列化出来，就会把Replicator添加到UnmappedReplicators数组里面
		if ( bHasUnmapped )
		{
			// 这个数组是会在TickFlush里面执行的
			Connection->Driver->UnmappedReplicators.Add( &Replicator.Get() );
		}
	}
	//第三部分
	for (auto RepComp = ReplicationMap.CreateIterator(); RepComp; ++RepComp)
	{
		TSharedRef<FObjectReplicator>& ObjectReplicator = RepComp.Value();
		// 这个方法里会调用CallRepNotifies方法，也就是客户端的onrep
		ObjectReplicator->PostReceivedBunch();
	}
	// 第四部分
	if (Actor && bSpawnedNewActor)
	{
		Actor->PostNetInit();
	}
}
```
#### 第二部分 ObjectReplicator::ReceivedBunch
```cpp
UActorChannel::FindOrCreateReplicator(...)
 ->UNetConnection::CreateReplicatorForNewActorChannel(...)
  ->FObjectReplicator::InitWithObject(...)
   ->FObjectReplicator::InitRecentProperties(...)
    ->FRepLayout::CreateRepState(...)// 这个里有客户端的staticbuffer的创建
 ->FObjectReplicator::StartReplicating(...)

FObjectReplicator::ReceivedBunch(...)
 ->FRepLayout::ReceiveProperties(...)
  ->FRepLayout::ReceiveProperties_BackwardsCompatible(...)
   ->FRepLayout::ReceiveProperties_BackwardsCompatible_r(...)
    ->static bool ReceivePropertyHelper(...)
```
##### 1 ActorChannel::ReadContentBlockHeader
```cpp

```
##### 2 FObjectReplicator::ReceivedBunch
```cpp
// 
bool FObjectReplicator::ReceivedBunch(FNetBitReader& Bunch, const FReplicationFlags& RepFlags, const bool bHasRepLayout, bool& bOutHasUnmapped)
{
	const bool bIsServer = ConnectionNetDriver->IsServer();
	const bool bCanDelayRPCs = (CVarDelayUnmappedRPCs.GetValueOnGameThread() > 0) && !bIsServer;
	// 如果有RepLayout，应该是Object中有Replicate标记的属性这个就为true吧
	if (bHasRepLayout)
	{
		bool bLocalHasUnmapped = false;
		// 参数1：OwningChannel -> 这个就是客户端所处的ActorChannel
		// 参数2：ObjectClass -> 客户端处理的Object的UClass
		// 参数3：客户端的ReceivingRepState
		// 参数4：现在处理的Object
		// 参数5：传进来的Bunch
		// 参数6：bLocalHasUnmapped标志位
		// 参数7：bGuidsChanged标志位
		// 参数8：ReceivePropFlags标志位
		if (!LocalRepLayout.ReceiveProperties(OwningChannel, ObjectClass, RepState->GetReceivingRepState(), Object, Bunch, bLocalHasUnmapped, bGuidsChanged, ReceivePropFlags))
		{
			return false;
		}
		bOutHasUnmapped |= bLocalHasUnmapped;
	}
	while (true)
	{
		if (FStructProperty* ReplicatedProp = CastField<FStructProperty>(FieldCache->Field.ToField()))
		{
			// 这里处理了Struct的同步
		}
		else if ( Cast< UFunction >( FieldCache->Field.ToUObject() ) )
		{
			// 这里处理了RPC的同步
			bool bDelayFunction = false;
			TSet<FNetworkGUID> UnmappedGuids;
			// Reader就是传过来的数据，RepFlags是标志位，FieldCache是UFunction的FieldCache,bCanDelayRPCs是false，bDelayFunction是引用要传出来的，UnmappedGuids这个是记录未序列化成功的NetGUID
			bool bSuccess = ReceivedRPC(Reader, RepFlags, FieldCache, bCanDelayRPCs, bDelayFunction, UnmappedGuids);
			// 如果不成功
			if (!bSuccess)
			{
				return false;
			}
			// 如果是延迟调用RPC函数的情况
			else if (bDelayFunction)
			{
				// 放入到 PendingLocalRPCs 数组里面
				PendingLocalRPCs.Emplace(FieldCache, RepFlags, Reader, UnmappedGuids);
				bOutHasUnmapped = true;
				bGuidsChanged = true;
				bForceUpdateUnmapped = true;
			}
			else if (Object == nullptr || Object->IsPendingKill())
			{
				return true;
			}
		}
		else
		{
			return false;
		}
	}
}
```
##### 3 FRepLayout::ReceiveProperties
```cpp
bool FRepLayout::ReceiveProperties(...)
{
	bOutHasUnmapped = false;
	FReceivePropertiesSharedParams Params{
		bDoChecksum,
		EnumHasAnyFlags(ReceiveFlags, EReceivePropertiesFlags::SkipRoleSwap) || !EnumHasAnyFlags(Flags, ERepLayoutFlags::IsActor),
		InBunch,// 我们传进来的Bunch
		bOutHasUnmapped,// 引用，需要传出去的标志位
		bOutGuidsChanged,// 引用，需要传出去的
		Parents,// ObjectReplicator 中的Parents属性组
		Cmds,// ObjectReplicator 中的Cmds属性组
		NetSerializeLayouts,
		Object,// 传进来的Object
		OwningChannel->Connection->GetInTraceCollector()
	};
	FReceivePropertiesStackParams StackParams{
		FRepObjectDataBuffer(Data),// 此次同步Object数据
		FRepShadowDataBuffer(RepState->StaticBuffer.GetData()),// 原有的Object数据
		&RepState->GuidReferencesMap,// Object的NetGUID映射数组吧
		0,// CmdStart
		Cmds.Num() - 1,// CmdEnd
		bEnableRepNotifies ? &RepState->RepNotifies : nullptr//是否有属性通知
	};
	// 进一步执行ReceiveProperties_r方法
	if (ReceiveProperties_r(Params, StackParams))  
	{
		// 返回true，意思是比较属性完成，没出现问题
		return true;
	}
	return false;
}
```
###### 1 ReceiveProperties_r
```cpp
static bool ReceiveProperties_r(FReceivePropertiesSharedParams& Params, FReceivePropertiesStackParams& StackParams)
{
	// 遍历Cmd数组
	for (int32 CmdIndex = StackParams.CmdStart; CmdIndex < StackParams.CmdEnd; ++CmdIndex)
	{
		// 找到具体的属性
		const FRepLayoutCmd& Cmd = Params.Cmds[CmdIndex];
		// 增加计数
		++StackParams.CurrentHandle;
		// 这个就是进一步执行的方法，里面会有比较属性的部分，如果我们有未映射的NetGUID，ReceivePropertyHelper 这个方法就会返回true，进而将bOutHasUnmapped标记未true。
		if (ReceivePropertyHelper(
			Params.Bunch,
			StackParams.GuidReferences,
			StackParams.ArrayElementOffset,
			StackParams.ShadowData,
			StackParams.ObjectData,
			StackParams.RepNotifies,
			Params.Parents,
			Params.Cmds,
			CmdIndex,
			Params.bDoChecksum,
			Params.bOutGuidsChanged,
			Params.bSkipRoleSwap,
			Params.NetSerializeLayouts,
			Params.OwningObject))
		{
			// 如果客户端通过SerializeObject方法没能序列化出Object，就会走进来，赋值bOutHasUnmapped
			Params.bOutHasUnmapped = true;
		}
		if (Params.Bunch.IsError())
		{
			// 如果有问题，才会返回false
			return false;
		}
		// Read the next property handle to serialize.
		// If we don't have any more properties, this could be a terminator.
		ReadPropertyHandle(Params);
	}
	// 正常就会返回true，比较完成
	return true;
}
```
###### 2 ReceivePropertyHelper
```cpp
static bool ReceivePropertyHelper(...)
{
	// 拿到我们的属性
	const FRepLayoutCmd& Cmd = Cmds[CmdIndex];  
	const FRepParentCmd& Parent = Parents[Cmd.ParentIndex];
	// 如果 GuidReferencesMap 这个数组存在，开启TrackedGUID
	if (GuidReferencesMap)
	{
		Bunch.PackageMap->ResetTrackedGuids(true);
	}
	// 如果有OnRep方法
	if (RepNotifies != nullptr && INDEX_NONE != Parent.RepNotifyNumParams)
	{
		// Data里的数据是客户端存在的值，把这个值赋给RepState->StaticBuffer。也就是赋值给ShadowData
		StoreProperty(Cmd, ShadowData + Cmd, Data + SwappedCmd);
		// 序列化属性，也就是从Bunch里面拿出数据赋值给Data
		Cmd.Property->NetSerializeItem(Bunch, Bunch.PackageMap, Data + SwappedCmd);
		// 具体检测属性是否改变的方法，也就是比较Data和ShadowData数据是否一样。如果改变了并有Onrep方法，就添加到RepNotifies里面
		if (Parent.RepNotifyCondition == REPNOTIFY_Always || !PropertiesAreIdentical(Cmd, ShadowData + Cmd, Data + SwappedCmd, NetSerializeLayouts))
		{
			// 具体的添加到RepNotifies数组里
			RepNotifies->AddUnique(Parent.Property);
		}
	}
	// 如果没有OnRep方法，就直接序列化出属性就行了
	else
	{
		Cmd.Property->NetSerializeItem(Bunch, Bunch.PackageMap, Data + SwappedCmd);
	}
	// 有没有这个数组，应该肯定是有的
	if (GuidReferencesMap)
	{
		// 没有映射的GUID数量是否大于0，这个TrackedUnmappedGuids数组是在PackageMapClient::SerializeObject里面，如果客户端找不到Object就会添加到数组里
		const bool bHasUnmapped = TrackedUnmappedGuids.Num()> 0;
		// 这个if else的作用，看上去就是把 TrackedUnmappedGuids 里面的数据放到GuidReferencesMap这个里面，然后清空 TrackedUnmappedGuids 数组
		if (TrackedUnmappedGuids.Num() > 0 || TrackedDynamicMappedGuids.Num()> 0)
		{
			if (GuidReferences != nullptr)
			{
				if (!NetworkGuidSetsAreSame(GuidReferences->UnmappedGUIDs, TrackedUnmappedGuids))
				{
					bOutGuidsChanged = true;
				}
				else if (!NetworkGuidSetsAreSame(GuidReferences->MappedDynamicGUIDs, TrackedDynamicMappedGuids))
				{
					bOutGuidsChanged = true;
				}
			}
			if (GuidReferences == nullptr || bOutGuidsChanged)
			{
				GuidReferencesMap->Add(AbsOffset, FGuidReferences(Bunch, Mark, TrackedUnmappedGuids, TrackedDynamicMappedGuids, Cmd.ParentIndex, CmdIndex));
				bOutGuidsChanged = true;
			}
		}
		else
		{
			if (GuidReferences != nullptr)
			{
				GuidReferencesMap->Remove(AbsOffset);
				bOutGuidsChanged = true;
			}
		}
		// 清掉TrackedUnmappedGuids数组
		Bunch.PackageMap->ResetTrackedGuids(false);
		// 如果 TrackedUnmappedGuids 这个里面有值，就返回true
		return bHasUnmapped;
	}
	return false
}
```
##### 4 FObjectReplicator::ReceivedRPC
```cpp
// 同步RPC属性相关
bool FObjectReplicator::ReceivedRPC(...)
{
	// 是否是服务器端
	const bool bIsServer = Connection->Driver->IsServer();
	// 获取属性复制器中的Object
	UObject* Object = GetObject();
	// 找到RPC的方法名称
	FName FunctionName = FieldCache->Field.GetFName();
	// 找到RPC方法的UFunction
	UFunction* Function = Object->FindFunction(FunctionName);
	// (!IsServer() || RepFlags.bNetOwner) 这是 ShouldCallRemoteFunction 这个方法的判断，客户端bCanExecute这个标志位肯定是true了
	const bool bCanExecute = Connection->Driver->ShouldCallRemoteFunction(Object, Function, RepFlags);
	if (bCanExecute)  
	{
		// bCanDelayRPC是false，所以bCanDelayUnmapped也是false。bCanDelayRPC这个是参数传过来的，是net.DelayUnmappedRPCs这个命令控制的。如果这个变量为true，客户端才有可能延迟求解RPC函数
		bool bCanDelayUnmapped = bCanDelayRPC && Function->FunctionFlags & FUNC_NetReliable;
		// 先获取RPC方法的RepLayout
		TSharedPtr<FRepLayout> FuncRepLayout = Connection->Driver->GetFunctionRepLayout(LayoutFunction);
		// 客户端接收传过来的数据，
		FuncRepLayout->ReceivePropertiesForRPC(Object, LayoutFunction, OwningChannel, Reader, Parms, UnmappedGuids);
		if (bCanDelayUnmapped && (UnmappedGuids.Num() > 0 || PendingLocalRPCs.Num() > 0))
		{
			bOutDelayRPC = true;	
		}
		// 这个是我们自己加的，bOutDelayRPC这个标志位的意思就是客户端收到RPCBunch后不立即调用RPC方法
		else if(OwningChannel && OwningChannel->bDeferredRepNotify)
		{
			bOutDelayRPC = true;
		}
		else
		{
			// ActorChannel里面的Actor
			AActor* OwningActor = OwningChannel->Actor;
			// 这个ShouldForwardFunction方法，是个虚函数，如果是客户端返回true
			if (Connection->Driver->ShouldForwardFunction(OwningActor, Function, Parms))
			{
				// World上下文
				FWorldContext* const Context = GEngine->GetWorldContextFromWorld(Connection->Driver->GetWorld());
				if (Context != nullptr)
				{
					// 是子物体么
					UObject* const SubObject = Object != OwningChannel->Actor ? Object : nullptr;
					for (FNamedNetDriver& Driver : Context->ActiveNetDrivers)
					{
						// 看这个判断Driver.NetDriver != Connection->Driver，是说当前world中的NetDrive里面有不是Connection里的Driver么，如果有的话就调用一次ProcessRemoteFunction方法
						if (Driver.NetDriver != nullptr && (Driver.NetDriver != Connection->Driver) && Driver.NetDriver->ShouldReplicateFunction(OwningActor, Function))
						{
							Driver.NetDriver->ProcessRemoteFunction(OwningActor, Function, Parms, nullptr, nullptr, SubObject);
						}
					}
				}
			}
			// 客户端调用RPC方法
			Object->ProcessEvent(Function, Parms);
		}
	}
	return true;
}
```
#### 第三部分 FObjectReplicator::PostReceivedBunch
```cpp
void FObjectReplicator::PostReceivedBunch()
{
	// Call PostNetReceive
	const bool bIsServer = (OwningChannel->Connection->Driver->ServerConnection == nullptr);
	if (!bIsServer && bHasReplicatedProperties)
	{
		PostNetReceive();
		bHasReplicatedProperties = false;
	}
	// Call RepNotifies 这个方法就是调用onrep的方法，通过上面存储的RepNotifies数组找到改变的属性，然后通过属性找到onrep方法，然后再通过ProcessEvent执行这个方法
	CallRepNotifies(true);
}
```
## 4 Client 属性同步延迟求解 UNetDriver::TickFlush
```cpp
void UNetDriver::TickFlush(float DeltaSeconds)
{
	
	if( ServerConnection )
	{
		// Queue client voice packets in the server's voice channel
		ProcessLocalClientPackets();
		// 这个会走UIpConnection::Tick，但里边不是很关心的逻辑吧，然后会走到父类的UNetConnection::Tick方法，下面看下这个方法
		ServerConnection->Tick(DeltaSeconds);
	}
	if ( CVarOptimizedRemapping.GetValueOnAnyThread() && GuidCache.IsValid() )
	{
		SpawnedNewActorFrameCount = 0;
		/*
		ImportedNetGuids 这个数组的添加有这么几个地方：
		1 UActorChannel::ReadContentBlockHeader方法里面，如果是动态的NetGUID，是客户端自己创建出来的SubObject，就会将NetGUID添加到 ImportedNetGuids 数组中，客户端添加
		2 UPackageMapClient::SerializeNewActor方法里面，就是在序列化NewActor的时候也会将NetGUID 添加到 ImportedNetGuids 数组中，客户端服务器都添加
		3 UPackageMapClient::InternalLoadObject方法里面，如果是ExportingNetGUIDBunch部分，也会将NetGUID 添加到 ImportedNetGuids 方法里面，客户端添加
		ImportedNetGuids 这个数组的含义应该就是，此次tick收到的Bunch中，客户端已经解析的NetGUID
		*/
		TSet< FNetworkGUID >& ImportedNetGuids = GuidCache->ImportedNetGuids;
		TMap< FNetworkGUID, TSet< FNetworkGUID > >& PendingOuterNetGuids = GuidCache->PendingOuterNetGuids;
		TSet< FObjectReplicator* > ForceUpdateReplicators;
		// UnmappedReplicators 这里数组里面存的是，此次Tick客户端中没能序列化出来的属性Object对应的ObjectReplicator，是在ProcessBunch里面添加的
		for (FObjectReplicator* Replicator : UnmappedReplicators)
		{
			// 这个是ReveivedRPC的时候赋值的，客户端在FObjectReplicator::ReceivedBunch的时候也会ReceivedRPC，如果不是立即调用RPC函数的话，就会设置 bForceUpdateUnmapped 标志位为true
			if (Replicator->bForceUpdateUnmapped)
			{
				Replicator->bForceUpdateUnmapped = false;
				ForceUpdateReplicators.Add(Replicator);
			}
		}
		// ImportedNetGuids 数组里存的就是此次Tick，客户端新创建出来的Object的NetGUID
		// ForceUpdateReplicators 数组里存的是在TickDispatch里面没找见对应Object的属性中RPC的Replicator
		if ( ImportedNetGuids.Num() || ForceUpdateReplicators.Num() )
		{
			int32 NumRemapTests = 0;
			// 服务器是0，客户端是100，意思是客户端一帧最多遍历ImportedNetGuids数组100次
			int32 MaxRemaps = IsServer() ? 0 : CVarMaxClientGuidRemaps.GetValueOnAnyThread();
			// 这个默认是true
			bool bFilterGuidRemapping = (CVarFilterGuidRemapping.GetValueOnAnyThread() > 0);
			
			TArray< FNetworkGUID > UnmappedGuids;
			TArray< FNetworkGUID > NewlyMappedGuids;
			for ( auto It = ImportedNetGuids.CreateIterator(); It; ++It )  
			{
				// 当前的NetGUID
				const FNetworkGUID NetworkGuid = *It;
				// 当前NetGUID是不是坏的或者是在客户但已经找到映射了
				bool bMappedOrBroken = false;
				if ( GuidCache->GetObjectFromNetGUID( NetworkGuid, false ) != nullptr )
				{
					// 如果客户端找到映射了，就进一步添加到NewlyMappedGuids数组里面
					NewlyMappedGuids.Add( NetworkGuid );
					bMappedOrBroken = true;
				}
				if ( GuidCache->IsGUIDBroken( NetworkGuid, false ) )
				{
					bMappedOrBroken = true;
				}
				// 将当前NetGUID从ImportedNetGuids数组里移除掉
				It.RemoveCurrent();
				// 如果没有映射 或者 NetGUID也不是Broken的
				if (!bMappedOrBroken)
				{
					// 当前NetGUID的OuterGUID
					FNetworkGUID OuterGUID = GuidCache->GetOuterNetGUID(NetworkGuid);
					// we're missing the outer, stop checking until we map it
					if (bFilterGuidRemapping && OuterGUID.IsValid() && !OuterGUID.IsDefault() && !GuidCache->IsGUIDLoaded(OuterGUID) && !GuidCache->IsGUIDPending(OuterGUID))
					{
						TSet<FNetworkGUID>& PendingGuids = PendingOuterNetGuids.FindOrAdd(OuterGUID);
						PendingGuids.Add(NetworkGuid);
					}
					else
					{
						if (ensure(NetworkGuid.IsValid()))
						{
							UnmappedGuids.Add(NetworkGuid);
						}
					}
				}
				// 这就是个计数器，限制这个for循环的次数，客户端是100次
				++NumRemapTests;
 				if ((MaxRemaps > 0) && (NumRemapTests >= MaxRemaps))
				{
					break;
				}
			}
			// 遍历下NewlyMappedGuids这一帧新映射出来的NetGUID，如果这一帧新的NetGUID在PendingOuterNetGuids这个数组里面，就把NetGUID放到ImportedNetGuids里面，在下一帧会处理ImportedNetGuids数组
			for ( const FNetworkGUID& NetGuid : NewlyMappedGuids )
			{
				TSet< FNetworkGUID >* DependentGuids = PendingOuterNetGuids.Find( NetGuid );
				if ( DependentGuids )
				{
					ImportedNetGuids.Append( *DependentGuids );
					PendingOuterNetGuids.Remove( NetGuid );
				}
			}
			// 如果还有没有映射的NetGUID，就放到ImportedNetGuids数组里，下一帧继续处理
			if (UnmappedGuids.Num())
			{
				ImportedNetGuids.CompactStable();
				ImportedNetGuids.Append(UnmappedGuids);
			}
			// 这一帧有新Map的NetGUID，或者是有RPC的需要处理。这一大段的作用就是拿到这一帧所有需要处理的Replicator，判断在UnmappedReplicators数组中是否存在，如果存在就调用UpdateUnmappedObjects方法处理
			if ( NewlyMappedGuids.Num() || ForceUpdateReplicators.Num() )
			{
				TSet< FObjectReplicator* > AllReplicators = ForceUpdateReplicators;
				for ( const FNetworkGUID& NetGuid : NewlyMappedGuids )
				{
					TSet< FObjectReplicator* >* Replicators = GuidToReplicatorMap.Find( NetGuid );

					if ( Replicators )
					{
						AllReplicators.Append( *Replicators );
					}
				}
				// 关键的是这里执行UpdateUnmappedObjects方法。这部分就是在客户端遍历这一帧能够解析出Object的Replicator，如果Replicator是在UnMappedReplicators数组里的，就调用UpdateUnmappedObjects，之后移除UnmappedReplicators数组
				for ( FObjectReplicator* Replicator : AllReplicators )
				{
					if ( UnmappedReplicators.Contains( Replicator ) )
					{
						bool bHasMoreUnmapped = false;
						
						Replicator->UpdateUnmappedObjects( bHasMoreUnmapped );
						if ( !bHasMoreUnmapped )
						{
							UnmappedReplicators.Remove( Replicator );
						}
					}
				}
			}
		}
	}
}
```
### 1 UNetConnection::Tick
```cpp
void UNetConnection::Tick(float DeltaSeconds)
{
	if (CVarTickAllOpenChannels.GetValueOnAnyThread() == 0)
	{
		for (int32 i = ChannelsToTick.Num() - 1; i >= 0; i--)
		{
			// 处理Channel的Tick
			ChannelsToTick[i]->Tick();

			if (ChannelsToTick[i]->CanStopTicking())
			{
				ChannelsToTick.RemoveAt(i);
			}
		}
	}
}
// ActorChannel的Tick里就是执行ProcessQueuedBunches方法
void UActorChannel::Tick()
{
	Super::Tick();
	ProcessQueuedBunches();
}
```
#### UActorChannel::ProcessQueuedBunches

```cpp
/*
本来是收到Bunch就会解析这个Bunch，但是如果是无法解析的情况就会将Bunch加入QueueBunch里面，然后再收到所有的Bunch后，会执行TickFlush方法，进而执行ProcessBunch方法来处理。
*/
bool UActorChannel::ProcessQueuedBunches()  
{
	// PendingGuidResolves这个数组里存的就是因为异步加载客户端没加载成功的NetGUID
	// QueuedBunches这个数组里存的就是因为异步加载客户端没加载成功的NetGUID所在的Bunch以及一些其他情况，比如我们项目就是直接将所有Bunch都放到了QueuedBunch里
	if (PendingGuidResolves.Num() == 0 && QueuedBunches.Num() == 0)
	{
		return true;
	}
	// 这个for循环就是处理PendingGuidResolves数组，如果数组中的NetGUID已经求解出来了或者NetGUID坏了 就移除数组
	for (auto It = PendingGuidResolves.CreateIterator(); It; ++It)
	{
		if (Connection->Driver->GuidCache->GetObjectFromNetGUID(*It, true))
		{
			It.RemoveCurrent();
		}
		else if (Connection->Driver->GuidCache->IsGUIDBroken(*It, true))
		{
			It.RemoveCurrent();
		}
	}
	// 满足处理的事件，引擎设置的，认为为true就行
	if (bHasTimeToProcess)
	{
		// 只有在满足以下条件的时候，才会执行if的内容
		/*
			1.PendingGuidResolves数组为空，也就是未求解出Object的NetGUID已经都求解出了
			2.当前ActorChannel销毁了还有没处理的Bunch
			3.当前帧处理QueueBunch花费的时间不多，多不多是配置的
		*/
		if (PendingGuidResolves.Num() == 0
			&& (ChIndex == -1 || !Connection->KeepProcessingActorChannelBunchesMap.Contains(ActorNetGUID))
			&& !Connection->Driver->ShouldQueueBunchesForActorGUID(ActorNetGUID))
		{
			int32 BunchIndex = 0;
			while ((BunchIndex < QueuedBunches.Num()))
			{
				FInBunch* QueuedInBunch = QueuedBunches[BunchIndex];
				// 处理QueuedBunch里面的Bunch，就用ProcessBunch方法
				ProcessBunch(*QueuedInBunch);
				delete QueuedInBunch;
				QueuedBunches[BunchIndex] = nullptr;

				++BunchIndex;
			}
			// 清空QueuedBunches数组
			QueuedBunches.RemoveAt(0, BunchIndex);
			// 这里调用客户端的OnRep函数
			for (auto& ReplicatorPair : ReplicationMap)
			{
				ReplicatorPair.Value->CallRepNotifies(true);
			}
		}
	}
	return QueuedBunches.Num() == 0;
}
```
# 4 Actor子物体同步

## 1 Server 子物体同步 UAActor::ReplicateSubobjects
```cpp
int64 UActorChannel::ReplicateActor()
{
	int64 UActorChannel::ReplicateActor()
	{
		if (!bIsNewlyReplicationPaused)
		{
			// 这个里面是属性同步，上边看过了
		}
		// 这个方法就是子物体同步的部分
		Actor->ReplicateSubobjects(this, &Bunch, &RepFlags);
		// 记录了同步属性的时间
		LastUpdateTime = Connection->Driver->GetElapsedTime();
	}
}

bool AActor::ReplicateSubobjects(UActorChannel *Channel, FOutBunch *Bunch, FReplicationFlags *RepFlags)
{
	bool WroteSomething = false;
	for (UActorComponent* ActorComp : ReplicatedComponents)
	{
		if (ActorComp && ActorComp->GetIsReplicated())
		{
			// 先写入Actor身上Component的SubObject
			// 这个里面就是Component自己处理了，需要重写ReplicateSubobjects方法，将Component里的SubObject写入到Bunch里
			WroteSomething |= ActorComp->ReplicateSubobjects(Channel, Bunch, RepFlags);
			// 然后再写入Actor身上Component
			WroteSomething |= Channel->ReplicateSubobject(ActorComp, *Bunch, *RepFlags);	
		}
	}
	return WroteSomething;
}
```
再同步Actor的子物体的时候，也就是同步Actor身上挂着的Component的时候，需要先同步Component里面需要同步的子物体，然后再同步Component。
### 1 UActorChannel::ReplicateSubobject
```cpp
bool UActorChannel::ReplicateSubobject(...)
{
	// 这个obj就是我们的子物体，Subobject
	TWeakObjectPtr<UObject> WeakObj(Obj);
	// 这部分是注册Obj的NetGUID,注掉是因为我们改过这段的逻辑，我们把注册NetGUID放到了ObjectReplicator的创建里面了，也就是放在了FindOrCreateReplicator这个方法里面
	//if ( !Connection->Driver->GuidCache->SupportsObject( Obj, &WeakObj ) )
	//{
	//	Connection->Driver->GuidCache->AssignNewNetGUID_Server( Obj );
	//}

	// 下面这部分就是创建子物体的ObjectReplicator，这个流程上边也分析过了
	bool NewSubobject = false;
	bool bCreatedReplicator = false;
	TSharedRef<FObjectReplicator>& ObjectReplicator = FindOrCreateReplicator(Obj, &bCreatedReplicator, &WeakObj);
	FObjectReplicator& ObjectReplicatorRef = ObjectReplicator.Get();
	if (bCreatedReplicator)
	{
		Bunch.bReliable = true;
		NewSubobject = true;
	}
	// 下面这部分是子物体属性同步的部分，这个流程上边也分析过了
	bool WroteSomething = ObjectReplicatorRef.ReplicateProperties(Bunch, RepFlags, bUpdateNothingChangedState, UniqueId);
	// 第一次同步subobject && 属性没有改变
	if (NewSubobject && !WroteSomething)
	{
		// Write empty payload to force object creation
		FNetBitWriter EmptyPayload;
		// 下面我们来看下这个方法
		WriteContentBlockPayload( Obj, Bunch, false, EmptyPayload );
		WroteSomething= true;
	}

}
```
#### 1 UActorChannel::WriteContentBlockPayload
```cpp
int32 UActorChannel::WriteContentBlockPayload( UObject* Obj, FNetBitWriter &Bunch, const bool bHasRepLayout, FNetBitWriter& Payload )
{
	// Bunch起始位置
	const int32 StartHeaderBits = Bunch.GetNumBits();
	// 往Bunch里写入数据，是将Obj序列化到Bunch里面
	WriteContentBlockHeader( Obj, Bunch, bHasRepLayout );
	// Payload这个Bunch的大小
	uint32 NumPayloadBits = Payload.GetNumBits();
	// 感觉是为Bunch分配NumPayloadBits这么大的空间?感觉好像也不是这样的
	Bunch.SerializeIntPacked( NumPayloadBits );
	// 返回值，返回的是经过这个方法后Bunch增加的大小
	const int32 HeaderNumBits = Bunch.GetNumBits() - StartHeaderBits;
	// 把Payload里的数据写到Bunch里,最终Bunch里面就会有Obj的数据 + Payload的数据
	Bunch.SerializeBits( Payload.GetData(), Payload.GetNumBits() );
	
	return HeaderNumBits;
}
/*
这个方法就分成了两部分，第一部分就是将Obj序列化到Bunch里面，第二部分就是将Payload数据也写到Bunch里面，所以经过这个方法过后，Bunch里面的数据就有Obj+Payload了。
*/
```
#### 2 UActorChannel::WriteContentBlockHeader UActorChannel::ReadContentBlockHeader
```cpp
// 服务器是走这个方法，写入数据
// Obj就是我们的SubObject，也就是component
// Bunch就是发送的Bunch
// bHasRepLayout应该是属性变化那，这里是false
void UActorChannel::WriteContentBlockHeader( UObject* Obj, FNetBitWriter &Bunch, const bool bHasRepLayout )
{
	// 1 写入bHasRepLayout标志位
	Bunch.WriteBit( bHasRepLayout ? 1 : 0 );
	// 2 写入IsActor标志位，如果是actorChannel里面的actor就不需要发送了
	const bool IsActor = Obj == Actor;
	Bunch.WriteBit( IsActor ? 1 : 0 );
	if ( IsActor )
	{
		return;
	}
	// 3 Bunch重写了Operator<<方法，具体执行的是这个方法
	// PackageMap->SerializeObject(*this, UObject::StaticClass(), Object);
	Bunch << Obj;
	// 4 在服务器端
	if ( Connection->Driver->IsServer() )
	{
		// 如果这个obj是网络可靠命名，就写入1
		// 这个网络稳定是个设置，后面看下这部分。
		/*
		满足以下条件的组件即拥有网络可靠命名：
		● 从数据包直接加载
		● 通过construction scripts脚本添加
		● 采用手动标记（通过 UActorComponent::SetNetAddressable 设置）蓝图中挂的component会默认SetNetAddressable
		● 只有当您知道要手动命名组件以便其在服务器和客户端上具有相同名称时，才应当使用这种方法（最好的例子就是 AActor 在C++ 构造函数中添加的组件）
		*/
		if ( Obj->IsNameStableForNetworking() )
		{
			Bunch.WriteBit( 1 );
		}
		else
		// 如果没有就会写入0，然后将Obj的UClass写入
		{
			Bunch.WriteBit( 0 );
			UClass *ObjClass = Obj->GetClass();
			Bunch << ObjClass;
		}
	}
}
// 客户端是走这个方法，读数据。和上面的WriteContentBlockHeader一一对应
// Bunch就是客户端收到的这个Bunch
// bObjectDeleted能够传出去的标志位
// bOutHasRepLayout能够传出去的标志位
UObject* UActorChannel::ReadContentBlockHeader( FInBunch & Bunch, bool& bObjectDeleted, bool& bOutHasRepLayout )
{
	// 1 读取bHasRepLayout
	bOutHasRepLayout = Bunch.ReadBit() != 0 ? true : false;
	// 2 读取是否是Actor
	const bool bIsActor = Bunch.ReadBit() != 0 ? true : false; 
	if ( bIsActor )
	{
		return Actor;
	}
	// 3 客户端序列化出来SubObj
	/*
	a. 这个方法会调用InternalLoadObject来序列化，这个里面就会判断bHasPath这个标志位，如果标志位为true，才会序列化出SubObj，还记得bHasPath这个标志位哪里设置的么？
	b. 是在InternalWriteObject这个方法里面，ExportFlags.bHasPath = ShouldSendFullPath( Object, NetGUID ) ? 1 : 0 这里设置的
	c. 也就是说那种静态的，网络名称稳定的，客户端这才能直接序列化出SubObj，而那种动态的，也就是运行时添加的，是无法直接序列化出SubObj的。这也就解释了后面还会序列出SubObjClassObj来生成SubObj。 
	*/
	FNetworkGUID NetGUID;
	UObject* SubObj = NULL;
	Connection->PackageMap->SerializeObject( Bunch, UObject::StaticClass(), SubObj, &NetGUID );
	// 4 是否是网络名称稳定
	const bool bStablyNamed = Bunch.ReadBit() != 0 ? true : false;
	// 如果是网络名称稳定的，就直接返回SubObj，客户端不需要创建
	if ( bStablyNamed )
	{
		return SubObj;
	}
	// 在服务器上obj如果不是网络名称稳定的，就需要在Bunch里写入UClass
	// 在这里也就是客户端就需要从Bunch读取UClass
	FNetworkGUID ClassNetGUID;
	UObject* SubObjClassObj = NULL;
	Connection->PackageMap->SerializeObject( Bunch, UObject::StaticClass(), SubObjClassObj, &ClassNetGUID );
	
	UClass * SubObjClass = Cast< UClass >( SubObjClassObj );
	if ( SubObj == NULL )
	{
		// new出我们的SubObject
		SubObj = NewObject< UObject >(Actor, SubObjClass);
		// Notify actor that we created a component from replication
		Actor->OnSubobjectCreatedFromReplication( SubObj );
		// 客户端这边注册NetGUID,也就是写到ObjectLookup数组中
		Connection->Driver->GuidCache->RegisterNetGUID_Client( NetGUID, SubObj );
		// Track which sub-object guids we are creating
		CreateSubObjects.Add( SubObj );
		// 如果NetGUID是有效的，我们也罢NetGUID添加到ImportedNetGuids数组里面
		if (ensureMsgf(NetGUID.IsValid(), TEXT("Channel tried to add an invalid GUID to the import list: %s"), *Describe()))
		{
			Connection->Driver->GuidCache->ImportedNetGuids.Add( NetGUID );
		}
	}
}
```
#### 3 UPackageMapClient::SerializeObject
```cpp
// 这个方法会从Bunch的operator<<调用过来，也就是上边的Bunch << Obj;这一行
bool UPackageMapClient::SerializeObject( FArchive& Ar, UClass* Class, UObject*& Object, FNetworkGUID *OutNetGUID)
{
	// IsSaving是服务器写入的过程
	if (Ar.IsSaving())
	{
		// 在服务器上给这个Object分配NetGUID
		FNetworkGUID NetGUID = GuidCache->GetOrAssignNetGUID( Object );
		if (OutNetGUID)
		{
			*OutNetGUID = NetGUID;
		}
		// 这个方法在这里的作用就是将NetGUID写入到Ar中，其余的部分应该都不走
		InternalWriteObject( Ar, NetGUID, Object, TEXT( "" ), NULL );
		if (!NetGUID.IsDefault() && ShouldSendFullPath(Object, NetGUID))
		{
			// 这个部分就是静态的Object么，里面会再次调用InternalWriteObject方法，
			// 这里面还会写入Object的Outer，是个递归，一直写入知道Outer已经被写如果了就退出
			// 多次调用InternalWriteObject方法，每次都会写入NetGUID，但写入的不是Ar，是CurrentExportBunch这个Bunch，这个Bunch最终会在SendBunch使用，会放在发送给客户端最终Bunch的头部
			if ( !ExportNetGUID( NetGUID, Object, TEXT(""), NULL ) )
			{
			}
		}
		return true;
	}
	// IsLoading是客户端读取的过程
	else if (Ar.IsLoading())
	{
		FNetworkGUID NetGUID;
		// 这个方法下边详细看下
		NetGUID = InternalLoadObject(Ar, Object, 0);
		// Object所在的Level没加载完
		if (!ObjectLevelHasFinishedLoading(Object))
		{
			Object = NULL;
		}
		// Object是否是正确的
		if (Object && !Object->IsA(Class))
		{
			Object = NULL;
		}
		// 如果NetGUID是有效的 && 可以TrackUnmappedGUID && NetGUDI不是Broken的
		if ( NetGUID.IsValid() && bShouldTrackUnmappedGuids && !GuidCache->IsGUIDBroken( NetGUID, false ) )
		{
			// 如果Object为空，就添加到TrackedUnmappedNetGuids数组里面
			if ( Object == nullptr )
			{
				TrackedUnmappedNetGuids.Add( NetGUID );
			}
			// 如果Object不为空，并且NetGUID是动态的
			else if ( NetGUID.IsDynamic() )
			{
				TrackedMappedDynamicNetGuids.Add( NetGUID );
			}
		}
	}
}
```
#### 4 Server UPackageMapClient::ExportNetGUID
```cpp
/*
1 这个方法是在上边SerializeObject方法调用，即Object满足if (!NetGUID.IsDefault() && ShouldSendFullPath(Object, NetGUID))这个条件调用过来的。
2 这个方法的作用就是将Object及其Outer的NetGUID都序列化到CurrentExportBunch这个Bunch里面。
3 如果CurrentExportBunch这个Bunch满了，就通过ExportNetGUIDHeader方法将CurrentExportBunch放到ExportBunches这个数组里面。
4 我们在SendBunch的时候，会执行AppendExportBunches方法，这个方法作用就是遍历ExportBunches数组，将其内容放到SendBunch里的要发送的Bunch中（在3中说的CurrentExportBunch满了ExportBunches才会有值，如果CurrentExportBunch没满，这一次SenBunch就不会发送么？也会发送，逻辑也是在AppendExportBunches方法中，这个方法会先判断CurrentExportBunch这个里面是否有值，就会把值放到ExportBunches数组里面）。
5 最终CurrentExportBunch这个Bunch会放在发送给客户端Bunch的头部
*/
```
#### 5 Server FNetGUIDCache::GetOrAssignNetGUID
```cpp
FNetworkGUID FNetGUIDCache::GetOrAssignNetGUID(...)
{
	if (!Object || !SupportsObject(Object, &WeakObject))
	{
		// Null of unsupported object, leave as default NetGUID and just return mapped=true
		return FNetworkGUID();
	}
	// 会先在数组中找，找到了就返回
	FNetworkGUID NetGUID = NetGUIDLookup.FindRef(WeakObject);
	if (NetGUID.IsValid())
	{
	}
	return AssignNewNetGUID_Server(Object);
}

FNetworkGUID FNetGUIDCache::AssignNewNetGUID_Server( UObject* Object )
{
	// 判断是否是静态的Object，前边说过的如果Object名称不是网络稳定的就是不是静态的
	const int32 IsStatic = IsDynamicObject( Object ) ? 0 : 1;
	// 如果是静态的Object，他的NetGUID的最后一位是1，这个ALLOC_NEW_NET_GUID宏有或的操作
	const FNetworkGUID NewNetGuid( ALLOC_NEW_NET_GUID( IsStatic ) );
	// 注册NetGUID
	RegisterNetGUID_Server( NewNetGuid, Object );
	return NewNetGuid;
}

void FNetGUIDCache::RegisterNetGUID_Server( const FNetworkGUID& NetGUID, UObject* Object )
{
	FNetGuidCacheObject CacheObject;
	// Object的弱指针
	CacheObject.Object				= MakeWeakObjectPtr(const_cast<UObject*>(Object));
	// Outer的NetGUID
	CacheObject.OuterGUID			= GetOrAssignNetGUID( Object->GetOuter() );
	CacheObject.PathName			= Object->GetFName();
	CacheObject.NetworkChecksum		= GetNetworkChecksum( Object );
	CacheObject.bNoLoad				= !CanClientLoadObject( Object, NetGUID );
	CacheObject.bIgnoreWhenMissing	= CacheObject.bNoLoad;
	RegisterNetGUID_Internal( NetGUID, CacheObject );
}

void FNetGUIDCache::RegisterNetGUID_Internal( const FNetworkGUID& NetGUID, const FNetGuidCacheObject& CacheObject )
{
	// 添加到了NetGUIDLookup数组中，Key是Object，Value是NetGUID
	if ( CacheObject.Object != NULL )
	{
		// If we have an object, associate it with this guid now
		NetGUIDLookup.Add( CacheObject.Object, NetGUID );
	}
}
```

#### 6 UPackageMapClient::InternalWriteObject UPackageMapClient::InternalLoadObject
```cpp
void UPackageMapClient::InternalWriteObject( FArchive & Ar, FNetworkGUID NetGUID, UObject* Object, FString ObjectPathName, UObject* ObjectOuter )
{
	// 
	/*
		GuidCache->ShouldAsyncLoad() 客户端是否支持异步加载Object
		IsNetGUIDAuthority() 是否在服务器上
		!GuidCache->IsExportingNetGUIDBunch 不能是ExportingNetGUIDBunch的流程
		!bNoLoad 客户端可以加载此Object
	*/
	// 满足条件后，会将Object的NetGUID添加到MustBeMappedGuidsInLastBunch这个数组里
	if ( GuidCache->ShouldAsyncLoad() && IsNetGUIDAuthority() && !GuidCache->IsExportingNetGUIDBunch && !bNoLoad )
	{
		MustBeMappedGuidsInLastBunch.AddUnique( NetGUID );
	}
	// 把NetGUID写入
	Ar << NetGUID;
	if ( NetGUID.IsDefault() )
	{
		ExportFlags.bHasPath = 1;
		Ar << ExportFlags.Value;
	}
	// 上边的ExportNetGUID这个方法就会走这里，如果我们Object通过ShouldSendFullPath这个方法，他就会写入bHasPath，意思是静态的Object,或者说是网络名称稳定的那种
	else if ( GuidCache->IsExportingNetGUIDBunch )
	{
		if ( Object != NULL )
		{
			ExportFlags.bHasPath = ShouldSendFullPath( Object, NetGUID ) ? 1 : 0;
		}
		else
		{
			ExportFlags.bHasPath = ObjectPathName.IsEmpty() ? 0 : 1;
		}
		ExportFlags.bNoLoad	= bNoLoad ? 1 : 0;
		Ar << ExportFlags.Value;
	}
	if ( ExportFlags.bHasPath )
	{
		if ( Object != NULL )
		{
			ObjectPathName = Object->GetName();
			ObjectOuter = Object->GetOuter();
		}
		// 是不是Package里面的Object，也就是场景中放置的Object
		// 如果Object不是网络名称稳定的，这个NetGUID.IsStatic()就返回true
		const bool bIsPackage = ( NetGUID.IsStatic() && Object != NULL && Object->GetOuter() == NULL );
		// 拿到Outer的GUID，然后递归调用
		FNetworkGUID OuterNetGUID = GuidCache->GetOrAssignNetGUID( ObjectOuter );
		InternalWriteObject( Ar, OuterNetGUID, ObjectOuter, TEXT( "" ), NULL );
		// 这部分就是写入ObjectPathName，这个就是Object那个路径吧
		if (Connection->Driver)
		{
			FName SearchPath = FName(*ObjectPathName);
			FName RenamedPath = Connection->Driver->RenamedStartupActors.FindRef(SearchPath);
			if (RenamedPath != NAME_None)
			{
				ObjectPathName = RenamedPath.ToString();
			}
		}
		// 这个GEnigne是走GameEngine么，打断点是走到的GameEngine
		GEngine->NetworkRemapPath(Connection, ObjectPathName, false);
		Ar << ObjectPathName;
		// 写入NetworkChecksum，还不知道这个干嘛的
		uint32 NetworkChecksum = 0;
		if ( ExportFlags.bHasNetworkChecksum )
		{
			NetworkChecksum = GuidCache->GetNetworkChecksum( Object );
			Ar << NetworkChecksum;
		}
		// 给ObjectLookup数组里存的，重新赋值
		FNetGuidCacheObject* CacheObject = GuidCache->ObjectLookup.Find( NetGUID );
		if ( CacheObject != NULL )
		{
			CacheObject->PathName			= FName( *ObjectPathName );
			CacheObject->OuterGUID			= OuterNetGUID;
			CacheObject->bNoLoad			= ExportFlags.bNoLoad;
			CacheObject->bIgnoreWhenMissing = ExportFlags.bNoLoad;
			CacheObject->NetworkChecksum	= NetworkChecksum;
		}
	}
}
/*
服务器这边写入的时候：
1 写入NetGUID
2 写入ExportFlags，他每一字节都是一个标志位
3 写入ObjectPathName，object的路径名称
4 写入NetworkChecksum，还不知道干嘛的
*/
/*
客户端这边读取的时候，就和服务器保持一致，按顺序读取
*/

// Ar 客户端收到的这个Bunch
// Object 这个Bunch里的Subobject，客户端需要序列化出来，然后传出去
// InternalLoadObjectRecursionCount 递归的次数，通过SerializeObject这里传进来是0
FNetworkGUID UPackageMapClient::InternalLoadObject( FArchive & Ar, UObject *& Object, const int InternalLoadObjectRecursionCount )  
{
	// 从Ar中读取出NetGUID
	FNetworkGUID NetGUID;  
	Ar << NetGUID;
	// 尝试通过NetGUID拿到他对应的Object，这个方法在客户端 子物体接收的地方看下
	if ( NetGUID.IsValid() && !NetGUID.IsDefault() )
	{
		Object = GetObjectFromNetGUID(NetGUID,GuidCache->IsExportingNetGUIDBunch );
	}
	// 和InternalWriteObject方法写入的时候保持一致，这里读取出FExportFlags
	FExportFlags ExportFlags;
	if ( NetGUID.IsDefault() || GuidCache->IsExportingNetGUIDBunch )
	{
		Ar << ExportFlags.Value;
	}
	// 记录我们的ImportNetGUID
	if ( GuidCache->IsExportingNetGUIDBunch )
	{
		if (NetGUID.IsValid())
		{
			GuidCache->ImportedNetGuids.Add( NetGUID );
		}
	}
	if ( ExportFlags.bHasPath )
	{
		// 递归，获取outer的NetGUID
		FNetworkGUID OuterGUID = InternalLoadObject( Ar, ObjOuter, InternalLoadObjectRecursionCount + 1 );
		// 读取我们的路径名称
		Ar << PathName;
		// 读取NetworkChecksum
		if ( ExportFlags.bHasNetworkChecksum )
		{
			Ar << NetworkChecksum;
		}
		// 网络名称稳定的NetGUID.IsStatic()为true
		const bool bIsPackage = NetGUID.IsStatic() && !OuterGUID.IsValid();
		
		GEngine->NetworkRemapPath( Connection, PathName, true );
		// 客户端这边注册NetGUID,也就是写到ObjectLookup数组中
		GuidCache->RegisterNetGUIDFromPath_Client( NetGUID, PathName, OuterGUID, NetworkChecksum, ExportFlags.bNoLoad, bIgnoreWhenMissing );
		// 再一次尝试通过NetGUID来拿到他对应的UObject
		Object = GuidCache->GetObjectFromNetGUID( NetGUID, GuidCache->IsExportingNetGUIDBunch );
	}
}
```
## 2 Client 子物体接收 UIpNetDriver::TickDispatch
``` cpp
UIpNetDriver::TickDispatch(...)
 ->UNetConnection::ReceivedRawPacket(...)
  ->UNetConnection::ReceivedPacket(...)
   ->UChannel::ReceivedRawBunch(...)
    ->UChannel::ReceivedNextBunch(...)
     ->UChannel::ReceivedSequencedBunch(...)
      ->UActorChannel::ReceivedBunch(...)
// 以上的流程就是，ActorChannel接收到Bunch，从ReceivedBunch开始就有可能分成两部分，一部分就是走流程处理这个Bunch，另一部分就是把Bunch放到QueueBunch里面，在客户端的TickFlush里面执行
// 第一部分
UActorChannel::ReceivedBunch(...)
 ->UActorChannel::ProcessBunch(...)
  ->UActorChannel::ReadContentBlockPayload(...)//这个方法在Server上分析过了
   ->UActorChannel::ReadContentBlockHeader(...)
    ->UPackageMapClient::SerializeObject(...)//这个方法在Server上分析过了
     ->UPackageMapClient::InternalLoadObject(...)//这个方法在Server上分析过了
      ->UPackageMapClient::GetObjectFromNetGUID(...)//主要看下这个方法
// 第二部分
UNetDriver::TickFlush(...)
 ->UNetConnection::Tick(...)
  ->UActorChannel::Tick(...)
   ->UActorChannel::ProcessQueuedBunches(...)//主要看下这个方法，我们是加了个命令，让所有的Bunch都放到QueuedBunch里面
```
### 1 UPackageMapClient::GetObjectFromNetGUID
```cpp
// NetGUID，就是我们SubObject的NetGUID
// bIgnoreMustBeMapped，这个是GuidCache->IsExportingNetGUIDBunch标志位
UObject* UPackageMapClient::GetObjectFromNetGUID( const FNetworkGUID& NetGUID, const bool bIgnoreMustBeMapped )
{
	return GuidCache->GetObjectFromNetGUID( NetGUID, bIgnoreMustBeMapped );
}
UObject* FNetGUIDCache::GetObjectFromNetGUID( const FNetworkGUID& NetGUID, const bool bIgnoreMustBeMapped )
{
	// 在客户端拿到NetGUID对应的CacheObject
	FNetGuidCacheObject * CacheObjectPtr = ObjectLookup.Find( NetGUID );
	// 这个Objet就是SubObject
	UObject* Object = CacheObjectPtr->Object.Get();
	// 如果有SubObject了，就直接返回了
	if ( Object != NULL )
	{
		return Object;
	}
	// SubObject的Outer
	UObject* ObjOuter = NULL;
	// Outer的NetGUID是有效的
	if ( CacheObjectPtr->OuterGUID.IsValid() )
	{
		// 如果客户端这还没解析SubObject的Outer的话，这里提前返回
		FNetGuidCacheObject * OuterCacheObject = ObjectLookup.Find( CacheObjectPtr->OuterGUID );
		if ( OuterCacheObject == NULL )
		{
			return NULL;
		}
		if ( OuterCacheObject->bIsBroken )
		{
			CacheObjectPtr->bIsBroken = 1;
			return NULL;
		}
		// 根据Outer的NetGUID来解析出UObject
		ObjOuter = GetObjectFromNetGUID( CacheObjectPtr->OuterGUID, bIgnoreMustBeMapped );
		// 没解析出来,返回NULL
		if ( ObjOuter == NULL )
		{
			return NULL;
		}
	}
	// 在内存中找下SubObject是否存在
	Object = StaticFindObject( UObject::StaticClass(), ObjOuter, *CacheObjectPtr->PathName.ToString(), false );
	// 内存中没有，还没加载过Object。并且bNoLoad为false，也就是客户端可以加载这个SubObject（CanClientLoadObject这个方法决定bNoLoad标志位）
	if ( Object == NULL && !CacheObjectPtr->bNoLoad )
	{
		if ( bIsPackage )
		{
			// 如果开启了异步加载就无法立即求解出Object，而是开始异步加载流程
			if ( ShouldAsyncLoad() )
			{
				if (!PendingAsyncLoadRequests.Contains(CacheObjectPtr->PathName))
				{
					StartAsyncLoadingPackage(*CacheObjectPtr, NetGUID, false);
				}
				else
				{
					ValidateAsyncLoadingPackage(*CacheObjectPtr, NetGUID);
				}
				return NULL;
			}
			else
			{
				// 如果是Package就LoadPackage
				Object = LoadPackage( NULL, *CacheObjectPtr->PathName.ToString(), LOAD_None );
				SyncLoadedGUIDs.AddUnique(NetGUID);
			}
		}
		else
		{
			// 如果不是Package就LoadObject
			Object = StaticLoadObject( UObject::StaticClass(), ObjOuter, *CacheObjectPtr->PathName.ToString(), NULL, LOAD_NoWarn );
		}
	}
	// 缓存住我们加载出来的Object
	CacheObjectPtr->Object = Object;
	// 求解出来Object，我们给QueuedBunchObjectReferences数组里的重新写入
	UpdateQueuedBunchObjectReference(NetGUID, Object);
	
	return Object
}
void FNetGUIDCache::UpdateQueuedBunchObjectReference(const FNetworkGUID NetGUID, UObject* NewObject)
{
	if (TWeakPtr<FQueuedBunchObjectReference>* WeakObjectReference = QueuedBunchObjectReferences.Find(NetGUID))
	{
		TSharedPtr<FQueuedBunchObjectReference> SharedObjectReference = WeakObjectReference->Pin();
		if (FQueuedBunchObjectReference* ObjectReference = SharedObjectReference.Get())
		{
			ObjectReference->Object = NewObject;
		}
	}
}
```
### 2 UActorChannel::ProcessQueuedBunches
```cpp
bool UActorChannel::ProcessQueuedBunches()
{
	bSuppressQueuedBunchWarningsDueToHitches |= Connection->Driver->DidHitchLastFrame();
	/* 
	1. SpawnedNewActorChannels 这个数组的添加是在SpawnedNewActor里面，移除是在SetActorChannel里面，表示了正在生成NewActor
	2. bSpawningNewActor 这个标志位和SpawnedNewActorChannels数组几乎一样吧，表示正在生成NewActor
	3. bAllowProcessBunch 表示了没有正在处理Bunch。因为我们是在Tick里面走，如果走到的时候我们正在处理Bunch，bAllowProcessBunch就为false
	*/ 
	const bool bAllowProcessBunch = Connection->Driver->SpawnedNewActorChannels.Num() == 0 || bSpawningNewActor;
	/*
	1. KeepProcessingActorChannelBunchesMap 这个数组的添加时在ActorChannel::CleanUp方法中，表示的含义就是我们在清理ActorChannel的时候，发现QueueBunch里面还有没来得及处理的Bunch，就添加到这个数组里面
	*/
	const bool bKeepProcessing = Connection->KeepProcessingActorChannelBunchesMap.Contains(ActorNetGUID);
	// 没有正在处理Bunch && (还有没来得及处理的Bunch || 时间限制，到时间了为true)
	if (bAllowProcessBunch && (bKeepProcessing || Connection->Driver->HasTimeToProcess()))
	{
		// 这个是我们自己添加的标志位，就是把整个ProcessBunch拆分了吧
		if (EnableProcessNewActorOnly)
		{
			// 没有生成过Actor，就SpawnedNewActor
			if (Actor == nullptr && QueuedBunches.Num())
			{
				FInBunch* QueuedInBunch = QueuedBunches[0];
				SpawnedNewActor(*QueuedInBunch);
			}
			// Actor生成错误，就清掉QueuedBunch
			if (!Actor)
			{
				QueuedBunches.Empty();
				return false;
			}
			// 继续处理Bunch
			while (QueuedBunches.Num())
			{
				FInBunch* QueuedInBunch = QueuedBunches[0];
				ProcessBunchWithoutNewActor(*QueuedInBunch);
				delete QueuedInBunch;
				QueuedBunches.RemoveAt(0);

				if (bSpawningNewActor)
				{
					PostSpawnedNewActor();
				}
			}
		}
		// 下面这个就是ue的流程，调用ProcessBunch方法来处理Bunch
		else
		{
			for (FInBunch* QueuedInBunch : QueuedBunches)
			{
				ProcessBunch(*QueuedInBunch);
				delete QueuedInBunch;
			}
			QueuedBunches.Empty();
		}
		// 调用Onrep方法吧
		for (auto& ReplicatorPair : ReplicationMap)
		{
			ReplicatorPair.Value->CallRepNotifies(true);
		}
	}
	else
	{
		QueuedBunchStartTime = FPlatformTime::Seconds();
	}
	return QueuedBunches.Num() == 0;
}
```

# 5 RPC 同步
## 1 Server 执行Client标记Function
```cpp
// AzurePlayerController.h文件中
UFUNCTION(client, reliable)
void ClientTestRPC(const FString& content);
// AzurePlayerController.cpp文件中
void AAzurePlayerController::ClientTestRPC_Implementation(const FString& content)
{
	UE_LOG(LogTemp, Log, TEXT("ClientTestRPC_Implementation exec content = %s"), *content);
}
// AzurePlayerController.gen.cpp文件中
static FName NAME_AAzurePlayerController_ClientTestRPC = FName(TEXT("ClientTestRPC"));
void AAzurePlayerController::ClientTestRPC(const FString& content)
{
	AzurePlayerController_eventClientTestRPC_Parms Parms;
	Parms.content=content;
	// FindFunctionChecked 方法就在UClass中找到名称为NAME_AAzurePlayerController_ClientTestRPC的UFunction
	ProcessEvent(
		FindFunctionChecked(NAME_AAzurePlayerController_ClientTestRPC),&Parms);
}
// 服务器调用ClientTestRPC方法是在gen.cpp文件中，通过AActor::ProcessEvent的方法调用
```
### ProcessEvent
```cpp
void AActor::ProcessEvent(UFunction* Function, void* Parameters)
{
	// 包含了一堆乱七八糟的东西，主要就是调用父类的ProcessEvent
	Super::ProcessEvent(Function, Parameters);
}

void UObject::ProcessEvent( UFunction* Function, void* Parms )
{
	// 如果PendingKill就提前返回，不执行了
	if (IsPendingKill())
	{
		return;
	}
	// 看样子得是CPP的Function
	if ((Function->FunctionFlags & FUNC_Native) != 0)
	{
		// 这个GetFunctionCallspace方法里有条件判断
		int32 FunctionCallspace = GetFunctionCallspace(Function, NULL);
		// 这个Function是在远端调用的
		if (FunctionCallspace & FunctionCallspace::Remote)
		{
			// 执行远端的方法，下面看下
			CallRemoteFunction(Function, Parms, NULL, NULL);
		}

		if ((FunctionCallspace & FunctionCallspace::Local) == 0)
		{
			return;
		}
	}
}
```
#### AActor::GetFunctionCallspace
```cpp
int32 AActor::GetFunctionCallspace( UFunction* Function, FFrame* Stack )
{
	// 这个方法就是判断Actro的localRole和连接建立的情况来返回是远端调用，还是本地调用，还是吸收也就是终止调用
	const ENetRole LocalRole = GetLocalRole();
	// 一般Callspace都是FunctionCallspace::Local
	FunctionCallspace::Type Callspace = (LocalRole < ROLE_Authority) && Function->HasAllFunctionFlags(FUNC_BlueprintAuthorityOnly) ? FunctionCallspace::Absorbed : FunctionCallspace::Local;
	// 如果在服务器端调用 并且 不是Client修饰的方法，直接返回Callspace
	if (bIsServer && !(Function->FunctionFlags & FUNC_NetClient))
	{
		return Callspace;
	}
	// 如果是在客户端调用 并且 不是server修饰的方法，直接返回Callspace
	if (!bIsServer && !(Function->FunctionFlags & FUNC_NetServer))
	{
		return Callspace;
	}
	// 如果Actor的RemoteRole是ROLE_None，则终止调用
	if (RemoteRole == ROLE_None)
	{
		return FunctionCallspace::Absorbed;
	}
	// 判断全过，才会远端调用
	return FunctionCallspace::Remote;
}
```
#### AActor::CallRemoteFunction
```cpp
bool AActor::CallRemoteFunction( UFunction* Function, void* Parameters, FOutParmRec* OutParms, FFrame* Stack )
{
	bool bProcessed = false;
	// 拿到当前World的上下文
	FWorldContext* const Context = GEngine->GetWorldContextFromWorld(GetWorld());
	if (Context != nullptr)
	{
		// 通过World上下文获取NetDriver
		for (FNamedNetDriver& Driver : Context->ActiveNetDrivers)
		{
			// 条件判断，ShouldReplicateFunction方法就是判断this所在的NetDriver和World上下文中的NetDriver是不是一个
			if (Driver.NetDriver != nullptr && Driver.NetDriver->ShouldReplicateFunction(this, Function))
			{
				// 调用NetDriver的ProcessRemoteFunction方法，下面看下
				// 参数就是调用的Actor,Function，Function的参数,剩下三个都是nullptr
				Driver.NetDriver->ProcessRemoteFunction(this, Function, Parameters, OutParms, Stack, nullptr);
				bProcessed = true;
			}
		}
	}

	return bProcessed;
}
```
##### UNetDriver::ProcessRemoteFunction
```cpp
void UNetDriver::ProcessRemoteFunction(...)  
{
	// 条件判断，提前返回掉
	if (Actor->IsActorBeingDestroyed())
	{
		return;
	}
	{
		// 是不是Server端
		const bool bIsServer = IsServer();
		// 是不是Server端调用的Multicast方法
		const bool bIsServerMulticast = bIsServer && (Function->FunctionFlags & FUNC_NetMulticast);
		// 自增一个计数器
		++TotalRPCsCalled;
		// 这个ReplicationDriver服务器端会创建，是在UNetDriver::InitBase方法中创建的，这里调用了它的ProcessRemoteFunction方法，这个方法的作用是？可能以后看看吧
		if (ReplicationDriver && ReplicationDriver->ProcessRemoteFunction(Actor, Function, Parameters, OutParms, Stack, SubObject))
		{
			return;
		}
		// 接下来这个判断里都是服务器调用多播函数的部分，等下细看下
		if (bIsServerMulticast)
		{
			// 拿到Function的RepLayout
			TSharedPtr<FRepLayout> RepLayout = GetFunctionRepLayout(Function);
			EProcessRemoteFunctionFlags RemoteFunctionFlags = EProcessRemoteFunctionFlags::None;
			TArray<UNetConnection*> UniqueRealConnections;
			// 遍历ClientConnections数组
			for (int32 i = 0; i < ClientConnections.Num(); ++i)
			{
				Connection = ClientConnections[i];
				if (Connection && Connection->ViewTarget)
				{
					FNetViewer Viewer(Connection, 0.f);
					if (Connection->GetUChildConnection() != nullptr)
					{
						Connection = ((UChildConnection*)Connection)->Parent;
					}
					// 条件判断,Actor的相关性 或者 Reliable的多播。这个判断也就是说广播不一定会给所有的客户端发送，要么Actor是相关的，要么是（Reliable的多播并且在客户端能找到Actor，也就是已经将Actor同步过了）
					if (Actor->IsNetRelevantFor(Viewer.InViewer, Viewer.ViewTarget, Viewer.ViewLocation) ||
						((Function->FunctionFlags & FUNC_NetReliable) && !!CVarAllowReliableMulticastToNonRelevantChannels.GetValueOnGameThread() && Connection->FindActorChannelRef(Actor)))
					{
						// 通过RepLayout来序列化RPC的属性
						RepLayout->BuildSharedSerializationForRPC(Parameters);
						// 后面详细看下
						InternalProcessRemoteFunctionPrivate(Actor, SubObject, Connection, Function, Parameters, OutParms, Stack, bIsServer, RemoteFunctionFlags);
					}
				}
			}
			// 清数据
			RepLayout->ClearSharedSerializationForRPC();
			return;
		}
		// 如果Actor的NetConnection存在
		Connection = Actor->GetNetConnection();
		if (Connection)
		{
			InternalProcessRemoteFunction(Actor, SubObject, Connection, Function, Parameters, OutParms, Stack, bIsServer);
		}
	}
}

void UNetDriver::InternalProcessRemoteFunction(
	AActor* Actor, // 调用RPC的Actor
	UObject* SubObject,// 我们的例子是Null的
	UNetConnection* Connection,// Actor所处的NetConnection
	UFunction* Function,// 此次调用的方法，也就是RPC函数
	void* Parms,// 方法中的参数
	FOutParmRec* OutParms,// 我们的例子是null的
	FFrame* Stack,// 我们的例子是null的
	const bool bIsServer)// 是否在服务端调用
{
	EProcessRemoteFunctionFlags UnusedFlags = EProcessRemoteFunctionFlags::None;
	InternalProcessRemoteFunctionPrivate(Actor, SubObject, Connection, Function, Parms, OutParms, Stack, bIsServer, UnusedFlags);
}
```
##### UNetDriver::InternalProcessRemoteFunctionPrivate
```cpp
void UNetDriver::InternalProcessRemoteFunctionPrivate(...)
{
	// 遍历到最上的Function
	while (Function->GetSuperFunction())
	{
		Function = Function->GetSuperFunction();
	}
	// 如果不是稳定RPC && 不是广播 && 网络通道已经满了 就抛弃RPC的调用
	if (!(Function->FunctionFlags & FUNC_NetReliable) && (!(Function->FunctionFlags & FUNC_NetMulticast)) && (!Connection->IsNetReady(0)))
	{
		return;
	}
	// TargetObj
	UObject* TargetObj = SubObject ? SubObject : Actor;
	// 拿到ClassCache
	const FClassNetCache* ClassCache = NetCache->GetClassNetCache( TargetObj->GetClass() );
	// 拿到FieldCache
	const FFieldNetCache* FieldCache = ClassCache->GetFromField(Function);
	// 找到调用RPC的Actor的ActorChannel
	UActorChannel* Ch = Connection->FindActorChannelRef(Actor);
	if (!Ch)
	{
		// 如果是服务器端调用RPC函数，
		if (bIsServer)
		{
			// 如果关卡已经初始化了，就创建ActorChannel
			if (IsLevelInitializedForActor(Actor, Connection))
			{
				Ch = Cast<UActorChannel>(Connection->CreateChannelByName(NAME_Actor, EChannelCreateFlags::OpenedLocally));
			}
			else
			{
				return;
			}
		}
		// 如果是客户端调用RPC函数，但是ActorChannel还未创建，就终止调用
		if (!Ch)
		{
			return;
		}
		// 然后紧接着SetChannelActor
		if (bIsServer)
		{
			Ch->SetChannelActor(Actor, ESetChannelActorFlags::None);
		}
	}
	// 进一步调用方法
	ProcessRemoteFunctionForChannelPrivate(Ch, ClassCache, FieldCache, TargetObj, Connection, Function, Parms, OutParms, Stack, bIsServer, ERemoteFunctionSendPolicy::Default, Flags);
}
```
##### UNetDriver::ProcessRemoteFunctionForChannelPrivate
```cpp
void UNetDriver::ProcessRemoteFunctionForChannelPrivate(
	UActorChannel* Ch,// 调用RPC的Actor的ActorChannel
	const FClassNetCache* ClassCache,// TargetObject 的 ClassCache
	const FFieldNetCache* FieldCache,// RPC方法的FieldCache
	UObject* TargetObj,// 调用RPC的Object
	UNetConnection* Connection,// 调用RPC的Actor的NetConnection
	UFunction* Function,// RPC方法
	void* Parms,// Function的参数
	FOutParmRec* OutParms,
	FFrame* Stack,// 栈帧结构，蓝图用到吧我猜
	const bool bIsServer,// 是否是服务器端
	const ERemoteFunctionSendPolicy SendPolicy,//ERemoteFunctionSendPolicy::Default
	EProcessRemoteFunctionFlags& RemoteFunctionFlags)// None 默认的
{
	// 在服务器上OpenPacketId这个会在SendBunch里面记录，在客户端上会在ReceivedNextBunch里面记录，初始化的时候是INDEX_NONE
	if (Ch->OpenPacketId.First == INDEX_NONE)
	{
		if (!bIsServer)
		{
			return;
		}
		// 这个bIsReplicatingActor标志位是在ReplicateActor方法中设置为true
		if (Ch->bIsReplicatingActor)
		{
			return;
		}
		// 如果RemoteFunctionFlags标志位里不含有EProcessRemoteFunctionFlags::ReplicatedActor，就添加上
		if (!EnumHasAnyFlags(RemoteFunctionFlags, EProcessRemoteFunctionFlags::ReplicatedActor))
		{
			ReplicationFrame++;
			Ch->GetActor()->CallPreReplication(this);
			RemoteFunctionFlags |= EProcessRemoteFunctionFlags::ReplicatedActor;
		}
		// 最后调用ReplicateActor方法
		Ch->ReplicateActor();
	}
	// 如果当前ActorChannel无效了
	if (Ch->ChIndex == -1)
	{
		return;
	}
	// RepContext 上下文
	FScopedRepContext RepContext(Connection, Ch->GetActor());
	// 应该就是存储RPC的Bunch了吧
	FOutBunch Bunch(Ch, 0);
	// Function中是否带有FUNC_NetReliable标志位
	if (Function->FunctionFlags & FUNC_NetReliable)
	{
		Bunch.bReliable = 1;
	}
	// 如果栈帧为nullptr，会赋值LocalOutParms
	TArray<NetDriverInternal::FAutoDestructProperty> LocalOutParms;
	if (Stack == nullptr)
	{
		LocalOutParms = NetDriverInternal::CopyOutParametersToLocalParameters(Function, OutParms, Parms, TargetObj);
	}
	FNetBitWriter TempWriter( Bunch.PackageMap, 0 );
	// 拿到Function的RepLayout
	TSharedPtr<FRepLayout> RepLayout = GetFunctionRepLayout(Function);
	// SendProperties，将属性放到TempWriter里面
	RepLayout->SendPropertiesForRPC(Function, Ch, TempWriter, Parms);
	// 
	FNetFieldExportGroup* NetFieldExportGroup = Ch->GetOrCreateNetFieldExportGroupForClassNetCache(TargetObj);
	// 是否将Bunch放到Queue里面，意思就是立即发送Bunch还是放到Queue里面等待下一次Actor同步的时候一块发送。默认情况下，不稳定的多播都是放到Queue里面，其余的都是立即发送
	bool QueueBunch = false;
	switch (SendPolicy)
	{
		case ERemoteFunctionSendPolicy::Default:
			QueueBunch = ( !Bunch.bReliable && Function->FunctionFlags & FUNC_NetMulticast );
			break;
		case ERemoteFunctionSendPolicy::ForceQueue:
			QueueBunch = true;
			break;
		case ERemoteFunctionSendPolicy::ForceSend:
			QueueBunch = false;
			break;
	}
	// 放入到Queue里面的操作
	if (QueueBunch)
	{
		// 将TempWriter里的内容写进Bunch里面
		Ch->WriteFieldHeaderAndPayload(Bunch, ClassCache, FieldCache, NetFieldExportGroup, TempWriter);
		// 记录下大小吧
		ParameterBits = Bunch.GetNumBits();
	}
	// 立即发送的操作
	else
	{
		Ch->PrepareForRemoteFunction(TargetObj);
		// 局部TempBlockWriter Bunch
		FNetBitWriter TempBlockWriter(Bunch.PackageMap, 0);
		// 将TempWriter里的内容写进TempBlockWriter
		Ch->WriteFieldHeaderAndPayload(TempBlockWriter, ClassCache, FieldCache, NetFieldExportGroup, TempWriter);
		// 记录下TempWriter大小
		ParameterBits = TempBlockWriter.GetNumBits();
		// WriteContentBlockPayload方法，序列化TargetObj里的内容了，然后将TempBlockWriter在写入Bunch里面
		HeaderBits = Ch->WriteContentBlockPayload(TargetObj, Bunch, false, TempBlockWriter);
	}
	// 放入到Queue里面的操作
	if (QueueBunch)
	{
		// 下面看下这个方法
		Ch->QueueRemoteFunctionBunch(TargetObj, Function, Bunch);
	}
	// 立即发送的操作
	else
	{
		// 直接调用SendBunch了
		Ch->SendBunch(&Bunch, true);
	}
	// 如果是内部的就Flush
	if (Connection->IsInternalAck())
	{
		Connection->FlushNet();
	}
}
```
###### UActorChannel::QueueRemoteFunctionBunch
```cpp
// CallTarget，调用RPC的Object
// Func，RPC方法
// Bunch，这个Bunch里面有UFunction的属性，具体看SendPropertiesForRPC这个方法
void UActorChannel::QueueRemoteFunctionBunch( UObject* CallTarget, UFunction* Func, FOutBunch &Bunch )
{
	// 拿到Object对应的ObjectReplicator
	FObjectReplicator& Replicator = FindOrCreateReplicator(CallTarget).Get();
	{
		Replicator.ChangedState.ResetReplicatePropertiesNothingChanged();
	}
	// 进一步执行 QueueRemoteFunctionBunch 方法
	Replicator.QueueRemoteFunctionBunch( Func, Bunch );
}
void FObjectReplicator::QueueRemoteFunctionBunch( UFunction* Func, FOutBunch &Bunch )
{
	// 往RemoteFuncInfo数组里面赋值
	int32 InfoIdx = INDEX_NONE;
	if (InfoIdx == INDEX_NONE)
	{
		InfoIdx = RemoteFuncInfo.AddUninitialized();
		RemoteFuncInfo[InfoIdx].FuncName = Func->GetFName();
		RemoteFuncInfo[InfoIdx].Calls = 0;
	}
	RemoteFuncInfo[InfoIdx].LastCallTimestamp = OwningChannel->Connection->Driver->GetElapsedTime();
	RemoteFuncInfo[InfoIdx].LastCallTime = RemoteFuncInfo[InfoIdx].LastCallTimestamp;
	// 创建 RemoteFunctions Bunch
	if (RemoteFunctions == nullptr)
	{
		RemoteFunctions = new FOutBunch(OwningChannel, 0);
	}
	// 将参数Bunch里面的信息，写入到 RemoteFunctions Bunch中
	RemoteFunctions->SerializeBits(Bunch.GetData(), Bunch.GetNumBits());
	// 这一大块就是添加QueuedMustBeMappedGuidsInLastBunch 和 QueuedExportBunches 数组，在这也执行应该是RPC是独立的流程，先将RPC中用到的写入对应的Queue中，然后再SendBunch的时候遍历Queue，在一块写入发送的Bunch里面
	if (Connection->PackageMap != nullptr)
	{
		UPackageMapClient* PackageMapClient = CastChecked<UPackageMapClient>(Connection->PackageMap);
		if (PackageMapClient->GetMustBeMappedGuidsInLastBunch().Num())
		{
			OwningChannel->QueuedMustBeMappedGuidsInLastBunch.Append(PackageMapClient->GetMustBeMappedGuidsInLastBunch());
			PackageMapClient->GetMustBeMappedGuidsInLastBunch().Reset();
		}
		if (!Connection->IsInternalAck())
		{
			PackageMapClient->AppendExportBunches(OwningChannel->QueuedExportBunches);
		}
	}
}
```
## 2 Client
![image.png](https://gitee.com/lurenjia399/image/raw/master/image/202408071103081.png)
## 3 RPC调用总结
### 1 判断条件
```cpp
/*
1 AActor->LocalRole == ROLE_Authority 检测是否为本地权威角色
2 AActor::GetNetConnection() != NULL 检测Actor是否有所属链接，里面就是判断Actor的Owner的NetConnection，也就是判断Owner是不是Controller吧
3 AActor->RemoteRole != ROLE_None 建立Actor是否参数属性同步，也就是被Replicated标记。对于可同步的Actor，服务器上RemoteRole是ROLE_SimulatedProxy，Role是ROLE_Authority，客户端上RemoteRole是ROLE_Authority，本地Role是ROLE_AutonomousProxy，模拟Role是ROLE_SimulatedProxy。对于客户端创建的Actor，Role是ROLE_Authority，被Replicated标记的话RemoteRole是ROLE_SimulatedProxy，没被Replicated标记的话是ROLE_None。
*/
```
### 2 UFUNCTION(Server)
```cpp
/*
a. 用于声明由客户端发起调用，在服务器执行的 RPC 函数。  
b. 无论是否可靠，构造的 RPC Bunch 都会由 UChannel::SendBunch() 立即发送。
执行空间：
1 在服务器调用时仅本地执行
2 在客户端调用时，对于本地权威角色的Actor，若其参与了属性同步且有所属链接远端执行，否则本地执行
3 在客户端调用时，对于非本地权威角色的Actor，若其参与了属性同步且有所属链接远端执行，否则终止执行
*/
// 注意情况2，客户端本地权威的Actor是客户端生成的Actor，尽管可以将其bReplicated设置为true，并将其Owner设置成PlayerController（意思就是参与了属性同步且有所属链接），从而骗过AActor::GetFunctionCallspace方法返回可以远端执行，但是会因为在后续的UNetDriver::InternalProcessRemoteFunctionPrivate这个方法中，因为在客户端对应的ActorChannel还未创建从而抛弃调用。
// 注意情况3，客户端非本地权威角色的Actor，这种Actor肯定参与了属性同步但只有autonomous的有所属链接（AActor::GetNetConnection返回不为nullptr），而simulated的没有所属链接（AActor::GetNetConnection返回nullptr），所以autonomous的可以调用RPC而simulated不能调用，但是将simulated的Actor的Owner设置成PlayerController，simulated也就可以调用RPC了。
// 注意，如果是unrealiable的RPC，如果netConnection饱和了，就抛弃此调用。
```
### 3 UFUNCTION(Client)
```cpp
/*
a. 用于声明由服务器发起调用，在客户端执行的 RPC 函数。  
b. 无论是否可靠，构造的 RPC Bunch 都会由 UChannel::SendBunch() 立即发送。
执行空间：
1 在客户端调用时仅本地执行
2 在服务器调用时，对于本地权威角色的Actor，若其参与了属性同步且有所属链接远端执行，否则本地执行
3 在服务器调用时，对于非本地权威角色的Actor，若其参与了属性同步且有所属链接远端执行，否则终止执行
*/
// 注意情况2，服务器本地权威的Actor是正常的。如果在发送RPC的时候，服务器ActorChannel还未创建，则要验证链接的客户端已加载此 AActor 所在的 ULevel 后，才会创建ActorChannel，否则抛弃调用，如果链接的客户端还未创建ActorChannel，则会自动发送一个 AActor 属性同步的 Bunch 作为 OpenBunch 促使客户端创建 AActor 和通道 ，然后才继续进行 RPC 调用。
// 注意情况3，服务器上非本地权威的Actor，是在服务器上SetRole了么，改变了role吧。
// 注意，如果是unrealiable的RPC，如果netConnection饱和了，就抛弃此调用。
```
### 4 UFUNCTION(NetMusticast)
```cpp
/*
a.用于声明由服务器发起调用，并广播到所有客户端执行的 RPC 函数
b.若为可靠的则 RPC Bunch 会立即发送，若为不可靠的则 RPC Bunch 会随着下次此 RPC 函数所在对象向此链接进行属性同步时，才会一起发送
执行空间：
1 在客户端调用时仅本地执行
2 在服务器调用时，对于参与同步的 AActor 即会本地执行也会远程执行，对于不参与同步的 AActor 仅本地执行
*/
// 注意情况2，服务器调用是会遍历ClientConnections数组，检测Actor和NetConnection的相关性，如果不相关则不会发送。但如果是realiable的广播，如果不相关但还未移除ActorChannel前，也会发送。
// 注意，如果在发送RPC的时候，服务器的ActorChannel还未创建，则要验证链接的客户端已加载此 AActor 所在的 ULevel 后，才会创建ActorChannel，否则抛弃调用，如果链接的客户端还未创建ActorChannel，则会自动发送一个 AActor 属性同步的 Bunch 作为 OpenBunch 促使客户端创建 AActor 和通道 ，然后才继续进行 RPC 调用。
```
# 6 问题：

- Connection饱和是如何判定的？
```cpp
// 在NetConnection里的Tick执行，也是在NetDriver的TickFlush里面的
// DeltaBias = CurrentNetSpeed（这个是客户端发送NMT_Netspeed包里带的参数） BandwidthDeltaTime（这个是服务器的帧长）。看上去就是速度乘上时间得到的就是距离,也就是可用空间长度
float DeltaBits = CurrentNetSpeed * BandwidthDeltaTime * 8.f;
// 每一帧可用空间长度都会扩大
QueuedBits -= FMath::TruncToInt(DeltaBits);
// 两倍的当前帧可用空间长度
float AllowedLag = 2.f * DeltaBits;
// 如果可用空间长度比当前帧两倍还多，就设置成两倍
if (QueuedBits < -AllowedLag)
{
	QueuedBits = FMath::TruncToInt(-AllowedLag);
}
```
> 在Connection的Tick中更新QueuedBits这个变量，也就是更新可用空间的长度。判断是否饱和就判断可用空间长度是否能够容纳发送的数据长度。

- 每一帧会把所有的Actor都同步么？
> 不会。每一帧会都会构建需要同步的Actor数组并计算下一次需要同步的时间（这个时间是当前world时间 + 小范围的随机值 + Actor的同步频率的配置），每次在构建同步数组的时间会看是否到达了时间，如果到了才有可能加入数组中。每一个ClientConnection都会有可用的空间长度，每次同步了一个Actor之后会计算这个空间长度是否足够，不够了就说明饱和了，剩余还未同步的Actor就给下一帧同步。

- 在客户端spawn一个actor，并调用AActor::SetReplicates方法，服务器会有这个actor么？
> 不会，客户端创建的一定只在客户端，setreplicates只是会把actor放在GetNetworkObjectList()数组中，但是这是在客户端里面，服务器同步的话，必须是放在服务器的GetNetworkObjectList()数组中。

- replicate怎么运作的？客户端服务器都走的代码，比如actor是怎么同步过来的？
> 看上去如果在服务器创建一个可复制的actor，他会自动同步到客户端。如果客户端在此基础上再创建一个可复制的actor，那就会在客户端存在两个actor。

- 静态的组件，也就是c++构造函数创建的和蓝图中挂上去的会默认同步？replicate不用为true就可以同步么？
> replicate为false的静态组件（路径稳定）看上去应该不算是默认同步，因为这些静态的组件在actor同步到客户端的时候就会创建出来了，相当于是客户端自己创建的，服务器没有把这些静态组件放到bunch里面。但是要是想同步这些静态组件的属性，就必须是replicate为true的，因为这样才会把组件放到Bunch里面，进而同步组件里replicate的属性（ReplicateSubobjects这个方法能看出来，只有是replicate的才会同步组件）。

- 路径稳定的对象，这些对象的路径是什么，为什么是稳定的？IsNameStableForNetworking这个方法作用？
> c++构造函数中创建的Subobject都是路径稳定的。因为newObject创建c++对象的时候这些就会创建吧，就会加载到内存中了，服务器传SubObject的名称，客户端就能通过名称和Outer在内存中找到，所以这样就是路径稳定的吧，目前感觉是这样的。
> 地图中摆放的actor也是路径稳定的。是因为地图会存放在磁盘中，其中的Actor也都是放在磁盘的，我们从磁盘中加载一个资源，服务器和客户端的路径都是一样的，所以这种也是路径稳定的吧，目前感觉是这样的。

- UClass是网络稳定的么？
> 看样子是稳定的，UClass的生成是在执行main函数之前的static变量中调用的，也就是服务器客户端都存在，自然算是网络稳定的。进而的所以CDO也是稳定的。

- 客户端GuidCache->IsExportingNetGUIDBunch这个标志位在哪读出来的？
> 这个标志位的意思就是当前这个Bunch里面是否包含ExportNetGUID。服务器在构建发送Bunch的时候会序列化Object，在序列化的时候如果Object是网络稳定的，就会将Object的NetGUID额外写到ExportBunch里面并设置Bunch的bHasPackageMapExports标志位为true，在SendBunch方法里面会将ExportBunch部分放到主体Bunch之前发往客户端，客户端检测到bHasPackageMapExports标志位为true，就会设置IsExportingNetGUIDBunch这个标志位为true，用来标记收到的这个Bunch里有ExportBunch部分。

- ExportNetGUID方法为什么这么设计呢？为什么最终发给客户端的Bunch头部是Export的那个Bunch呢？
> 首先ExportBunches里面存放的这个ActorChannel中Actor的那些静态的UObject信息，比如Actor的CDO，CDO的Outer（UPackage），Actor身上挂着的Component。在客户端的ReceivedRawBunch这个方法中，就会优先解析ExportBunch里的内容，就是加载这些静态Object，然后注册NetGUID。然后通过ReadContentBlockPayload读取Component信息，如果发现Component是静态的，那已经加载过了，如果发现Component是动态的，那就会加载动态的。

- bunch里面的bHasMustBeMappedGUIDs这个是干嘛用的？
> 用于客户端标记收到的Bunch中是否有MustBeMapped部分，这个部分保存的是开启异步加载情况下路径稳定的NetGUID。

- 客户端在确定MustBeMappedGUIDs中的GUID对象是否加载的时候，在ReceivedBunch方法中如果在GuidCache中找不到NetGUID，就会出错说明有问题。这个NetGUID的注册是什么时候执行呢？
> 在服务器上，MustBeMappedGuidsInLastBunch这个Bunch的写入是在InternalWriteObject方法的最开头，写入的都是那种可以异步加载的静态的Object。IsExportingNetGUIDBunch这个Bunch会将静态的Object也写入进去。
> 在客户端上，收到的Bunch的组成是ExportingNetGUIDBunch + MustBeMappedGuidsInLastBunch + 具体的数据Bunch，客户端会在ReceivedRawBunch方法优先解析ExportingNetGUIDBunch中的NetGUID并注册，所以执行到ReceivedBunch方法中的时候MustBeMappedGUIDs这个里面的NetGUID都是完成注册的了。

- 同步Actor是流程是什么呢？
> 1 同步Actor前的准备，在服务器上，主要通过NetDriver::TickFlush方法来开始，会调用到ServerReplicataActors方法，这个方法里面首先通过ServerReplicateActors_PrepConnections方法确定有几个ClientConnection可以同步，然后会在ServerReplicateActors_BuildConsiderList这个方法中构建可同步的ConsiderActorList，之后会在ServerReplicateActors_PrioritizeActors这个方法中对ConsiderActorList进行优先级排序，之后会在ServerReplicateActors_ProcessPrioritizedActors方法中对排好序的ConsiderActorList数组中每个Actor，创建ActorChannel，最后就是通过ActorChannel的ReplicateActor方法来同步Actor了。
> 2 同步Actor部分，继续上边的描述，起始位置是ReplicateActor方法，然后会调用SerializeNewActor方法来序列化Actor，再SerializeNewActor方法里调用SerializeObject来序列化Actor（将Actor的NetGUID注册，然后将NetGUID写入Bunch里），然后会再次调用SerializeObject来序列化Actor的Archetype（一般Archetype都是uclass中的cdo，所以是网络稳定。在通过SerializeObject序列化网络稳定Object的时候会将Object的NetGUID，Object的Flags以及outer的NetGUID，outer的Flags以及outer的路径和Object的路径写入ExportBunch里面，在最后SendBunch的时候会将ExportBunch写在Bunch的前部）。然后会再次调用SerializeObject来序列化Actor所在的Level（Level也是网络稳定的对象，和Archetype序列化流程一样，需要注意的只有第一次会完整序列化，后续都是序列化NetGUID就行了）。最后会序列化Actor的Location, Rotation, Scale, Velocity信息，至此初次同步Actor的流程就结束。
> 3 客户端接收，起始位置是在NetDriver的TickDispatch方法中，之后会在ReceivedPacket方法中创建出Actor的ActorChannel，然后会在ReceivedRawBunch方法中优先处理Bunch中的ExportBunch部分，也就是网络稳定的那部分优先注册解析NetGUID，后续就是会走到ReceivedBunch方法中，然后走到ProcessBunch方法里面，里面就是会通过SerializeObject方法找出Bunch中Actor的Archetype以及Level，然后就是通过SpawnActorAbsolute方法通过Archetype来生成，生成后根据Bunch中的Location等信息在设置到Actor上。

- Actor::IsNetRelevantFor 这个方法是怎么实现相关性判断的呢？
> 就是根据距离判断，根据参数一般是playercontroller中的viewtarget和actor之间距离的平方和NetCullDistanceSquared（蓝图中可以配置的）来进行大小判断，<= 就是相关，>就是不相关。

- Actor上的这个变量bOnlyRelevantToOwner是干什么用的呢？
> 服务器再同步Actor的时候会再ServerReplicateActors_PrioritizeActors方法中进行判断，如果ClentConnection中的ViewTarget和Actor的NetOwner不同，就不会同步。一般来说ViewTarget就是PlayerController控制的那个Pawn，Actor的Owner可以自己设置。

- UNetConnection中的GetUChildConnection方法是干什么的？
> ue注释Represents a secondary split screen connection that reroutes calls to the parent connection.可能是双人成行那种分屏的设计？

- 同步Actor的属性流程是什么呢？
> 同步Actor的属性也是在同步Actor的基础之上，所以也是从TickFlush开始，经历一系列的方法到ReplicateActor方法中，在SerializeNewAcdtor方法之后调用的ActorReplicator::ReplicateProperties方法里同步。整个属性同步流程就分成两个部分，第一个部分是初始化创建，第二部分是比较属性改变进而写入Bunch。
> 1 第一部分，这部分的开始是在ActorChannel的创建完成后会执行SetChannelActor方法将Actor和ActorChannel相关联，在这个方法里面会执行ObjectReplicator的创建，进而触发RepLayout，RepState，RepChangelistMgr，RepChangedTracker的创建。RepLayout里会将可复制的属性信息放到Parents和cmds数组中以及复制条件啥的，总的来说作用是存储可复制属性的布局。客户端属性改变的判断是通过RepState中的StaticBuffer来完成的，而服务器属性改变的判断是通过RepchangeListMgr中的StaticBuffer来完成的。RepChangedTracker是条件属性赋值相关的。
> 2 第二部分，这部分的开始是在ActorReplicator::ReplicateProperties这个方法中，这个方法中首先会比较属性是否改变，将改变属性的序号记录到RepChangelistMgr的RepChangelistState结构中，然后回将改变的属性序列化到局部Bunch里面，然后会序列化自定义条件属性，再然后会序列化RPC，最后将局部Bunch写入到发送Bunch里面。

- Actor A中有一个Replicate的Actor成员变量B，他是怎么同步到客户端的？
> Actor都是通过创建ActorChannel来进行同步的。分成两种情况，
> 情况1：在同一帧中B比A先同步过来，这也就是正常流程，客户端在解析A的属性的时候，会找到B，然后赋值给A的成员变量。
> 情况2：在同一帧中A比B先同步过来，这就会导致在赋值A成员变量的时候找不到B，然后就会把B的Replicator放到UnMappedReplicators数组里面，随后TickFlush方法里面处理当前帧解析出来ObjectReplicator，如果其在UnMappedReplicators数组里面，就执行UpdateUnmappedObjects方法重新映射赋值。

- 现在知道为啥单机模式下不走onrep方法了么？
> onrep方法是通过CallRepNotifies这个执行到的，正常流程是在ProcessBunch方法里走到，也就是会在NetDriver::TickDispatch方法里执行到，而NetDriver的创建在客户端是通过InitConnect来创建的PendingNetGame进而创建出NetDriver，在服务器是通过InitListen来创建的，单机应该不会创建NetDriver吧，自然也就不会走onrep了吧。

- 同步SubObject的流程是什么呢？
> 同步SubObject是在同步Actor的基础之上，所以也是从TickFlush开始，经历一系列的方法到ReplicateActor方法中，在SerializeNewActor方法和ReplicateProperties方法之后调用的ReplicateSubobjects方法里面会进行SubObject的同步。在这个方法中就是遍历可复制的Component，然后先同步Component中的SubObject之后在同步Component（需要注意的是ue默认同步到Component属性行这一层，要想同步Component上的SubObject的属性就需要重写ReplicateSubobjects方法）。同步Component的方式是首先创建Component对应的ObjectReplicator，然后通过ObjectReplicator::ReplicateProperties方法先把Component上的属性同步到Bunch中。

- ActorA里面有个可同步的UObject类型的属性UObjectB，这个属性里也有可同步的UObjectC，UObjectC里面有一些需要同步的属性D，这是怎么同步到客户端的？
> ActorA同步UObjectB属性应该是通过属性同步的流程，但属性同步的流程只是同步了UObject,也就是让客户端创建出了UObjectB，没有同步UObjectB的属性，所以还需要重写actor的ReplicateSubObjcet方法来同步UObjectB的属性，这时客户端就会创建出UObjectC了，但是UObjectC的属性也没有同步下来，所以还需要重写UObject的ReplicateSubObjcet方法来同步UObjectC的属性。这里需要重写ActorA的ReplicateSubObjcet方法的原因是默认的ReplicateSubObjcet这个只会同步component，而UObject需要我们自己加上。

- 在可复制的Component中写一个Replicated的属性，这个属性的同步还用重写ReplicateSubobject方法么？不重写的话他能同步到客户端么？
> 看上去属性同步的时候，会将属性写入到Bunch里面，如果是UObject类型的就会调用serializeObject来写入，这个方法不会写入属性，要想执行到这个方法还需要重写IsSupportedForNetworking方法来保证生成NetGUID。重写的ReplicateSubobject方法首先会找到SubObject的ObjectReplicator，进而调用ReplicateProperties方法来将SubObject的属性写入到Bunch。所以要想同步SubObject的属性，首先需要在Actor上标记SubObject为Replicated，然后重写ReplicateSubobject方法和GetLifetimeReplicatedProps方法，还需要SubObject实现IsSupportedForNetworking方法才行。

- 当客户端接收到一个NetGUID的时候，其对象还未创建，这时是怎么延迟求解的呢？
> 好像有两个种情况，第一种情况是路径稳定的Object，客户端会在硬盘中找对应的Object然后加载，如果开启了异步加载就无法立即求解出了。第二情况是动态的Object，客户端在收到NetGUID的时候，可能服务器还没同步过来，客户但还没创建，这样也无法立即求解出。为了解决这两种情况，就有了对应的延迟求解机制，对于第一种情况开启了异步加载，服务器在序列化Object的时候会将NetGUID放入到MustBeMappedBunch里面，然后放入到exportBunch后边一起发往客户端，客户端收到Bunch如果有路径稳定的Object没加载出来，就会把Bunch放入到QueueBunch里面，等待TickFlush里对QueueBunch的处理。对于第二种情况，是因为两个Object同步顺序导致的，比如ObjectA中有属性ObjectB，客户端在处理ObjectA的时候ObjectB还没有同步。在这种情况下在序列化ObjectB属性的时候，因为客户端还没有，就会把对应属性的ObjectReplicator放到UnmappedReplicators数组里面，等待TickFlush里对UnMappedReplicators的处理。
> 引擎的Tick顺序是客户端的TickDispatch，客户端的TickFlush，RunTickGroup，服务器TickFlush。
> 在客户端的TickFlush里面首先就会执行ActorChannel::Tick方法，Tick里面会执行ProcessQueuedBunch方法，也就是通过ProcessBunch方法来处理QueuedBunch里面的Bunch，然后会对这一帧新解析出来的NetGUID进行处理，如果新NetGUID是在UnmappedReplicators数组里的，那么就执行UpdateUnmappedObjects方法来更新之前未解析成功的属性。

- 我在controller的服务器上调用组播，其他玩家的controller为什么不会同步?
>服务器上会存在ClientNetConnections数组，数组里每个元素都会有个PlayerController，所以每个客户端上的PlayerController都是不一样的，只会给controller自己对应的客户端发，而不会给其他客户端发。

- 对于一个Actor，如何能让自己有所属链接呢？也就是GetNetConnection返回不是nullptr
> 1 设置自己的owner为拥有连接的PlayerController，或者自己owner的owner为拥有连接的PlayerController
> 2 这个Actor必须是Pawn并且Possess了拥有连接的PlayerController
> 3 这个Actor设置自己的owner为拥有连接的Pawn

- Obj->GetClass()，Actor->GetArchetype()，CDO这几个有什么区别呢？
> // (The archetype object is the object that's edited when properties are changed in the Blueprint editor details inspector.
> GetClass就是返回Object的UClass，GetArchetype是返回Object的原型，原型是带有默认属性值的对象，COD是Object的类默认对象，再创建UClass的时候创建，里面存储的都是默认值。
> gtp的回答：
> 1. 对象是基于 Archetype 创建的实例
>当使用 Archetype 来创建对象实例时，该对象会从特定 Archetype 复制默认属性，而不是从类默认对象（CDO）复制。这种方式允许开发人员创建具有特定配置的对象实例，而不需要改变全局类默认值。示例：你有一个名为 AEnemyCharacter 的敌人角色类，并且你希望有一个强版敌人和一个普通版敌人。你可以创建两个不同的 Archetype（或称为蓝图），一个用于普通敌人，一个用于强版敌人。当你实例化一个敌人对象时，可以指定使用哪个 Archetype。此时，这个敌人对象的 Archetype 将会不同于 CDO，因为它的默认属性值是从 Archetype 而不是 CDO 复制的
>2. 对象是在运行时动态生成的
>运行时动态生成的对象可以通过特定的模板（`Archetype`）来创建，这样它们不会直接使用 `CDO` 的默认值。 示例：在游戏运行时，你可能会从某个特定的 `Archetype` 动态生成一个角色或物品，而不是使用默认的类实例化方法。这种情况下，生成的对象的默认属性是基于 `Archetype` 的值，而非 `CDO` 的值。
>3. 对象是通过蓝图编辑器实例化的
>在蓝图编辑器中，你可以创建不同的蓝图类，这些蓝图类实际上是 `UClass` 的子类，并且每个蓝图类可视为一个 `Archetype`。示例：你有一个基础类 `AVehicle`，并且在蓝图编辑器中创建了两个不同的蓝图类 `SportsCar` 和 `Truck`，它们分别具有不同的默认属性。当你使用这两个蓝图类来创建对象时，尽管它们在代码上继承自相同的 `AVehicle` 基类，但它们的 `Archetype` 将不同于 `AVehicle` 的 `CDO`。
>4. 使用修改后的默认属性
>某些情况下，你可能会修改特定对象的某些默认属性值，使其与 `CDO` 不同，同时将这些不同的属性值保存为一个 `Archetype`。示例：你可以在编辑器中调整某个实例的属性值，然后将这个实例保存为一个新的 `Archetype`，这种操作会导致这个对象的默认属性值与其 `UClass` 的 `CDO` 默认属性值不同。

- 项目中，联机模式下客户端链接服务器之后，服务器创建playerController之后创建pawn，地图是加载完的。单机模式下创建playerController之后创建pawn的时候，地图没加载完？
> 看上去联机模式下，服务器在加载完地图等待客户端的链接，当客户端加载完地图发送消息给服务器后，服务器创建playerContrller进一步创建pawn，这时候地图肯定是加载完的。
> 但是在单机模式下，在loadmap中会首先通过localplayer创建对应的PlayerController进一步创建pawn，但是地图加载完的事件PostLoadMapCaller是在PlayerController创建之后广播，也就是会认为地图没有加载完。注意单机模式下会有localPlayer。

- 项目中，咱们GameState在客户端是怎么保证第一个同步过来的呢？
> 看上去也没保证第一个同步过来吧 ，客户端会将收到的Bunch都放到QueueBunch里面，然后再TickFlush里面调用ProcessQueuedBunch方法来对QueueBunch数组进行处理，在处理的时候就判断下QueueBunch[0]号元素是否是GameState，不是就提前返回了
- rpc里写一个资源，这个资源的同步是怎样的？比如说在rpc的参数是一个蒙太奇资源
> 客户端在接受RPC是通过ObjectReplicator::ReceiveBunch方法来接受的，在FObjectReplicator::ReceivedRPC方法中会序列化RPC的UObject类型参数，如果客户端还没有，就会将其NetGUID添加到UnmappedGuids中，如果我们开启了延迟求解RPC的话就会将ObjectReplicator添加到UnMappedReplicated数组里面，等待延迟求解。如果我们没开启延迟求解RPC的话就会直接调用RPC，参数是nullptr。
- 如何使用pushmodel呢？
> 需要开启pushmodel，是在GetLifetimeReplicatedProps方法中标记属性是否开启pushmodel（bIsPushBased标志位），每次修改属性时还需要标记dirty。然后在CompareParentProperties比较属性的方法中，如果标记pushmodel的属性为dirty才会进行比较属性是否改变，如果不是dirty就不比较了。
- UFunction里面能放裸c++指针参数么
> 1 不能，裸指针没纳入到gc系统中，可能会被gc清掉
> 2 裸指针没有序列化的能力，无法网络同步
> 3 裸指针没有反射的能力，无法被蓝图调用到
- RPC的UFunction里面可以放c++结构体
> 1 UFunction会自动把函数参数都创建FProperty
> 2 裸c++写NetSerialize方法，就可以同步







